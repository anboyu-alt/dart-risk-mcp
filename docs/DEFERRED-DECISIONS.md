# 허락이 필요한 작업 목록

자율 감사 중 발견했지만 **함부로 고치면 위험한** 것들. 각 항목은 무엇이
문제인지·왜 위험한지·어떤 선택지가 있는지를 적고, 제작자의 판단을 기다린다.

고쳐도 되는 것(사실 표기 오류, 조용한 절단, 죽은 배선 등)은 이 목록에 없다 —
그건 바로 고치고 릴리스한다.

**현재(2026-09-01): 1~9는 전부 처리됐고, 10~13이 열려 있다.** 10~13은 전부
**측정이 끝나 있고 판단만 남은** 것들이다 — 무엇을 재야 하는지까지 각 항목에
적어 뒀다.

---

## 1. ~~신호 신설 — 상장폐지 위기 계열의 공백~~ — **해소 확인(2026-08-30)**

**이 항목은 낡은 기록이었다.** 적어 둘 때 CLAUDE.md에는 이미 해소 기록이
있었는데(2026-08-23) 여기만 따라오지 않았다. 공백을 메운 것은 v1.12.2의
`DELISTING_RISK`와 v1.12.3의 `WATCH_ISSUE`다.

### 두 번 쟀다

**① 고정 코퍼스(1년, 고유 제목 952종)** — 상장폐지 계열 1,018건이 **전부**
신호에 잡힌다. 무신호 0건.

    상장폐지          378  DELISTING_RISK 375
    상장적격성실질심사  245  DELISTING_RISK 227 · WATCH_ISSUE 28
    관리종목          171  WATCH_ISSUE 171
    개선기간부여       109  DELISTING_RISK 109
    상장폐지사유발생     88  DELISTING_RISK 88
    정리매매           27  DELISTING_RISK 27

**② 시장 전수(90일, 54,485건 · 상한 도달일 0)** — 픽스처는 「신호가 붙는
제목」만 담아 무신호를 볼 수 없다. 그래서 원본을 다시 훑고, 검색어에
「매매거래정지」까지 **일부러 넓게** 넣어 우리가 못 잡는 것이 드러나게 했다.

    퇴출 계열 573건 → 관찰 326 · 강등 8 · 무신호 239

무신호 239건을 **전수로 분류**했더니 전부 **퇴출과 무관한 정례 매매정지**였다.

    94  주권매매거래정지 (주식의 병합, 분할 등 전자등록 변경, 말소)
    54  주권매매거래정지해제 (액면병합 주권 변경상장)
    15  매매거래정지및정지해제(중요내용공시)
    15  주권매매거래정지해제 (감자 주권 변경상장)
    23  (단일판매공급계약)·(무상증자)·(주식분할) 등 기업행위

이건 **v1.12.1에서 이미 판단한 자리**다 — 그때 「"거래정지"·"매매정지"
키워드를 제거했다(304건 중 조회공시 계열은 4건뿐, 나머지는 주식병합·감자·
무상증자 등 정례적 매매정지)」고 실측 근거와 함께 기록했다. 오늘 재보니
그 판단이 여전히 맞다.

강등 8건도 전수 확인했고 전부 정당하다 — 「상장폐지사유 **미해당**」·
「실질심사 대상 **제외** 결정」·「합병결정 **철회**」(R6·R2).

### 결론

신호를 새로 만들 이유가 없다. 선택지 (a)(키워드 확장)도 필요 없다 —
넓히면 위 239건(정례 매매정지)이 들어와 v1.12.1이 고친 오탐을 되풀이한다.

## 2. ~~`_format_amount`의 표기~~ — **처리됨(2026-08-30)**

**적어 둘 때 문제를 축소해서 봤다.** 제목이 「1억 차이」였지만 실제로는
**1조를 넘으면 자릿수 감각이 통째로 사라졌다**.

    삼성전자 자산총계 514,531,948,000,000원
      옛 표기  5145319억원     ← 자릿수 구분 없음. 「5,145,319억」인지 셀 수 없다
      새 표기  514.5조원

절삭↔반올림의 1억 차이도 함께 정리했다(반올림). 실측 골든 40개 파일이
바뀌었고 값 자체는 그대로다 — 바뀐 것은 **읽을 수 있는가**다.

    - 매출액: 6413억원   →  - 매출액: 6,413억원
    - 재고자산: 568억원  →  - 재고자산: 569억원   (절삭 → 반올림)

⚠ **뷰어는 건드리지 않았다** — 뷰어의 `fmtKRW`는 억 단위까지만 쓰고 조 단위
자리에 오지 않는다. 두 레이어의 차이는 여전히 문서화된 대로다.

---

## 3. ~~도구 출력의 날짜 표기 혼재~~ — **해소 확인(2026-08-30)**

**전제가 달랐다.** 골든 전수를 겹침 없이 세고 원문 인용 골든을 분리했다.

    도구 생성  5,580   YYYYMMDD 5,123(92%) · YYYY.MM.DD 314 · YYYY-MM-DD 134 · YYYY-MM 9
    원문 인용    394

우리가 만드는 표기는 **`_fmt_date8` 하나뿐**이고 일관되게 `YYYY.MM.DD`다.
`YYYYMMDD`가 아닌 것은 전부 **값의 출처**가 그 표기다 — `affiliates`
최초취득일·`fund_usage` 납입일은 DART가 점 표기로 주고, 대시 표기는 금감원
카탈로그 보도자료 날짜(61)와 `majorstock`의 `rcept_dt`(23)다. 즉 선택지 (a)
「우리가 만드는 줄만 통일」은 **이미 참**이었고, 남은 혼재를 없애려면 DART가
준 문자열을 다시 써야 한다 — 그러면 이 항목이 스스로 경고한 위험(원문 표기와
우리 표기의 구분 소멸)이 현실이 된다.

**부수 발견 1** `search_market_disclosures`의 창 라벨이 같은 포맷을 **손으로
한 번 더 조립**하고 있었다(출력은 같지만 8자리가 아니면 쓰레기가 나온다).
헬퍼 호출로 바꿨다.

**부수 발견 2** `majorstock.json`의 `rcept_dt`는 **`2026-06-08`**로 온다 —
같은 이름의 필드가 `list.json`에서는 `20260608`이다. 렌더가 `len(dt) == 8`로
갈라 통과시키는데, 이걸 버그로 보고 `dt[:8]`로 자르면 **`2026-06-`**가 된다.

⚠ **1차 측정이 두 군데 틀렸다** — `YYYY-MM` 정규식이 `YYYY-MM-DD`의 앞부분을
겹쳐 세 두 수가 항상 같았고, 원문 골든 판별이 `stem.startswith("doc_")`인데
파일명은 `회사_doc_…`라 한 건도 안 걸려 「원문 인용 0」이 나왔다.

`tests/test_date_format_sources.py`가 고정.

---

## 4. ~~`fetch_indicator_history` — 죽은 공개 API~~ — **처리됨(2026-08-30)**

제작자 승인으로 처리. **목록이 두 방향으로 틀려 있었다.**

AST 전수(정규식이 아니라 `ast.Call`)로 재보니 미사용 공개 함수는 5개가 아니라
**7개**였고, 지목된 것 중 `build_note_summary`는 **실제로 쓰이고 있었다**
(`server.py:3670`).

    calculate_risk_score          v0.8.5가 점수 노출을 금지하며 렌더에서 빠졌다
    estimate_crisis_timeline      「위기 타임라인」이 v0.8.5로 출력에서 제거됐다
    fetch_executive_roster        `_detail` 판으로 대체됐다
    fetch_indicator_history       폐기된 SE(#239) 화면용
    is_false_amendment            2026-08-26 근본 수정으로 우회가 불필요해졌다
    resolve_disclosure_row_…      하위 호환 래퍼
    summarize_note_sections       도구가 `classify_note_title`+`scan_note_titles`를 직접 쓴다

### 지우지 않았다 (선택지 (b))

이 패키지는 **PyPI에 배포된다**(`publish-pypi.yml` · README `pip install
dart-risk-mcp`). `core.__all__`은 외부에서 import 가능한 진짜 공개 API라,
삭제는 되돌리기 어려운 **외부 영향**이다.

그리고 위험 목록의 「검증된 적이 없다」는 표현도 정확하지 않았다 — 이것들은
**테스트가 있다**(각 3~21개 호출). 「검증 안 된」 코드가 아니라 「검증됐지만
안 쓰는」 코드다.

### 대신 한 것

1. 각 함수 독스트링에 **「현재 프로덕션 소비처 0」** 명시 — 다음 사람이
   「있으니 쓰면 되겠지」 하지 않도록. `is_false_amendment`는 독스트링이
   **없어진 호출부를 설명**하고 있어서 그것도 바로잡았다.
2. `server.py`의 **죽은 import 2개 제거**(`calculate_risk_score`·
   `estimate_crisis_timeline`) — 내부 변경이라 외부 영향 없음. 특히 점수 함수가
   서버 네임스페이스에 남아 있던 것은 v0.8.5 관점에서도 정리할 값이었다.
3. **`tests/test_unused_exports.py`로 목록을 못 박았다** — 위험 목록이 적은
   진짜 문제(「있는 줄 알고 새 코드가 기댄다」)의 durable한 해법이다. 새 죽은
   export가 생기면 실패하고, 쓰이게 되면 목록에서 빼라고 실패한다.
4. CLAUDE.md의 낡은 서술 정정 — `find_actor_overlap`이 `fetch_executive_roster`를
   쓴다고 두 곳에 적혀 있었으나 실코드는 `_detail` 판을 쓴다.

### 삭제를 원한다면

(a)는 여전히 가능하지만 **deprecation 주기**를 거쳐야 한다 — 한 릴리스에서
`DeprecationWarning`을 붙이고 다음 메이저에서 제거. 지금 판단으로는 얻는 것
(코드 몇 줄)보다 잃는 것(외부 import 파손 가능성)이 크다.

## 5. ~~「N개사」류 문서 수치의 재측정 불가~~ — **처리됨(2026-08-30)**

**전제가 절반은 틀렸다.** CLAUDE.md의 「N개사」 51건을 전수로 훑었다.

    같은 줄에 측정 시점(날짜·버전) 있음   47 / 51
    문단 안에 재현 근거 경로 있음          32 / 51

선택지 (a)「시점 병기」는 이미 92% 되어 있었다. 진짜 빠진 것은 **표본 목록**
이었고, 그것은 측정 스크립트에서 그대로 복원됐다 —
`tests/fixtures/samples/measurement_cohorts.json`에 9개 표본(39·38·25·20·15·
12·12·10·8사)의 회사 이름을 담았다. **값이 아니라 표본을 고정한다**: 시장이
달라지면 건수는 바뀌는 게 맞고, 바뀌었는지 알려면 같은 회사를 다시 재야 한다.

**부수 발견** 고정하자마자 불일치가 하나 드러났다 — 매입채무 별칭은 **39사**를
넘겼는데 `dart_client.py` 주석과 CLAUDE.md는 「38개사」라 보고한다. 어느 쪽을
지울지는 재측정 전에는 알 수 없어 `n`(표본)과 `doc_n`(보고 값)을 **따로**
들고 사유를 남겼다.

`tests/test_measurement_cohorts.py`가 고정.

---

## 6. ~~`scripts/`의 조용한 상한~~ — **처리됨(2026-08-30, 선택지 (a))**

`backfill_corp_aliases.py`·`backfill_renames.py`는 30일 청크 × `max_pages=60`
(= 6,000건)으로 훑는다. 두 독스트링 다 「90일이면 상한에 걸리므로 30일 청크로
순회한다(**누락 방지**)」라 적으면서, **그 30일 청크가 다시 상한에 닿았는지는
보지 않았다**. 닿으면 그 청크 뒷부분이 조용히 사라진다 — 별칭이면 그 회사가
옛 이름으로 검색되지 않고, 개명이면 행위자 이름 병합이 그만큼 빠진다.
`backfill_sightings.py`는 같은 자리에서 이미 청크별 절단을 세어 알리고 있었다
(셋 중 둘만 빠져 있었다).

**상한도 청크 길이도 올리지 않았다** — 예산·실행 시간이 달라지고, 이미 쌓인
비공개 데이터와의 정합이 흔들린다. **닿았다는 사실만** 알린다.

⚠ **한 번 위험하게 짰다가 되돌렸다.** 처음엔 `collect_renames`의 반환 dict에
`_truncated_chunks` 키를 넣었는데, 그 dict는 **키가 corp_code**라 `main`의
`e["names"]`가 죽고 `merge_renames`가 그 키를 **비공개 sightings 파일에 써
넣는다**. 이 항목이 경고한 바로 그 위험이었다. 로그로만 알리고,
`backfill_corp_aliases`는 이미 따로 반환하는 `stats`에 담았다.

`tests/test_backfill_cap_reporting.py`가 고정(상한 값 불변까지 함께).

---

## 7. ~~`dfOcr`/`dsRsOcr` 응답 필드 미검증~~ — **해소(2026-08-30)**

**라이브 사례를 찾을 필요가 없었다 — 레포 안에 공식 명세가 있다.**
`opendart_api_guide.md`의 고유 응답 필드 표와 코드를 대조하니 네 서브타입이
전부 맞았다.

    dfOcr        df_cn · df_amt · df_bnk        ✓
    bsnSp        bsnsp_cn · bsnsp_rs            ✓
    ctrcvsBgrq   rq_rs                          ✓ (2026-08-04 수정분)
    dsRsOcr      ds_rs                          ✓

선택지 (b)의 방어 코드는 넣지 않았다 — 전제(「한 번도 확인된 적이 없다」)가
사라졌고, `_distress_summary`가 폴백으로 내는 「부도」·「해산사유 발생」은
서브타입 라벨이라 **거짓 진술이 아니다**.

⚠ 명세에 있지만 안 읽는 필드가 있다(`df_rs` 부도사유·`dfd` 최종부도일자).
**라이브 사례가 없어 렌더를 확인할 수 없으므로 넣지 않았다** — 검증 못 한
필드를 화면에 올리는 것이 이 프로젝트에서 반복된 사고다.

`tests/test_distress_fields_vs_spec.py`가 명세 대조를 기계로 수행한다.

---

## 8. ~~뷰어 피드의 「MAX」 배지~~ — **처리됨(2026-08-30)**

제작자가 (b)를 골랐다: 집합에 맞는 한국어 사실 표기로 교체.

**조사하고 보니 취향이 아니라 결함이었다.** 배지 조건이 「이 행이 헤드라인
**신호 키**를 담고 있나」라, 그 키를 가진 행이 여럿이면 전부 붙는다.

실측(15개사 1년, 관찰 행 215건):

    HLB       16행 중 **14행(88%)**   헤드라인 CB_BW · CB 공시가 14건
    오르비텍   17행 중 10행(59%)
    코아스     24행 중 12행(50%)
    합계                59행(**27.4%**)

「최대」라는 최상급이 대부분의 행에 찍히고 있었다. 참고로 「먼저」 배지는
13.0%이고, CLAUDE.md는 그 언저리를 「배지가 아니라 기본값이 되는 경계」로
기록해 뒀다.

**집합은 그대로 두고 낱말만 사실에 맞췄다** — 배지 자체는 「이 회사에서 가장
무거운 유형이 이 줄에 있다」는 쓸모 있는 정보라 없애지 않았다.

    피드      MAX                  → 대표 유형
    상세 헤더  SIGNAL · TOP WEIGHT  → SIGNAL · HEADLINE TYPE
    상세 META  이 기간 최상위        → 이 기간 대표 유형

곁가지: `.badge-max`의 `background: var(--red)fff;`는 **유효하지 않은 선언**
이었다(다음 줄이 덮어써서 화면은 멀쩡했다). 지웠다.

`tests/test_headline_badge_wording.py`.

## 9. ~~`track_capital_structure`가 한정층을 쓰지 않는다~~ — **처리됨(2026-08-30)**

제작자가 (a)를 골랐다: 「정확한 게 더 중요하다」.

**측정하고 보니 (a)의 전제가 틀렸다.** 두 가지가 드러났다.

1. `detect_capital_churn`은 **이미** 정정(`is_amendment`)과 「발행결과」·
   「결과보고서」(`CHURN_RESULT_MARKS`)를 뺀다. 내가 적은 공백이 실제보다
   넓었다. 새로 빠지는 것은 25개사 3년에서 **69건**이다 —
   R3 자회사 34 · R5 `[첨부정정]` 20 · R2 해제·철회 9 · R2c 「청약결과」 6.
2. `Qualified`에 규칙 id를 신설할 필요가 **없었다**. 25개사에서
   「중복·타사만 뺀다」와 「observed만 남긴다」가 **결과가 완전히 같다** —
   자본 이벤트 키가 R1·R1b·R1c·R4·R6로 강등되는 일이 실제로는 없기 때문이다.
   그리고 `analyze_company_risk`는 **이미 `observed_events`만 넘기고 있었다**.
   즉 문제는 「API가 없다」가 아니라 **한 함수의 두 호출부가 다른 것을 셌다**는
   것이었다.

그래서 형제 호출부와 같은 방식(`observed_events`)을 쓰고, 안 쓰는 공개
필드는 만들지 않았다(그건 4번의 바로 그 문제다).

**되사기·소각은 빠지지 않는다** — 강등이 아니라 방향 안내라 tier가 observed다.
`detect_capital_churn` 독스트링의 「빼면 한탑이 떨어진다」 경고와 충돌하지 않는다.

라이브: 진짜 사례 유지(한탑·코아스·제이스코홀딩스·진원생명과학·유티아이) ·
대형주 3곳 해소(SK하이닉스·이마트·고려아연). 제외 건수는 화면에 적는다.

`tests/test_capital_churn_scope.py` · CLAUDE.md 20번 항목.

---

## 10. ~~전환청구권행사가 무신호다~~ — **절반 처리(2026-09-01) · 나머지는 열려 있다**

**시장 전수로 재니 선택지 (a)의 전제가 틀렸다.** 아래 「(a) `CB_BW` 키워드 확장
— 새 taxonomy 불필요, 1.1이 이미 있다」고 적혀 있었는데, **`1.1`은
「Refixing(리픽싱)」** 곧 전환가액 **조정**이다. 후보 900건은 성격이 다른 둘이었다.

측정: 1년 전수 **271,141건 · 절단일 0**(2025-08-22~2026-08-21). 처음 수집분
269,596건에서 사업보고서 마감일 이틀(20260331·20260401)이 5,000건 상한에 닿아
수집기 자가진단이 잡았고, 그 이틀만 다시 깊이 받아 1,545건을 메웠다. 정정 제외
228,492건(정정을 빼지 않으면 무신호 집계가 부풀려진다).

### A. 조정(refixing) — **처리됨**

`CB_BW`가 「전환가액의조정」은 잡는데 **표기 변형**을 놓치고 있었다.

    전환가액ㆍ신주인수권행사가액ㆍ교환가액의조정(안내공시)   127
    신주인수권행사가액의조정 (+회차)                       33
    교환가액의조정 (+회차)                                 6
                                          합계  166건 / 75개사

첫 줄이 핵심 — **결합 표기라 어미가 「교환가액의조정」**이어서 걸리지 않았다.
키워드 2개(`교환가액의조정`·`행사가액의조정`)로 166건이 전부 덮인다. 새 개념이
아니라 이미 매핑된 `1.1`의 표기라 taxonomy 변경이 없다.
`tests/test_refix_notation_variants.py`가 고정.

### B. 행사(exercise) — **열려 있음 · 새 taxonomy가 필요하다**

    전환청구권행사 / 신주인수권행사 / 교환청구권행사 계열   735건 / 304개사

전부 observed이고 오탐도 거의 없지만(2종 5건), **담을 taxonomy가 없다.**
카테고리 1·2를 전수 확인했다 — `1.1`은 조정이고 `1.4` RCPS도 발행 조건이지
행사가 아니다. 「실제 희석이 일어난 시점」을 담는 항목이 없다.

뜻이 어긋나는 자리에 밀어 넣는 것이 `INQUIRY` 실사고(v1.12.1)의 뿌리였고
v1.13.4·v1.13.5에서 후보 7종 중 5종이 같은 이유로 기각됐다. 그래서 넣지 않았다.

**왜 아까운가** 전환청구권행사는 **기존 주주가 실제로 희석된 시점**이다. 이
도구는 발행(제목)과 전환 실적(증자·감자 현황 원장)을 각각 보는데 **그 사이의
공시 기록**이 신호에서 빠져 있다. 메자닌 창의 공시 리스트에는 나온다(분류가 별도).

**감사 결과(같은 전수)**

    부분 문자열 충돌     0
    한정층               A·B 모두 100% observed (강등 0)
    패턴 영향(A)         11개 패턴 전부 **+0** · 카드를 받는 회사 252곳 불변

⚠ **패턴 영향은 `supports_pattern`을 태워 재야 한다.** 처음엔 그것 없이 재서
「fund_diversion_chain 146 → 151곳(+5)」이라 적었는데, 방향 안내가 붙은 이벤트는
패턴 근거로 서지 못하므로 그 다섯은 실제로 임계를 채우지 못한다. 제대로 재면
기준선부터 139곳이고 변화가 없다.

⚠ 그리고 **1.1을 요구하는 패턴만 보면 안 된다** — `CB_BW`는 taxonomy를 셋
(1.1·1.5·1.6) 켠다. `signals.py`의 기존 주석이 「이 제목 하나가 RCPS(1.4)와 함께
debt_spiral의 임계를 **혼자** 충족했다(한탑 실사고형)」고 경고하는 구조다. 11개
패턴 전수로 재고, {1.1,1.5,1.6}만으로 임계를 채우는 패턴이 있는지도 따로 확인했다
(없음 — 비례 임계 도입 v1.20.13 이후 debt_spiral은 5신호 중 3개를 요구한다).

이 제목을 낸 회사 75곳 중 41곳(55%)은 이미 다른 공시로 `CB_BW`를 갖고 있었고,
처음 켜지는 34곳도 짝이 없어 어느 패턴에도 닿지 않는다. **이 변경의 효과는
관찰 목록에 사실이 보이는 것**이지 패턴이 늘어나는 게 아니다.

**선택지(B)**
- (a) 새 taxonomy 신설 — 「메자닌 행사에 따른 희석 실행」. 연쇄: `TAXONOMY` →
  `labels_ko.json` → 카탈로그 재생성 → `signals-data.json` → 뷰어. 11·12번과
  같은 부류의 판단이다.
- (b) 그대로 둔다(현재) — 메자닌 창의 공시 리스트에는 나온다.

> ⚠ 계획 단계에서 나는 이 공백을 「자기전환사채매도결정 73건이 전부 무신호」로
> 적었는데 **틀렸다**. 그 73건 중 49건이 정정이라 `match_signals`가 빈 값을 준
> 것이고, 원본 24건은 `CB_BW`가 정상적으로 잡는다. 정정을 빼고 세지 않으면
> 무신호 집계가 부풀려진다.

---

## 11. taxonomy 45종 중 13종이 발화 경로가 없다

**무엇** 주인 신호가 아예 없는 것 2종(`6.3` RCPS Accounting Restructuring ·
`7.3` Derivative Abuse), 주인이 있어도 **전부 `NON_TITLE_SIGNALS`**(관측 불가)인
것 11종(`1.2` `1.7` `2.2` `2.3` `2.5` `3.5` `4.1` `5.2` `6.2` `7.2` `8.3`).

**왜 위험한가** v1.20.2가 「요구 taxonomy를 담당하는 유일한 신호가 관측 불가라
5~6신호 패턴이 뜨지 않는다」고 기록한 것과 같은 뿌리다. 정리하려면 패턴
`signal_sequence`·카탈로그 매핑·뷰어 데이터가 연쇄로 걸린다. 그리고 일부는
**의도적 공백**이다(조사·제재의 결과를 가리키는 말은 회사 공시 제목에 없다).

⚠ `7.3`은 특히 주의 — 키워드에 `"파생상품거래"`가 있어 붙이고 싶어지지만
정의가 「투기적 남용」이라 파생상품거래손실 공시와 뜻이 다르다(2026-08-31 실측
후 신설을 되돌린 이유).

**선택지**
- (a) 13종을 전수 분류해 「의도적 공백」과 「메울 수 있는 것」을 가른다(측정만)
- (b) 주인 없는 2종을 정리한다(패턴·카탈로그 연쇄 확인 필요)
- (c) 그대로 둔다(현재)

---

## 12. 제재·벌금 신호 — 가능하지만 새 taxonomy가 필요하다

**무엇** 2026-08-31 실측(90일 전수 55,104건)에서 벌금·과징금 계열은 **방향이
안정적이고 원문 필드가 완비**돼 있다(금액 8/8 · 사유 8/8 · 기관 7/8). 다만
원본 11건에서 모·자회사 중복 3쌍을 빼면 **고유 5건/90일**(≈20건/년)이다.

    대상 2,341억 과징금(공정위) · 삼양 2,103억(공정위) · DL 8,533억 추징금
    고려제약 52억(세무조사) · 한창 8억(금융위)

**왜 위험한가** `4.3`은 **공시** 의무 위반이고 행정·형사 제재를 담는 taxonomy가
8개 카테고리 어디에도 없다 → 신설이 필요하고 그 연쇄는 `TAXONOMY` →
`labels_ko.json` → 카탈로그 재생성 → `signals-data.json` → 뷰어다. 빈도로는
정당화되지 않고, **저빈도·고중대성**(자기자본 11~21%)이라는 성격은
`DISTRESS_EVENT`와 비슷하다.

**선택지**
- (a) 새 taxonomy를 만들어 신설 — 중대성으로 판단
- (b) 그대로 둔다(현재)

근거: `docs/superpowers/specs/2026-08-31-litigation-sanction-signal-feasibility.md`

---

## 13. CHANGELOG에 `1.20.25`~`1.20.42` 18개 버전 항목이 없다

**무엇** 그 버전들은 **실재했다**(릴리스 커밋으로 확인). 변경 자체는 코드와
커밋 이력에 남아 있지만 CHANGELOG 작성이 그 구간에서 누락됐다.

**왜 위험한가** 커밋 메시지로 18개를 소급 작성하면 **없는 근거를 지어낼 위험**이
있다. 그때의 실측·판단이 CHANGELOG 문장으로 재구성되면 그것이 근거처럼 읽힌다.

**선택지**
- (a) 그 구간을 `git log v1.12.0..v1.21.20`으로 보라는 안내만 남긴다(현재 —
  CLAUDE.md 릴리스 절에 기록됨)
- (b) 커밋 메시지에서 소급 작성하되 「커밋에서 재구성」임을 명시
- (c) 그대로 둔다

---
