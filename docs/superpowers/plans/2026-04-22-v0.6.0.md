# v0.6.0 — 자본구조 추적 + 재무 이상 스캔 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 자본 이벤트 시간축 집계(`CAPITAL_CHURN`)와 재무제표 YoY 이상 탐지(`AR_SURGE`·`INVENTORY_SURGE`·`CASH_GAP`·`CAPITAL_IMPAIRMENT`)를 `analyze_company_risk`에 통합하고, 2개 신규 MCP 도구로 노출한다.

**Architecture:** 신규 DART 엔드포인트 0개. 기존 `fetch_company_disclosures`·`fetch_financial_statements` 재사용. 순수 계산 함수 2개(`detect_capital_churn`·`detect_financial_anomaly`)를 `dart_client.py`에 추가하고, 기존 `signal_events: list[dict]` 스키마에 플래그를 합산해 기존 score/pattern 파이프라인이 자동 처리하게 함.

**Tech Stack:** Python 3.11+, `mcp`, `requests`, `unittest`/`unittest.mock` (외부 의존성 추가 금지)

**관련 스펙:** `docs/superpowers/specs/2026-04-22-v0.6.0-design.md`

---

## File Structure

| 파일 | 책임 | 작업 |
|---|---|---|
| `dart_risk_mcp/core/signals.py` | 신호 키·카테고리 매핑 | 수정: `CAPITAL_EVENT_KEYS` 상수, `SIGNAL_TYPES` 5개, `SIGNAL_KEY_TO_TAXONOMY` 5개 추가 |
| `dart_risk_mcp/core/taxonomy.py` | taxonomy·복합 패턴·페이즈 매핑 | 수정: `TAXONOMY["2.7"]`, `CROSS_SIGNAL_PATTERNS` 1개, `_PHASE_MAP` 5개 키 추가 |
| `dart_risk_mcp/core/dart_client.py` | DART 래퍼·계산 함수 | 수정: `_pick_account`, `_fs_response_to_periods`, `detect_capital_churn`, `detect_financial_anomaly` 추가 |
| `dart_risk_mcp/core/__init__.py` | 공개 API | 수정: 4개 함수 export |
| `dart_risk_mcp/server.py` | MCP 도구 등록 | 수정: `track_capital_structure`, `scan_financial_anomaly` 2개 도구 추가. `analyze_company_risk` 통합. `build_event_timeline` 확장 |
| `tests/test_capital_churn.py` | capital churn 로직 검증 | 신규: 6 tests |
| `tests/test_financial_anomaly.py` | 재무이상 로직 검증 | 신규: 8 tests |
| `tests/test_v6_integration.py` | `analyze_company_risk` 통합 검증 | 신규: 3 tests (mock 사용) |
| `dart_risk_mcp/__init__.py` | 버전 | 수정: 0.5.0 → 0.6.0 |
| `CHANGELOG.md` | 변경 이력 | 수정: v0.6.0 섹션 추가 |
| `CLAUDE.md` | 개발자 가이드 | 수정: 도구 수 19→21, 신규 섹션 |

---

## Task 1: 신호 키 등록

**Files:**
- Modify: `dart_risk_mcp/core/signals.py`
- Test: `tests/test_signals_v6.py` (신규)

- [ ] **Step 1.1: 실패하는 테스트 작성**

Create `tests/test_signals_v6.py`:

```python
"""v0.6.0 신호 키 등록 검증."""
import unittest

from dart_risk_mcp.core.signals import (
    SIGNAL_TYPES,
    SIGNAL_KEY_TO_TAXONOMY,
    CAPITAL_EVENT_KEYS,
)


NEW_KEYS = ["CAPITAL_CHURN", "AR_SURGE", "INVENTORY_SURGE", "CASH_GAP", "CAPITAL_IMPAIRMENT"]


class TestV6Signals(unittest.TestCase):
    def test_new_keys_in_signal_types(self):
        keys = {s["key"] for s in SIGNAL_TYPES}
        for k in NEW_KEYS:
            self.assertIn(k, keys, f"{k} missing in SIGNAL_TYPES")

    def test_new_keys_have_taxonomy(self):
        for k in NEW_KEYS:
            self.assertIn(k, SIGNAL_KEY_TO_TAXONOMY, f"{k} missing in SIGNAL_KEY_TO_TAXONOMY")

    def test_capital_event_keys_is_frozen_set(self):
        self.assertIsInstance(CAPITAL_EVENT_KEYS, (set, frozenset))
        expected = {"3PCA", "RIGHTS_UNDER", "GAMJA_MERGE", "REVERSE_SPLIT",
                    "TREASURY", "CB_BW", "EB", "RCPS", "TREASURY_EB"}
        self.assertEqual(CAPITAL_EVENT_KEYS, expected)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 1.2: 테스트 실패 확인**

Run: `python -m unittest tests.test_signals_v6 -v`

Expected: `ImportError: cannot import name 'CAPITAL_EVENT_KEYS' from dart_risk_mcp.core.signals` 또는 `AttributeError`.

- [ ] **Step 1.3: `signals.py`에 상수·신호 키 추가**

Edit `dart_risk_mcp/core/signals.py`. `SIGNAL_TYPES` 리스트 **끝**에 다음 항목 추가:

```python
    {"key": "CAPITAL_CHURN", "label": "자본 이벤트 과다 반복", "score": 7, "keywords": []},
    {"key": "AR_SURGE", "label": "매출채권/매출 비율 급등", "score": 8, "keywords": []},
    {"key": "INVENTORY_SURGE", "label": "재고자산/매출 비율 급등", "score": 7, "keywords": []},
    {"key": "CASH_GAP", "label": "순이익·현금흐름 괴리", "score": 8, "keywords": []},
    {"key": "CAPITAL_IMPAIRMENT", "label": "자본잠식 근접", "score": 9, "keywords": []},
```

`SIGNAL_KEY_TO_TAXONOMY` 딕셔너리에 5개 매핑 추가:

```python
    "CAPITAL_CHURN": "2.7",
    "AR_SURGE": "6.1",
    "INVENTORY_SURGE": "6.1",
    "CASH_GAP": "6.1",
    "CAPITAL_IMPAIRMENT": "8.2",
```

파일 하단(모듈 수준)에 상수 추가:

```python
# 자본 이벤트로 분류되는 신호 키 (track_capital_structure에서 필터링 기준)
CAPITAL_EVENT_KEYS = frozenset({
    "3PCA",           # 3자배정 유상증자
    "RIGHTS_UNDER",   # 주주배정/일반공모 유상증자
    "GAMJA_MERGE",    # 감자
    "REVERSE_SPLIT",  # 주식병합·액면병합
    "TREASURY",       # 자사주 취득/처분/소각
    "CB_BW",          # 전환사채·신주인수권부사채
    "EB",             # 교환사채
    "RCPS",           # 전환상환우선주
    "TREASURY_EB",    # 자사주 EB
})
```

- [ ] **Step 1.4: 테스트 통과 확인**

Run: `python -m unittest tests.test_signals_v6 -v`

Expected: `Ran 3 tests in Xs — OK`.

- [ ] **Step 1.5: 커밋**

```bash
git add dart_risk_mcp/core/signals.py tests/test_signals_v6.py
git commit -m "feat(signals): register 5 v0.6.0 flags + CAPITAL_EVENT_KEYS"
```

---

## Task 2: taxonomy 2.7 + 복합 패턴 등록

**Files:**
- Modify: `dart_risk_mcp/core/taxonomy.py`
- Test: `tests/test_taxonomy_v6.py` (신규)

- [ ] **Step 2.1: 실패하는 테스트 작성**

Create `tests/test_taxonomy_v6.py`:

```python
"""v0.6.0 taxonomy·패턴 등록 검증."""
import unittest

from dart_risk_mcp.core.taxonomy import TAXONOMY, CROSS_SIGNAL_PATTERNS


class TestV6Taxonomy(unittest.TestCase):
    def test_2_7_exists(self):
        self.assertIn("2.7", TAXONOMY)
        entry = TAXONOMY["2.7"]
        self.assertEqual(entry["label"], "자본 이벤트 과다 반복")
        self.assertEqual(entry["severity"], "HIGH")

    def test_capital_churn_pattern_registered(self):
        names = {p["name"] for p in CROSS_SIGNAL_PATTERNS}
        self.assertIn("capital_churn_anomaly", names)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2.2: 테스트 실패 확인**

Run: `python -m unittest tests.test_taxonomy_v6 -v`

Expected: `KeyError: '2.7'`.

- [ ] **Step 2.3: `taxonomy.py`에 항목 추가**

Edit `dart_risk_mcp/core/taxonomy.py`. `TAXONOMY` 딕셔너리에 다음 키 추가(카테고리 2 자본구조 블록 근처):

```python
    "2.7": {
        "label": "자본 이벤트 과다 반복",
        "severity": "HIGH",
        "category": "Category 2 자본구조",
        "keywords": [],  # 복합 판정이라 키워드 없음
        "indicators": [
            "12개월 내 증자·감자·자사주 등 자본 이벤트 3건 이상",
            "CB·BW·EB 연속 발행",
            "자본구조 리듬의 비정상성",
        ],
        "field_evidence": (
            "금감원 2024·2025 주가조작 적발 사례(동성제약·헬릭스미스·셀리버리 등) "
            "공통 특징: 12개월 내 3~6건의 자본 이벤트 집중"
        ),
        "typical_timeline": "6~18개월",
        "related_patterns": ["zombie_ma", "fake_new_biz", "delisting_evasion"],
    },
```

`CROSS_SIGNAL_PATTERNS` 리스트에 항목 추가:

```python
    {
        "name": "capital_churn_anomaly",
        "signals": ["2.7", "4.3"],
        "description": "잦은 자본 변동과 공시 지연·위반이 겹치면 자본 투명성 훼손 우려",
        "severity": "HIGH",
    },
```

- [ ] **Step 2.4: 테스트 통과 확인**

Run: `python -m unittest tests.test_taxonomy_v6 -v`

Expected: `Ran 2 tests in Xs — OK`.

- [ ] **Step 2.5: 커밋**

```bash
git add dart_risk_mcp/core/taxonomy.py tests/test_taxonomy_v6.py
git commit -m "feat(taxonomy): add 2.7 capital churn + capital_churn_anomaly pattern"
```

---

## Task 3: `detect_capital_churn` TDD

**Files:**
- Modify: `dart_risk_mcp/core/dart_client.py`
- Test: `tests/test_capital_churn.py` (신규)

- [ ] **Step 3.1: 실패하는 테스트 작성**

Create `tests/test_capital_churn.py`:

```python
"""detect_capital_churn 순수 로직 검증."""
import unittest

from dart_risk_mcp.core.dart_client import detect_capital_churn


def _ev(key: str, dt: str, is_amendment: bool = False) -> dict:
    return {
        "key": key,
        "rcept_dt": dt,
        "report_nm": "",
        "is_amendment": is_amendment,
        "score": 5,
        "label": "",
    }


class TestDetectCapitalChurn(unittest.TestCase):
    def test_empty_events_returns_no_flag(self):
        r = detect_capital_churn([], 3)
        self.assertEqual(r["flags"], [])
        self.assertEqual(r["total_events"], 0)
        self.assertEqual(r["max_12m_count"], 0)

    def test_two_events_12m_no_flag(self):
        evs = [_ev("3PCA", "20250101"), _ev("CB_BW", "20250501")]
        r = detect_capital_churn(evs, 3)
        self.assertEqual(r["total_events"], 2)
        self.assertNotIn("CAPITAL_CHURN", r["flags"])
        self.assertEqual(r["max_12m_count"], 2)

    def test_three_events_12m_triggers_flag(self):
        evs = [_ev("3PCA", "20250101"), _ev("CB_BW", "20250501"), _ev("TREASURY", "20251001")]
        r = detect_capital_churn(evs, 3)
        self.assertIn("CAPITAL_CHURN", r["flags"])
        self.assertEqual(r["max_12m_count"], 3)

    def test_events_spread_over_24m_no_flag(self):
        # 이벤트를 366일 이상 간격으로 배치: 어떤 365일 윈도우에도 2건까지만
        evs = [
            _ev("3PCA", "20240101"),
            _ev("3PCA", "20250201"),   # 396일 후
            _ev("3PCA", "20260301"),   # 독립 윈도우
        ]
        r = detect_capital_churn(evs, 3)
        self.assertLessEqual(r["max_12m_count"], 2)
        self.assertNotIn("CAPITAL_CHURN", r["flags"])

    def test_amendment_excluded(self):
        evs = [
            _ev("3PCA", "20250101"),
            _ev("CB_BW", "20250501"),
            _ev("TREASURY", "20250801", is_amendment=True),  # 제외
        ]
        r = detect_capital_churn(evs, 3)
        self.assertEqual(r["total_events"], 2)
        self.assertNotIn("CAPITAL_CHURN", r["flags"])

    def test_mixed_signals_only_capital_counted(self):
        evs = [
            _ev("3PCA", "20250101"),
            _ev("SHAREHOLDER", "20250201"),   # 비자본
            _ev("EXEC", "20250301"),           # 비자본
            _ev("CB_BW", "20250401"),
            _ev("TREASURY", "20250501"),
        ]
        r = detect_capital_churn(evs, 3)
        self.assertEqual(r["total_events"], 3)
        self.assertIn("CAPITAL_CHURN", r["flags"])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 3.2: 테스트 실패 확인**

Run: `python -m unittest tests.test_capital_churn -v`

Expected: `ImportError: cannot import name 'detect_capital_churn'`.

- [ ] **Step 3.3: `dart_client.py`에 구현**

Edit `dart_risk_mcp/core/dart_client.py`. 파일 상단 import 근처에 추가:

```python
from datetime import datetime, timedelta
from .signals import CAPITAL_EVENT_KEYS
```

파일 하단(다른 detect_* 근처)에 함수 추가:

```python
def detect_capital_churn(events: list[dict], lookback_years: int) -> dict:
    """
    12개월 슬라이딩 윈도우에서 자본 이벤트 >= 3건이면 CAPITAL_CHURN 플래그.

    Args:
        events: match_signals가 반환한 신호 이벤트 리스트.
                각 항목은 {"key", "rcept_dt" (YYYYMMDD), "is_amendment", ...} 포함.
        lookback_years: 조회 기간(년). 집계 메타에만 기록되며 로직에는 영향 없음.

    Returns:
        {
            "max_12m_count": int,       # 12개월 윈도우 최대 이벤트 수
            "total_events": int,        # 필터 후 자본 이벤트 전체 수
            "by_year": dict[str, int],  # 연도별 집계 {"2024": 3, ...}
            "events": list[dict],       # 필터된 자본 이벤트 (시간 오름차순)
            "flags": list[str],         # ["CAPITAL_CHURN"] or []
            "lookback_years": int,
        }
    """
    # 1) 자본 이벤트만 필터 + 정정공시 제외
    caps: list[dict] = []
    for e in events or []:
        if e.get("key") not in CAPITAL_EVENT_KEYS:
            continue
        if e.get("is_amendment"):
            continue
        caps.append(e)

    # 2) rcept_dt 오름차순 정렬
    caps.sort(key=lambda e: (e.get("rcept_dt") or "00000000"))

    # 3) 날짜 파싱 (잘못된 포맷은 스킵)
    dates: list[datetime] = []
    for e in caps:
        raw = (e.get("rcept_dt") or "")[:8]
        try:
            dates.append(datetime.strptime(raw, "%Y%m%d"))
        except ValueError:
            continue

    # 4) 365일 슬라이딩 윈도우 최대 카운트
    max_count = 0
    for i, start in enumerate(dates):
        end = start + timedelta(days=365)
        cnt = sum(1 for d in dates[i:] if start <= d <= end)
        if cnt > max_count:
            max_count = cnt

    # 5) 연도별 집계
    by_year: dict[str, int] = {}
    for e in caps:
        y = (e.get("rcept_dt") or "")[:4]
        if y:
            by_year[y] = by_year.get(y, 0) + 1

    flags = ["CAPITAL_CHURN"] if max_count >= 3 else []

    return {
        "max_12m_count": max_count,
        "total_events": len(caps),
        "by_year": by_year,
        "events": caps,
        "flags": flags,
        "lookback_years": lookback_years,
    }
```

- [ ] **Step 3.4: 테스트 통과 확인**

Run: `python -m unittest tests.test_capital_churn -v`

Expected: `Ran 6 tests in Xs — OK`.

- [ ] **Step 3.5: 커밋**

```bash
git add dart_risk_mcp/core/dart_client.py tests/test_capital_churn.py
git commit -m "feat(dart_client): add detect_capital_churn (12m sliding window)"
```

---

## Task 4: `detect_financial_anomaly` TDD + 헬퍼

**Files:**
- Modify: `dart_risk_mcp/core/dart_client.py`
- Test: `tests/test_financial_anomaly.py` (신규)

- [ ] **Step 4.1: 실패하는 테스트 작성**

Create `tests/test_financial_anomaly.py`:

```python
"""detect_financial_anomaly 순수 로직 검증."""
import unittest

from dart_risk_mcp.core.dart_client import detect_financial_anomaly


def _base(rev=1000, ar=100, inv=100, ocf=50, ni=50, eq=500, cap=100) -> dict:
    return {
        "매출액": rev,
        "매출채권": ar,
        "재고자산": inv,
        "영업활동현금흐름": ocf,
        "당기순이익": ni,
        "자본총계": eq,
        "자본금": cap,
    }


class TestDetectFinancialAnomaly(unittest.TestCase):
    def test_ar_surge_triggers_at_50pp(self):
        # 전기 20% → 당기 71% → Δ=51%p → flag
        cur = _base(ar=710)
        pri = _base(ar=200)
        flags, _ = detect_financial_anomaly(cur, pri)
        self.assertIn("AR_SURGE", flags)
        # 49%p boundary: 당기 69% → Δ=49%p → no flag
        cur2 = _base(ar=690)
        flags2, _ = detect_financial_anomaly(cur2, pri)
        self.assertNotIn("AR_SURGE", flags2)

    def test_ar_surge_missing_field_skips(self):
        cur = {"매출액": 1000}  # 매출채권 없음
        pri = _base()
        flags, metrics = detect_financial_anomaly(cur, pri)
        self.assertNotIn("AR_SURGE", flags)

    def test_inventory_surge_boundary(self):
        # 전기 10% → 당기 70% → Δ=60%p → flag
        cur = _base(inv=700)
        pri = _base(inv=100)
        flags, _ = detect_financial_anomaly(cur, pri)
        self.assertIn("INVENTORY_SURGE", flags)

    def test_cash_gap_profit_positive_cf_negative(self):
        cur = _base(ni=30, ocf=-5)
        pri = _base()
        flags, _ = detect_financial_anomaly(cur, pri)
        self.assertIn("CASH_GAP", flags)

    def test_cash_gap_both_positive_no_flag(self):
        cur = _base(ni=30, ocf=20)
        pri = _base()
        flags, _ = detect_financial_anomaly(cur, pri)
        self.assertNotIn("CASH_GAP", flags)

    def test_capital_impairment_below_50(self):
        # 자본총계 40 / 자본금 100 = 40% → flag
        cur = _base(eq=40)
        pri = _base(eq=60)
        flags, _ = detect_financial_anomaly(cur, pri)
        self.assertIn("CAPITAL_IMPAIRMENT", flags)

    def test_capital_impairment_at_boundary(self):
        # 자본총계 50 / 자본금 100 = 50% → no flag (strict <)
        cur = _base(eq=50)
        pri = _base(eq=60)
        flags, _ = detect_financial_anomaly(cur, pri)
        self.assertNotIn("CAPITAL_IMPAIRMENT", flags)

    def test_account_name_fallback(self):
        # "영업수익"을 "매출액" 대체 명칭으로 인식
        cur = {"영업수익": 1000, "매출채권": 710, "재고자산": 100,
               "영업활동현금흐름": 50, "당기순이익": 50, "자본총계": 500, "자본금": 100}
        pri = {"영업수익": 1000, "매출채권": 200, "재고자산": 100,
               "영업활동현금흐름": 50, "당기순이익": 50, "자본총계": 500, "자본금": 100}
        flags, _ = detect_financial_anomaly(cur, pri)
        self.assertIn("AR_SURGE", flags)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 4.2: 테스트 실패 확인**

Run: `python -m unittest tests.test_financial_anomaly -v`

Expected: `ImportError: cannot import name 'detect_financial_anomaly'`.

- [ ] **Step 4.3: `dart_client.py`에 구현**

Edit `dart_risk_mcp/core/dart_client.py`. `detect_capital_churn` 아래에 추가:

```python
_FS_ALIASES = {
    "매출": ["매출액", "영업수익"],
    "매출채권": ["매출채권", "매출채권및기타채권"],
    "재고자산": ["재고자산"],
    "영업현금흐름": ["영업활동현금흐름", "영업활동으로인한현금흐름"],
    "당기순이익": ["당기순이익", "당기순이익(손실)"],
    "자본총계": ["자본총계"],
    "자본금": ["자본금"],
}


def _pick_account(fs: dict, names: list[str]):
    """fs dict에서 names 우선순위대로 첫 유효 값을 반환. 없으면 None."""
    for n in names:
        v = fs.get(n)
        if v is not None:
            return v
    return None


def detect_financial_anomaly(current: dict, prior: dict) -> tuple[list[str], list[dict]]:
    """
    당기·전기 재무 dict를 받아 4개 이상 지표 판정.

    Args:
        current: 당기 재무 {account_nm: int, ...}
        prior: 전기 재무 {account_nm: int, ...}

    Returns:
        (flags, metrics)
        flags: ["AR_SURGE", "INVENTORY_SURGE", "CASH_GAP", "CAPITAL_IMPAIRMENT"] 부분집합
        metrics: [{"name", "current", "prior", "delta", "unit", "flagged"} ...]
    """
    flags: list[str] = []
    metrics: list[dict] = []

    rev_c = _pick_account(current, _FS_ALIASES["매출"])
    rev_p = _pick_account(prior, _FS_ALIASES["매출"])
    ar_c = _pick_account(current, _FS_ALIASES["매출채권"])
    ar_p = _pick_account(prior, _FS_ALIASES["매출채권"])
    inv_c = _pick_account(current, _FS_ALIASES["재고자산"])
    inv_p = _pick_account(prior, _FS_ALIASES["재고자산"])
    ni_c = _pick_account(current, _FS_ALIASES["당기순이익"])
    ocf_c = _pick_account(current, _FS_ALIASES["영업현금흐름"])
    eq_c = _pick_account(current, _FS_ALIASES["자본총계"])
    cap_c = _pick_account(current, _FS_ALIASES["자본금"])

    # AR_SURGE
    if rev_c and rev_p and ar_c is not None and ar_p is not None and rev_c > 0 and rev_p > 0:
        r_c = ar_c / rev_c * 100
        r_p = ar_p / rev_p * 100
        delta = r_c - r_p
        m = {"name": "매출채권/매출", "current": r_c, "prior": r_p, "delta": delta, "unit": "%", "flagged": False}
        if delta >= 50:
            flags.append("AR_SURGE")
            m["flagged"] = True
        metrics.append(m)

    # INVENTORY_SURGE
    if rev_c and rev_p and inv_c is not None and inv_p is not None and rev_c > 0 and rev_p > 0:
        r_c = inv_c / rev_c * 100
        r_p = inv_p / rev_p * 100
        delta = r_c - r_p
        m = {"name": "재고자산/매출", "current": r_c, "prior": r_p, "delta": delta, "unit": "%", "flagged": False}
        if delta >= 50:
            flags.append("INVENTORY_SURGE")
            m["flagged"] = True
        metrics.append(m)

    # CASH_GAP
    if ni_c is not None and ocf_c is not None:
        m = {"name": "순이익 vs 영업현금흐름", "current_ni": ni_c, "current_ocf": ocf_c, "flagged": False}
        if ni_c > 0 and ocf_c < 0:
            flags.append("CASH_GAP")
            m["flagged"] = True
        metrics.append(m)

    # CAPITAL_IMPAIRMENT
    if eq_c is not None and cap_c is not None and cap_c > 0:
        ratio = eq_c / cap_c * 100
        m = {"name": "자본총계/자본금", "current": ratio, "unit": "%", "flagged": False}
        if ratio < 50:
            flags.append("CAPITAL_IMPAIRMENT")
            m["flagged"] = True
        metrics.append(m)

    return flags, metrics


def _fs_response_to_periods(fs_data: dict) -> tuple[dict, dict]:
    """
    fetch_financial_statements 응답(list of account items) → (current, prior) 두 기간 dict.

    DART fnlttSinglAcnt.json 응답은 {"status": "000", "list": [{"account_nm": "매출액",
    "thstrm_amount": "12,345", "frmtrm_amount": "10,000", ...}, ...]}.
    연결(CFS) 우선, 없으면 별도(OFS) 값을 채움.
    """
    current: dict = {}
    prior: dict = {}
    for item in fs_data.get("list", []) if isinstance(fs_data, dict) else []:
        name = (item.get("account_nm") or "").strip()
        if not name:
            continue
        # 금액 파싱 (콤마·괄호 제거, 빈 문자열/"-"은 None)
        def _parse(s):
            if s is None:
                return None
            s = str(s).replace(",", "").replace(" ", "").strip()
            if not s or s in ("-", "null"):
                return None
            # 괄호는 음수 표기
            if s.startswith("(") and s.endswith(")"):
                s = "-" + s[1:-1]
            try:
                return int(s)
            except ValueError:
                try:
                    return int(float(s))
                except ValueError:
                    return None

        cur_v = _parse(item.get("thstrm_amount"))
        pri_v = _parse(item.get("frmtrm_amount"))
        # 이미 채워진 키는 덮어쓰지 않음 (CFS가 먼저 나오는 DART 관행 활용)
        if cur_v is not None and name not in current:
            current[name] = cur_v
        if pri_v is not None and name not in prior:
            prior[name] = pri_v
    return current, prior
```

- [ ] **Step 4.4: 테스트 통과 확인**

Run: `python -m unittest tests.test_financial_anomaly -v`

Expected: `Ran 8 tests in Xs — OK`.

- [ ] **Step 4.5: 커밋**

```bash
git add dart_risk_mcp/core/dart_client.py tests/test_financial_anomaly.py
git commit -m "feat(dart_client): add detect_financial_anomaly + FS period adapter"
```

---

## Task 5: core 패키지 공개 API 확장

**Files:**
- Modify: `dart_risk_mcp/core/__init__.py`

- [ ] **Step 5.1: 수정**

Edit `dart_risk_mcp/core/__init__.py`. `from .dart_client import (` 블록에 추가:

```python
    detect_capital_churn,
    detect_financial_anomaly,
    _fs_response_to_periods,
```

`from .signals import` 블록에 추가:

```python
    CAPITAL_EVENT_KEYS,
```

`__all__` 리스트에 추가:

```python
    "detect_capital_churn",
    "detect_financial_anomaly",
    "CAPITAL_EVENT_KEYS",
```

(`_fs_response_to_periods`는 내부 함수라 `__all__`에 포함하지 않음)

- [ ] **Step 5.2: import 검증**

Run:
```bash
python -c "from dart_risk_mcp.core import detect_capital_churn, detect_financial_anomaly, CAPITAL_EVENT_KEYS; print('OK', len(CAPITAL_EVENT_KEYS))"
```

Expected: `OK 9`.

- [ ] **Step 5.3: 커밋**

```bash
git add dart_risk_mcp/core/__init__.py
git commit -m "feat(core): export v0.6.0 detectors and CAPITAL_EVENT_KEYS"
```

---

## Task 6: `scan_financial_anomaly` MCP 도구

**Files:**
- Modify: `dart_risk_mcp/server.py`

- [ ] **Step 6.1: server.py import 확장**

Edit `dart_risk_mcp/server.py`. 기존 `from dart_risk_mcp.core import (...)` 블록에 추가:

```python
    detect_capital_churn,
    detect_financial_anomaly,
    _fs_response_to_periods,
    CAPITAL_EVENT_KEYS,
```

(`_fs_response_to_periods`는 dart_client 모듈에서 직접 import해야 할 수도 있음. 그 경우: `from dart_risk_mcp.core.dart_client import _fs_response_to_periods`)

- [ ] **Step 6.2: `scan_financial_anomaly` 도구 추가**

`server.py`에서 기존 `track_fund_usage` 아래에 추가 (또는 `def main()` 앞 적절한 위치):

```python
@mcp.tool()
def scan_financial_anomaly(
    company_name: str,
    year: str = "",
    report_type: str = "annual",
) -> str:
    """
    재무제표 4개 지표(매출채권·재고자산·현금흐름·자본잠식)를 전년 대비로 비교해
    분식·부실 초기 조짐을 탐지합니다.

    Args:
        company_name: 기업명 또는 종목코드(6자리).
        year: 사업연도(예: "2024"). 빈 값이면 직전 연도.
        report_type: "annual"(사업보고서) | "half"(반기) | "q1" | "q3".

    Returns:
        지표별 당기/전기/Δ/판정 표 + 탐지된 플래그 목록 텍스트.
    """
    api_key = os.environ.get("DART_API_KEY", "")
    if not api_key:
        return "❌ DART_API_KEY 환경변수가 설정되지 않았습니다."

    corp_info = resolve_corp(company_name, api_key)
    if not corp_info[1]:
        return f"❌ 기업 '{company_name}'을(를) 찾을 수 없습니다."
    corp_name, info = corp_info
    corp_code = info["corp_code"]

    if not year:
        from datetime import datetime
        year = str(datetime.now().year - 1)

    fs_data = fetch_financial_statements(corp_code, api_key, year, report_type)
    if not fs_data or fs_data.get("status") not in ("000",):
        return (f"📊 **{corp_name}** ({info.get('stock_code','')}) — {year} {report_type}\n\n"
                "재무제표 조회 불가(데이터 없음 또는 권한 부족).")

    current, prior = _fs_response_to_periods(fs_data)
    flags, metrics = detect_financial_anomaly(current, prior)

    # 포맷
    lines = [f"📊 **{corp_name}** ({info.get('stock_code','')}) — 재무 이상 스캔 ({year}, {report_type})", ""]
    lines.append("| 지표 | 당기 | 전기 | Δ | 판정 |")
    lines.append("|---|---|---|---|---|")
    for m in metrics:
        name = m["name"]
        if name == "순이익 vs 영업현금흐름":
            cur = f"순이익 {m['current_ni']:,} / OCF {m['current_ocf']:,}"
            pri = "-"
            delta = "-"
        elif "current" in m and "prior" in m:
            cur = f"{m['current']:.1f}{m.get('unit','')}"
            pri = f"{m['prior']:.1f}{m.get('unit','')}"
            delta = f"{m['delta']:+.1f}%p"
        else:
            cur = f"{m.get('current', 0):.1f}{m.get('unit','')}"
            pri = "-"
            delta = "-"
        verdict = "🚩 " + _v6_flag_label(name) if m.get("flagged") else "정상"
        lines.append(f"| {name} | {cur} | {pri} | {delta} | {verdict} |")
    lines.append("")
    if flags:
        lines.append(f"**탐지 플래그:** {', '.join(flags)}")
    else:
        lines.append("**탐지 플래그:** 없음 (모든 지표 정상)")
    lines.append("")
    lines.append("📎 참고: DART 공시 기준. 감사 전 수치 포함 가능. 회계 전문가 판단이 아닌 스크리닝 참고용.")
    return "\n".join(lines)


def _v6_flag_label(metric_name: str) -> str:
    """지표명 → 대응 플래그 라벨."""
    return {
        "매출채권/매출": "AR_SURGE",
        "재고자산/매출": "INVENTORY_SURGE",
        "순이익 vs 영업현금흐름": "CASH_GAP",
        "자본총계/자본금": "CAPITAL_IMPAIRMENT",
    }.get(metric_name, "FLAG")
```

- [ ] **Step 6.3: 등록 확인**

Run:
```bash
python -c "from dart_risk_mcp.server import mcp; names=[t.name for t in mcp._tool_manager.list_tools()]; print('scan_financial_anomaly:', 'scan_financial_anomaly' in names, 'total:', len(names))"
```

Expected: `scan_financial_anomaly: True total: 20` (19 + 1).

- [ ] **Step 6.4: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "feat(server): add scan_financial_anomaly MCP tool"
```

---

## Task 7: `track_capital_structure` MCP 도구

**Files:**
- Modify: `dart_risk_mcp/server.py`

- [ ] **Step 7.1: `track_capital_structure` 도구 추가**

Edit `dart_risk_mcp/server.py`. `scan_financial_anomaly` 바로 아래에 추가:

```python
@mcp.tool()
def track_capital_structure(
    company_name: str,
    lookback_years: int = 3,
) -> str:
    """
    자본 이벤트(증자·감자·자사주·CB/BW/EB/RCPS 등)를 시간순으로 집계해
    '자본 주무르기' 리듬을 탐지합니다.

    Args:
        company_name: 기업명 또는 종목코드(6자리).
        lookback_years: 1~5(밖이면 3으로 강제).

    Returns:
        이벤트 총수·12개월 집중도·연도별 집계·시계열·플래그 텍스트.
    """
    api_key = os.environ.get("DART_API_KEY", "")
    if not api_key:
        return "❌ DART_API_KEY 환경변수가 설정되지 않았습니다."

    if not isinstance(lookback_years, int) or not (1 <= lookback_years <= 5):
        lookback_years = 3

    corp_info = resolve_corp(company_name, api_key)
    if not corp_info[1]:
        return f"❌ 기업 '{company_name}'을(를) 찾을 수 없습니다."
    corp_name, info = corp_info
    corp_code = info["corp_code"]

    disclosures = fetch_company_disclosures(corp_code, api_key, lookback_years * 365)

    # match_signals로 신호 탐지 + 자본 이벤트만 필터는 detect_capital_churn이 처리
    signal_events: list[dict] = []
    for d in disclosures:
        matches = match_signals(d.get("report_nm", ""))
        for m in matches:
            signal_events.append({
                "key": m["key"],
                "label": m["label"],
                "score": m.get("score", 0),
                "report_nm": d.get("report_nm", ""),
                "rcept_dt": d.get("rcept_dt", ""),
                "rcept_no": d.get("rcept_no", ""),
                "is_amendment": is_amendment_disclosure(d.get("report_nm", "")),
            })

    result = detect_capital_churn(signal_events, lookback_years)

    # 포맷
    lines = [
        f"📊 **{corp_name}** ({info.get('stock_code','')}) — 자본구조 추적 (최근 {lookback_years}년)",
        "",
        f"자본 이벤트 총 **{result['total_events']}건** | "
        f"12개월 최대 집중도: **{result['max_12m_count']}건**"
        + (f" | 🚩 CAPITAL_CHURN" if "CAPITAL_CHURN" in result["flags"] else ""),
        "",
    ]
    if result["by_year"]:
        lines.append("**연도별 집계**")
        for y in sorted(result["by_year"].keys()):
            lines.append(f"- {y}: {result['by_year'][y]}건")
        lines.append("")
    if result["events"]:
        lines.append("**시계열** (최대 30건)")
        for e in result["events"][:30]:
            lines.append(f"- {e['rcept_dt']}  `{e['key']:<14}`  {e['report_nm']}")
        if len(result["events"]) > 30:
            lines.append(f"- ... (총 {len(result['events'])}건 중 30건 표시)")
        lines.append("")
    lines.append("📎 참고: 이 도구는 공시 '횟수·리듬' 탐지 전용. 정확한 희석률·금액은 "
                 "`get_major_decision` 또는 `get_disclosure_document`로 개별 확인.")
    return "\n".join(lines)
```

- [ ] **Step 7.2: 등록 확인**

Run:
```bash
python -c "from dart_risk_mcp.server import mcp; names=[t.name for t in mcp._tool_manager.list_tools()]; print('tc:', 'track_capital_structure' in names, 'sfa:', 'scan_financial_anomaly' in names, 'total:', len(names))"
```

Expected: `tc: True sfa: True total: 21`.

- [ ] **Step 7.3: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "feat(server): add track_capital_structure MCP tool"
```

---

## Task 8: `analyze_company_risk` 통합

**Files:**
- Modify: `dart_risk_mcp/server.py`
- Test: `tests/test_v6_integration.py` (신규)

현재 구현된 `analyze_company_risk`는 v0.5.0에서 `signal_events`(list of dict)로 수집하고 `calculate_risk_score` 호출 전에 자금사용·주요결정 블록을 덧붙인다. 같은 패턴으로 v0.6.0 블록을 추가한다.

- [ ] **Step 8.1: 통합 테스트 작성**

Create `tests/test_v6_integration.py`:

```python
"""analyze_company_risk v0.6.0 통합 검증."""
import os
import unittest
from unittest.mock import patch, MagicMock


class TestAnalyzeV6Integration(unittest.TestCase):
    def setUp(self):
        os.environ["DART_API_KEY"] = "TESTKEY"

    def _mock_disclosures(self):
        # 12개월 내 자본 이벤트 3건 → CAPITAL_CHURN 발화 기대
        return [
            {"rcept_no": "20250101000001", "rcept_dt": "20250101",
             "report_nm": "유상증자결정(제3자배정)", "corp_code": "00000001"},
            {"rcept_no": "20250501000001", "rcept_dt": "20250501",
             "report_nm": "전환사채권발행결정", "corp_code": "00000001"},
            {"rcept_no": "20251001000001", "rcept_dt": "20251001",
             "report_nm": "자기주식취득결정", "corp_code": "00000001"},
        ]

    def _mock_fs(self):
        # AR_SURGE 발화: 매출채권/매출 전기 20% → 당기 71%
        return {
            "status": "000",
            "list": [
                {"account_nm": "매출액", "thstrm_amount": "1,000", "frmtrm_amount": "1,000"},
                {"account_nm": "매출채권", "thstrm_amount": "710", "frmtrm_amount": "200"},
                {"account_nm": "재고자산", "thstrm_amount": "100", "frmtrm_amount": "100"},
                {"account_nm": "당기순이익", "thstrm_amount": "50", "frmtrm_amount": "50"},
                {"account_nm": "영업활동현금흐름", "thstrm_amount": "50", "frmtrm_amount": "50"},
                {"account_nm": "자본총계", "thstrm_amount": "500", "frmtrm_amount": "500"},
                {"account_nm": "자본금", "thstrm_amount": "100", "frmtrm_amount": "100"},
            ],
        }

    @patch("dart_risk_mcp.server.fetch_financial_statements")
    @patch("dart_risk_mcp.server.fetch_company_disclosures")
    @patch("dart_risk_mcp.server.resolve_corp")
    def test_includes_capital_churn_flag(self, m_resolve, m_disc, m_fs):
        m_resolve.return_value = ("테스트기업", {"corp_code": "00000001", "stock_code": "000001"})
        m_disc.return_value = self._mock_disclosures()
        m_fs.return_value = {"status": "013", "list": []}  # 재무는 실패
        from dart_risk_mcp.server import analyze_company_risk
        out = analyze_company_risk("테스트기업", 365)
        self.assertIn("CAPITAL_CHURN", out)

    @patch("dart_risk_mcp.server.fetch_financial_statements")
    @patch("dart_risk_mcp.server.fetch_company_disclosures")
    @patch("dart_risk_mcp.server.resolve_corp")
    def test_includes_financial_anomaly_flag(self, m_resolve, m_disc, m_fs):
        m_resolve.return_value = ("테스트기업", {"corp_code": "00000001", "stock_code": "000001"})
        m_disc.return_value = []  # 공시 없음
        m_fs.return_value = self._mock_fs()
        from dart_risk_mcp.server import analyze_company_risk
        out = analyze_company_risk("테스트기업", 365)
        self.assertIn("AR_SURGE", out)

    @patch("dart_risk_mcp.server.fetch_financial_statements")
    @patch("dart_risk_mcp.server.fetch_company_disclosures")
    @patch("dart_risk_mcp.server.resolve_corp")
    def test_financial_fetch_failure_isolated(self, m_resolve, m_disc, m_fs):
        m_resolve.return_value = ("테스트기업", {"corp_code": "00000001", "stock_code": "000001"})
        m_disc.return_value = []
        m_fs.side_effect = Exception("network error")
        from dart_risk_mcp.server import analyze_company_risk
        # 예외가 도구 레벨로 전파되지 않고, 리포트는 최소한의 요약을 반환해야 함
        out = analyze_company_risk("테스트기업", 365)
        self.assertIsInstance(out, str)
        self.assertIn("테스트기업", out)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 8.2: 테스트 실패 확인**

Run: `python -m unittest tests.test_v6_integration -v`

Expected: `FAIL`(앞 2개) 또는 기존 통합 누락으로 인한 오류.

- [ ] **Step 8.3: `analyze_company_risk` 본문에 v6 블록 삽입**

Edit `dart_risk_mcp/server.py` — `analyze_company_risk` 본문에서 **v0.5.0 자금사용·주요결정 블록 바로 뒤, `calculate_risk_score` 호출 직전**에 다음 블록 추가:

```python
    # ============ v0.6.0 블록 시작 ============
    # 자본 churn 탐지 (최근 12개월 window)
    try:
        churn = detect_capital_churn(signal_events, lookback_years=1)
        if "CAPITAL_CHURN" in churn["flags"]:
            signal_events.append({
                "key": "CAPITAL_CHURN",
                "label": "자본 이벤트 과다 반복",
                "score": 7,
                "report_nm": f"최근 12개월 내 자본 이벤트 {churn['max_12m_count']}건 집중",
                "rcept_dt": "",
                "rcept_no": "",
                "is_amendment": False,
            })
    except Exception:
        churn = {"flags": [], "events": [], "max_12m_count": 0, "total_events": 0, "by_year": {}}

    # 재무이상 스캔 (당기/전기)
    _v6_labels = {
        "AR_SURGE": ("매출채권/매출 비율 급등", 8),
        "INVENTORY_SURGE": ("재고자산/매출 비율 급등", 7),
        "CASH_GAP": ("순이익·현금흐름 괴리", 8),
        "CAPITAL_IMPAIRMENT": ("자본잠식 근접", 9),
    }
    fs_flags: list[str] = []
    fs_metrics: list[dict] = []
    try:
        from datetime import datetime as _dt
        _year = str(_dt.now().year - 1)
        fs_data = fetch_financial_statements(corp_code, api_key, _year, "annual")
        if fs_data and fs_data.get("status") == "000":
            _cur, _pri = _fs_response_to_periods(fs_data)
            fs_flags, fs_metrics = detect_financial_anomaly(_cur, _pri)
            for f in fs_flags:
                label, score = _v6_labels[f]
                signal_events.append({
                    "key": f,
                    "label": label,
                    "score": score,
                    "report_nm": f"{_year} 재무제표 YoY 이상",
                    "rcept_dt": "",
                    "rcept_no": "",
                    "is_amendment": False,
                })
    except Exception:
        pass
    # ============ v0.6.0 블록 끝 ============
```

`calculate_risk_score` 호출 뒤, **최종 리포트 조립 단계**(자금사용·주요결정 섹션 뒤, 타임라인 앞)에서 다음 섹션들을 추가:

```python
    # v0.6.0 자본 변동 타임라인 (최근 12개월 요약)
    if churn.get("events"):
        report_sections.append("\n## 📊 자본 변동 타임라인 (최근 12개월)")
        recent = [e for e in churn["events"] if e["rcept_dt"] >= (datetime.now() - timedelta(days=365)).strftime("%Y%m%d")]
        if recent:
            for e in recent[:10]:
                report_sections.append(f"- {e['rcept_dt']} `{e['key']}` {e['report_nm']}")
            if len(recent) > 10:
                report_sections.append(f"- ... (+{len(recent)-10}건)")
        else:
            report_sections.append("- 최근 12개월 내 자본 이벤트 없음")

    # v0.6.0 재무 이상 스캔
    if fs_metrics:
        report_sections.append("\n## 📊 재무 이상 스캔")
        flagged_only = [m for m in fs_metrics if m.get("flagged")]
        if flagged_only:
            for m in flagged_only:
                if m["name"] == "순이익 vs 영업현금흐름":
                    report_sections.append(
                        f"- 🚩 **CASH_GAP**: 순이익 {m['current_ni']:,} vs 영업현금흐름 {m['current_ocf']:,}"
                    )
                elif "current" in m and "prior" in m and "delta" in m:
                    report_sections.append(
                        f"- 🚩 **{_v6_metric_to_flag(m['name'])}**: "
                        f"{m['name']} {m['current']:.1f}% (전기 {m['prior']:.1f}%, Δ {m['delta']:+.1f}%p)"
                    )
                else:
                    report_sections.append(
                        f"- 🚩 **{_v6_metric_to_flag(m['name'])}**: "
                        f"{m['name']} {m.get('current', 0):.1f}%"
                    )
        else:
            report_sections.append("- 모든 지표 정상")
    elif not fs_flags:
        # 재무제표 조회 실패 사례
        pass
```

(`report_sections`는 기존 `analyze_company_risk`가 이미 쓰는 이름이면 그대로. 다른 이름이면 실제 변수명으로 치환.)

파일 어딘가에 헬퍼 추가 (`_v6_flag_label` 근처):

```python
def _v6_metric_to_flag(metric_name: str) -> str:
    return {
        "매출채권/매출": "AR_SURGE",
        "재고자산/매출": "INVENTORY_SURGE",
        "순이익 vs 영업현금흐름": "CASH_GAP",
        "자본총계/자본금": "CAPITAL_IMPAIRMENT",
    }.get(metric_name, "FLAG")
```

필요 시 import 추가:
```python
from datetime import datetime, timedelta
```

- [ ] **Step 8.4: 테스트 통과 확인**

Run: `python -m unittest tests.test_v6_integration -v`

Expected: `Ran 3 tests in Xs — OK`.

- [ ] **Step 8.5: 전체 테스트 회귀 확인**

Run: `python -m unittest discover -s tests -v`

Expected: 기존 18 + 신규 17 = `Ran 35 tests — OK`.

- [ ] **Step 8.6: 커밋**

```bash
git add dart_risk_mcp/server.py tests/test_v6_integration.py
git commit -m "feat(server): integrate v0.6.0 flags into analyze_company_risk"
```

---

## Task 9: `build_event_timeline` 확장

**Files:**
- Modify: `dart_risk_mcp/server.py` (또는 `dart_risk_mcp/core/taxonomy.py`에 _PHASE_MAP이 있다면 거기도)

현재 `_PHASE_MAP`은 `server.py` 또는 `taxonomy.py`에 있다. 실제 위치 확인 후 수정.

- [ ] **Step 9.1: `_PHASE_MAP` 확장**

`_PHASE_MAP` 딕셔너리에 다음 5개 키 추가:

```python
    "CAPITAL_CHURN":       "심화기",
    "AR_SURGE":            "심화기",
    "INVENTORY_SURGE":     "심화기",
    "CASH_GAP":            "탈출기",
    "CAPITAL_IMPAIRMENT":  "탈출기",
```

- [ ] **Step 9.2: build_event_timeline 말미에 "재무 징후" 블록 추가**

`build_event_timeline` 본문의 최종 렌더링 단계, **전체 3-phase 리스트 출력 뒤**에 다음 블록을 덧붙인다:

```python
    # v0.6.0 재무 징후 블록 (공시 이벤트가 아닌 스칼라 판정)
    try:
        from datetime import datetime as _dt
        _year = str(_dt.now().year - 1)
        fs_data = fetch_financial_statements(corp_code, api_key, _year, "annual")
        if fs_data and fs_data.get("status") == "000":
            _cur, _pri = _fs_response_to_periods(fs_data)
            fs_flags, _ = detect_financial_anomaly(_cur, _pri)
            if fs_flags:
                lines.append("\n### 재무 징후")
                lines.append(f"**{_year} 사업보고서 기준 이상 플래그:** {', '.join(fs_flags)}")
    except Exception:
        pass
```

(변수명 `lines`는 `build_event_timeline`이 사용하는 렌더링 리스트. 다른 이름이면 치환.)

- [ ] **Step 9.3: 스모크 검증 (mock 없이 import만 확인)**

Run:
```bash
python -c "from dart_risk_mcp.server import build_event_timeline; print('OK')"
```

Expected: `OK`.

전체 테스트 회귀 재확인:
```bash
python -m unittest discover -s tests -v
```

Expected: `Ran 35 tests — OK`.

- [ ] **Step 9.4: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "feat(server): extend build_event_timeline with v0.6.0 phase map + 재무 징후 block"
```

---

## Task 10: 릴리스 준비

**Files:**
- Modify: `dart_risk_mcp/__init__.py`
- Modify: `CHANGELOG.md`
- Modify: `CLAUDE.md`

- [ ] **Step 10.1: 버전 번프**

Edit `dart_risk_mcp/__init__.py`:

```python
"""DART 기업 위험 분석 MCP 서버 패키지."""

__version__ = "0.6.0"
```

- [ ] **Step 10.2: CHANGELOG.md 업데이트**

`CHANGELOG.md`의 `## [Unreleased]` 아래, `## [0.5.0]` 위에 다음 섹션 추가:

```markdown
## [0.6.0] — 2026-04-22

### Added
- **신규 MCP 도구 2개** (총 19개 → 21개):
  - `track_capital_structure` — 자본 이벤트(증자·감자·자사주·CB/BW/EB/RCPS 9종)를 시간순 집계. 12개월 내 3건 이상 발생 시 `CAPITAL_CHURN` 플래그
  - `scan_financial_anomaly` — 재무제표 4개 지표 YoY 이상 탐지(`AR_SURGE`·`INVENTORY_SURGE`·`CASH_GAP`·`CAPITAL_IMPAIRMENT`)
- **신규 신호 키 5개**: `CAPITAL_CHURN`(2.7), `AR_SURGE`(6.1), `INVENTORY_SURGE`(6.1), `CASH_GAP`(6.1), `CAPITAL_IMPAIRMENT`(8.2)
- **신규 taxonomy 2.7** (Category 2 자본구조) — 자본 이벤트 과다 반복
- **신규 복합 패턴 1개**: `capital_churn_anomaly` (2.7 + 4.3)
- `detect_capital_churn`, `detect_financial_anomaly` — 순수 계산 함수. 신규 DART 엔드포인트 0개

### Changed
- `analyze_company_risk` — 자본 churn·재무이상 플래그를 signal_events에 자동 합산. 리포트에 자본 변동 타임라인·재무 이상 스캔 섹션 2개 추가
- `build_event_timeline` — 신규 5개 키 `_PHASE_MAP` 매핑 추가, 말미에 "재무 징후" 블록 렌더링

### Infra
- `CAPITAL_EVENT_KEYS` 상수 추가 (자본 이벤트 신호 9개 집합)
- 재무 응답 어댑터 `_fs_response_to_periods` 추가 (DART `fnlttSinglAcnt.json` list → 당기/전기 dict 변환)
```

- [ ] **Step 10.3: CLAUDE.md 업데이트**

Edit `CLAUDE.md`:

1. `## MCP 도구 19개` → `## MCP 도구 21개`
2. `### 19. get_major_decision` 아래에 다음 두 섹션 추가:

```markdown
### 20. `track_capital_structure(company_name, lookback_years=3)` ✨

자본 이벤트(증자·감자·자사주·CB/BW/EB/RCPS 9종)를 시간순으로 집계해 '자본 주무르기' 리듬을 탐지합니다.

- 내부 흐름: `resolve_corp` → `fetch_company_disclosures` → `match_signals` × N → `CAPITAL_EVENT_KEYS` 필터 → `detect_capital_churn`
- 판정 규칙: 12개월 슬라이딩 윈도우에서 자본 이벤트 ≥3건 → `CAPITAL_CHURN` 플래그
- `lookback_years` 범위: 1~5년

### 21. `scan_financial_anomaly(company_name, year="", report_type="annual")` ✨

재무제표 4개 지표(매출채권·재고자산·현금흐름·자본잠식)를 전년 대비 비교해 이상을 탐지합니다.

- 내부 흐름: `resolve_corp` → `fetch_financial_statements` 1회 호출 → `_fs_response_to_periods` → `detect_financial_anomaly`
- 이상 플래그 4종: `AR_SURGE`, `INVENTORY_SURGE`, `CASH_GAP`, `CAPITAL_IMPAIRMENT`
- `report_type` 허용값: `annual`·`half`·`q1`·`q3`
```

3. 핵심 내부 함수 표에 2행 추가:

```markdown
| `detect_capital_churn(events, lookback_years)` | 12개월 슬라이딩 윈도우로 CAPITAL_CHURN 판정 |
| `detect_financial_anomaly(current, prior)` | 4개 지표 YoY 비교 → 플래그+메트릭 |
```

4. "자주 있는 작업 > 새 신호 유형 추가" 하단의 복합 패턴 목록에 언급:

```markdown
- **v0.6.0 신규 1개**: `capital_churn_anomaly`(자본 이벤트 과다 반복 + 공시의무 위반)
```

- [ ] **Step 10.4: 전체 스모크 테스트**

Run:
```bash
python -c "
import dart_risk_mcp
assert dart_risk_mcp.__version__ == '0.6.0', dart_risk_mcp.__version__
from dart_risk_mcp.server import mcp
tools = list(mcp._tool_manager.list_tools())
assert len(tools) == 21, len(tools)
names = {t.name for t in tools}
assert 'track_capital_structure' in names
assert 'scan_financial_anomaly' in names
print('version:', dart_risk_mcp.__version__, 'tools:', len(tools))
"
```

Expected: `version: 0.6.0 tools: 21`.

Run 전체 테스트:
```bash
python -m unittest discover -s tests -v 2>&1 | tail -5
```

Expected: `Ran 35 tests — OK`.

- [ ] **Step 10.5: 커밋**

```bash
git add dart_risk_mcp/__init__.py CHANGELOG.md CLAUDE.md
git commit -m "chore: v0.6.0 릴리스 — 자본구조·재무이상 스캔 확장"
```

---

## Task 11: 릴리스 게이트 검증 (V.1~V.4)

Task 10까지 완료된 후 수행.

- [ ] **Step V.1: 회귀 확인 — 삼성전자**

Run:
```bash
PYTHONIOENCODING=utf-8 python -c "
from dart_risk_mcp.server import analyze_company_risk
print(analyze_company_risk('삼성전자', 90)[:2500])
"
```

Expected:
- 기존 형식 유지(위험 등급·신호·복합 패턴·카탈로그 순서).
- 삼성전자는 정상 기업이므로 `CAPITAL_CHURN` 또는 재무이상 플래그 **없음**. 리포트가 이전 대비 크게 길어지지 않아야 함.

- [ ] **Step V.2: 금감원 적발 상장사 5건 재탐지**

각 기업에 대해 실행:
```bash
PYTHONIOENCODING=utf-8 python -c "
from dart_risk_mcp.server import analyze_company_risk
out = analyze_company_risk('<기업명>', 365)
print(out)
" | Out-File -Encoding utf8 verify_v6_<기업명>.txt
```

대상 5개 기업 (v0.5.0과 동일):
- 동성제약
- 헬릭스미스
- 네오이뮨텍
- 위메이드
- 셀리버리

각 파일에서 다음 플래그 중 **최소 1개**가 새로 탐지되는지 확인:
`CAPITAL_CHURN`, `AR_SURGE`, `INVENTORY_SURGE`, `CASH_GAP`, `CAPITAL_IMPAIRMENT`.

- [ ] **Step V.3: 게이트 판정**

- 5건 중 **3건 이상**에서 신규 플래그 탐지 → v0.6.0 **승인**.
- 3건 미만일 경우 1차 임계값 조정:
  - `AR_SURGE`·`INVENTORY_SURGE` 임계값 50%p → 40%p (`dart_client.py` `detect_financial_anomaly` 내부 상수 수정)
  - `CAPITAL_CHURN` 임계값 3건 → 2건 (`detect_capital_churn` 내부 `>= 3` → `>= 2`)
  - 재검증 후에도 미달 시 사용자 결정 요청.

- [ ] **Step V.4: 검증 결과 문서화 + 푸시**

Edit `docs/superpowers/specs/2026-04-22-v0.6.0-design.md` 끝에 `## 10. 검증 결과 (YYYY-MM-DD)` 섹션 추가. 내용:
- V.1 통과 여부 및 리포트 길이 비교
- V.2 기업별 탐지 결과 표 (기업명·리포트 길이·탐지된 플래그)
- V.3 판정 (통과/재조정)
- V.4 미탐지 사유·후속 관찰 사항

```bash
git add docs/superpowers/specs/2026-04-22-v0.6.0-design.md
git commit -m "docs: v0.6.0 릴리스 게이트 검증 결과 기록"
git push origin master
```

---

## Self-Review Checklist (구현 시작 전 마지막 점검)

- **스펙 커버리지**: 스펙 §3~§6 모든 요구사항이 Task 1~11에 대응 ✅
- **타입 일관성**: `events: list[dict]` 스키마 일관, `flags: list[str]`, `metrics: list[dict]` 통일 ✅
- **호환성**: 기존 19개 도구 시그니처·반환 형식 변경 없음. 기존 18개 테스트 수정 금지 ✅
- **Placeholder 스캔**: "적절히"·"TBD"·"TODO" 없음. 모든 테스트 케이스에 구체 데이터·assertion ✅
- **변수명 치환 주의 (Task 8·9)**: `signal_events`·`report_sections`·`lines`·`corp_code`는 현재 `server.py`의 실제 변수명과 일치 여부를 구현 시 반드시 확인 후 치환할 것
- **테스트 격리**: v6 integration 테스트는 `unittest.mock.patch`로 네트워크 차단. 실제 API 호출은 V.2에서만.

---

## 실행 방식 선택 가이드

- **Subagent-Driven (권장)**: Task 1~7은 자족적이므로 신규 서브에이전트로 병렬 진행 가능. Task 8·9는 현재 `server.py` 구조 파악이 필요하므로 세션 컨텍스트 유리.
- **Inline Execution**: 현재 세션에서 체크포인트 배치 실행. Task 8·9의 기존 변수명 치환이 중요하므로 안전한 선택.
