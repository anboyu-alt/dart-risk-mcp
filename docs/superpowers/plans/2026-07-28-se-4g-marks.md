# SE-4g 사실 강조(Fact Marks) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 표 안에서 눈으로 놓치기 쉬운 **산술적 사실**(부호·두 값의 비교·DART 원본 값)을 셀 단위로 표시해 즉시 눈에 띄게 한다.

**Architecture:** `app.js`에 선언적 규칙표 `MARK_RULES`와 순수 함수 `cellMarks(records, sectionKey)` / `markLegend(records, sectionKey)`를 둔다. `ui.js`의 `tableEl()`이 이미 계산하고 있는 `(rowIdx, cellKey)` 좌표를 그대로 써서 CSS 클래스만 붙인다. 표 구조·데이터·기존 렌더 경로는 건드리지 않는다.

**Tech Stack:** 순수 ES5 스타일 JS(빌드 없음), pytest에서 node 서브프로세스로 순수 함수 검증, CSS 변수.

## Global Constraints

- **v0.8.5 원칙 — 판정 금지.** 강조는 **사실의 가시화**이지 위험 판정이 아니다. 강조에 이름을 붙이지 않는다: `부실`·`위험`·`주의`·`손상`·`악화`·`양호`·`이상` 등 어떤 해석 어휘도 코드·문구·클래스명에 쓰지 않는다. 표기하는 것은 **규칙 그 자체**뿐이다(예: `기말 장부가액 < 최초 취득금액`).
- **임계값을 우리가 정하지 않는다.** 모든 규칙은 (a) 부호, (b) 같은 레코드 안 두 값의 비교, (c) DART 원본 값의 동등 비교, (d) core가 이미 계산해 보내준 값 — 이 넷 중 하나여야 한다. "N% 초과", "N배 이상" 같은 규칙은 금지한다.
- **점수·등급·순위 금지.** 강조된 셀 개수를 세어 합계·비율·등급을 표시하지 않는다.
- **외부 의존성 추가 금지.** `requests`/`mcp` 외 파이썬 의존성, CDN 자산 모두 금지.
- **`dart_risk_mcp/`·`se_server/`는 이 계획에서 한 줄도 고치지 않는다.** MCP 도구 26개에 영향이 가면 안 된다.
- **DOM 주입 금지.** 데이터는 `textContent`로만 넣는다. `innerHTML`/`outerHTML`/`insertAdjacentHTML`/`document.write`에 데이터가 들어가면 정적 검사가 막는다.
- **다크·라이트 양쪽 정의.** 새 CSS 변수는 `:root`와 `:root[data-theme="light"]` 양쪽에 정의한다. 한쪽만 정의하면 반대 테마에서 색이 사라진다.
- **로그아웃 시 잔류 금지.** `showGate()`가 지우는 대상에 새로 만든 DOM이 포함되어야 한다(이전 사용자의 실명·수치 잔류 방지).

---

## 배경: 실측으로 확인한 것

2026-07-28 엔켐(`corp_code=01011526`) 실 API 응답으로 확인했다. 아래 값은 **픽스처에 그대로 쓸 실측값**이다.

### 타법인 출자 `otrCprInvstmntSttus` — 27건

| 필드 | 뜻 |
|---|---|
| `inv_prm` | 법인명 |
| `frst_acqs_amount` | 최초 취득금액 |
| `bsis_blce_acntbk_amount` | 기초 장부가액 |
| `incrs_dcrs_evl_lstmn` | 증감 평가손익 |
| `trmend_blce_acntbk_amount` | 기말 장부가액 |
| `recent_bsns_year_fnnr_sttus_thstrm_ntpf` | 피투자사 최근 사업연도 당기순이익 |

실측 발화 건수: 기말장부<최초취득 **7건**, 피투자사 순이익<0 **21건**, 평가손익<0 **3건**.

**⚠️ 27건 중 21건(78%)이 적자다.** 그래서 셀 배경을 채우면 표 대부분이 칠해져 강조가 배경이 된다. **좌측 얇은 막대(3px)** 를 주 표기로 쓰고 배경은 거의 보이지 않을 정도(alpha 0.10 이하)로만 쓴다. 이건 취향이 아니라 실측에서 나온 제약이다.

**⚠️ `incrs_dcrs_evl_lstmn`은 27건 중 21건이 문자열 `"-"` 다.** `"-"`를 음수나 0으로 읽으면 21건이 전부 잘못 강조된다. 반드시 결측으로 처리한다.

### 감사의견 `accnutAdtorNmNdAdtOpinion`

`adt_opinion` 실측값 = `"적정의견"`. 다른 회사에서는 `한정의견`·`부적정의견`·`의견거절`이 나온다.

**⚠️ 함정: `"부적정의견"`은 부분 문자열 `"적정"`을 포함한다.** `indexOf("적정") >= 0` 으로 판정하면 **가장 무거운 의견을 정상으로 읽는다.** 반드시 **접두 일치**(`"적정"`으로 시작하는지)로 판정한다.

### 최대주주 현황 `hyslrSttus`

`bsis_posesn_stock_qota_rt`(기초 지분율) → `trmend_posesn_stock_qota_rt`(기말 지분율). 실측: 오정강 `14.82` → `14.41`.

### 재무제표 `fnlttSinglAcnt` 계정과목 실측 전체

`유동자산 · 비유동자산 · 자산총계 · 유동부채 · 비유동부채 · 부채총계 · 자본금 · 이익잉여금 · 자본총계 · 매출액 · 영업이익 · 법인세차감전 순이익 · 당기순이익(손실) · 총포괄손익`

**⚠️ 당기순이익 계정명은 `당기순이익(손실)`(괄호 포함)이다.** SE-4f에서 이것 때문에 순이익률이 실데이터에서 영원히 "없음"이었다. `pickAccount`/`ACCOUNT_ALIASES`가 이미 있으니 **그것을 재사용한다.** 새로 문자열을 비교하지 말 것.

### 자금 사용 — 이번 계획에서 제외

`otrCptalUseDtls`/`prstInvstmEntrCptalUseDtls`가 `status=101`을 반환해 "차이 발생 사유" 필드명을 **확인하지 못했다.** 확인하지 못한 필드로 규칙을 만들지 않는다. 이 규칙은 이번 범위 밖이다.

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `docs/tool/se/app.js` | 순수 로직. `markNumber`·`MARK_RULES`·`cellMarks`·`markLegend` 추가 | 수정 |
| `docs/tool/se/ui.js` | `tableEl()`에서 좌표별 클래스 부착, 표 아래 범례 렌더 | 수정 |
| `docs/tool/se/index.html` | `--mark`·`--mark-bg` 변수(양 테마) + `.mk`·`.mk-legend` 규칙 | 수정 |
| `tests/se/test_se_app_js.py` | 순수 함수 검증(node 서브프로세스) | 수정 |
| `tests/se/test_se_page_assets.py` | 판정 어휘·CSS 양 테마·DOM 주입 정적 검사 | 수정 |

---

## Task 1: `markNumber` + `cellMarks` 뼈대 + 타법인 출자 3규칙

**Files:**
- Modify: `docs/tool/se/app.js`
- Test: `tests/se/test_se_app_js.py`

**Interfaces:**
- Consumes: `isNoDataMarker(v)` (app.js:520) — 이미 `null`/`""`/`"-"` 를 결측으로 판정한다. 재사용하라.
- Produces:
  - `markNumber(v)` → `number | null`. 콤마·공백을 제거한 뒤 숫자로 변환한다. `isNoDataMarker(v)`가 참이면 **반드시 `null`**.
  - `MARK_RULES` → `{ [sectionKey]: Array<{key, when, why}> }`
    - `key`: 표시를 붙일 컬럼 키(문자열)
    - `when(record)`: `boolean` 반환. 재료가 결측이면 **`false`**(모르는 것을 강조하지 않는다)
    - `why`: 규칙 문구(문자열). **해석 어휘 금지**, 규칙 그 자체만 적는다
  - `cellMarks(records, sectionKey)` → `{ "<행번호>|<키>": "<why>" }` 평면 객체. 규칙이 없거나 `records`가 배열이 아니면 `{}`
  - `markLegend(records, sectionKey)` → 실제로 발화한 `why` 문자열 배열(중복 제거, `MARK_RULES` 선언 순서 유지). 하나도 발화하지 않으면 `[]`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/se/test_se_app_js.py`에 추가한다. 픽스처는 **위 배경 절의 실측값 그대로**다 — 이 계열에서 픽스처가 실데이터와 형태가 달라 결함이 초록으로 통과한 사고가 다섯 번 있었다.

```python
AFFILIATE_ROWS = """[
  {"inv_prm":"제이알에너지솔루션","frst_acqs_amount":"10,000,000,000",
   "incrs_dcrs_evl_lstmn":"-1,259,000,000","trmend_blce_acntbk_amount":"4,991,000,000",
   "recent_bsns_year_fnnr_sttus_thstrm_ntpf":"-10,378,000,000"},
  {"inv_prm":"DFD Yangfu New Mater","frst_acqs_amount":"38,687,000,000",
   "incrs_dcrs_evl_lstmn":"25,806,000,000","trmend_blce_acntbk_amount":"63,481,000,000",
   "recent_bsns_year_fnnr_sttus_thstrm_ntpf":"5,148,000,000"},
  {"inv_prm":"Enchem Poland Sp. z","frst_acqs_amount":"2,004,000,000",
   "incrs_dcrs_evl_lstmn":"-","trmend_blce_acntbk_amount":"2,004,000,000",
   "recent_bsns_year_fnnr_sttus_thstrm_ntpf":"-10,534,000,000"}
]"""


class TestMarkNumber:
    def test_hyphen_is_missing_not_zero(self):
        # 실측 27건 중 21건이 "-" 다. 0이나 음수로 읽으면 21건이 잘못 강조된다.
        assert run_js('console.log(JSON.stringify(markNumber("-")))') == "null"

    def test_blank_and_null_are_missing(self):
        assert run_js('console.log(JSON.stringify([markNumber(""), markNumber(null)]))') == "[null,null]"

    def test_parses_commas_and_sign(self):
        out = run_js('console.log(JSON.stringify(markNumber("-10,534,000,000")))')
        assert out == "-10534000000"


class TestAffiliateMarks:
    def test_book_value_below_acquisition(self):
        out = run_js(
            'const m = cellMarks(%s, "affiliates");'
            'console.log(JSON.stringify(Object.keys(m).filter(k => k.endsWith("|trmend_blce_acntbk_amount")).sort()))'
            % AFFILIATE_ROWS
        )
        # 제이알(100억→49.9억)만 해당. DFD는 증가, Poland는 동일(< 가 아니다).
        assert json.loads(out) == ["0|trmend_blce_acntbk_amount"]

    def test_equal_book_value_is_not_marked(self):
        # Poland 는 최초취득 == 기말장부(2,004,000,000). "낮은 경우"이지 "같은 경우"가 아니다.
        out = run_js(
            'const m = cellMarks(%s, "affiliates");'
            'console.log(JSON.stringify(m["2|trmend_blce_acntbk_amount"] || null))' % AFFILIATE_ROWS
        )
        assert json.loads(out) is None

    def test_investee_net_loss(self):
        out = run_js(
            'const m = cellMarks(%s, "affiliates");'
            'console.log(JSON.stringify(Object.keys(m).filter(k => k.indexOf("thstrm_ntpf") >= 0).sort()))'
            % AFFILIATE_ROWS
        )
        assert json.loads(out) == [
            "0|recent_bsns_year_fnnr_sttus_thstrm_ntpf",
            "2|recent_bsns_year_fnnr_sttus_thstrm_ntpf",
        ]

    def test_hyphen_valuation_is_not_marked(self):
        # Poland 의 평가손익은 "-" 다. 결측을 음수로 읽으면 여기서 터진다.
        out = run_js(
            'const m = cellMarks(%s, "affiliates");'
            'console.log(JSON.stringify(Object.keys(m).filter(k => k.indexOf("evl_lstmn") >= 0).sort()))'
            % AFFILIATE_ROWS
        )
        assert json.loads(out) == ["0|incrs_dcrs_evl_lstmn"]

    def test_legend_lists_only_fired_rules(self):
        out = run_js('console.log(JSON.stringify(markLegend(%s, "affiliates")))' % AFFILIATE_ROWS)
        legend = json.loads(out)
        assert len(legend) == 3
        assert all(isinstance(s, str) and s for s in legend)

    def test_legend_empty_when_nothing_fires(self):
        clean = ('[{"frst_acqs_amount":"1,000","trmend_blce_acntbk_amount":"2,000",'
                 '"incrs_dcrs_evl_lstmn":"500","recent_bsns_year_fnnr_sttus_thstrm_ntpf":"900"}]')
        assert json.loads(run_js('console.log(JSON.stringify(markLegend(%s, "affiliates")))' % clean)) == []

    def test_unknown_section_returns_empty(self):
        assert json.loads(run_js('console.log(JSON.stringify(cellMarks([{"a":1}], "nope")))')) == {}

    def test_non_array_input_is_safe(self):
        out = run_js('console.log(JSON.stringify([cellMarks(null,"affiliates"), cellMarks({},"affiliates")]))')
        assert json.loads(out) == [{}, {}]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/se/test_se_app_js.py -k "MarkNumber or AffiliateMarks" -v`
Expected: FAIL — `markNumber is not defined`

- [ ] **Step 3: 최소 구현**

`docs/tool/se/app.js`에 추가한다.

```js
// ── 사실 강조 ────────────────────────────────────────────────────────────
// 표 안에서 눈으로 놓치기 쉬운 산술적 사실을 셀 단위로 표시한다.
// 강조는 사실의 가시화이지 위험 판정이 아니다 — 규칙은 부호·두 값의 비교·
// 원본 값 동등 비교뿐이고, 임계값("N% 초과")은 두지 않는다(v0.8.5).

// DART는 결측을 null이 아니라 문자열 "-" 로 채운다. 엔켐 타법인 출자
// 27건 중 21건이 그렇다 — "-"를 음수로 읽으면 21건이 전부 잘못 강조된다.
function markNumber(v) {
  if (isNoDataMarker(v)) return null;
  if (typeof v === "number") return isFinite(v) ? v : null;
  const n = Number(String(v).replace(/[,\s]/g, ""));
  return isFinite(n) ? n : null;
}

// 두 값 모두 있을 때만 비교한다. 한쪽이라도 결측이면 강조하지 않는다 —
// 모르는 것을 강조하면 없는 사실을 만들어내는 것이다.
function markLt(a, b) {
  const x = markNumber(a), y = markNumber(b);
  return x !== null && y !== null && x < y;
}

function markNeg(v) {
  const x = markNumber(v);
  return x !== null && x < 0;
}

const MARK_RULES = Object.create(null);

MARK_RULES.affiliates = [
  {
    key: "trmend_blce_acntbk_amount",
    when: function (r) { return markLt(r.trmend_blce_acntbk_amount, r.frst_acqs_amount); },
    why: "기말 장부가액 < 최초 취득금액",
  },
  {
    key: "incrs_dcrs_evl_lstmn",
    when: function (r) { return markNeg(r.incrs_dcrs_evl_lstmn); },
    why: "증감 평가손익 < 0",
  },
  {
    key: "recent_bsns_year_fnnr_sttus_thstrm_ntpf",
    when: function (r) { return markNeg(r.recent_bsns_year_fnnr_sttus_thstrm_ntpf); },
    why: "피투자사 당기순이익 < 0",
  },
];

// 반환 키는 ui.js 의 tableEl()이 이미 계산하는 좌표와 같은 형식이다:
// 가로 표는 (행번호, keys[열]), 세로 표는 (0, keys[행]).
function cellMarks(records, sectionKey) {
  const out = Object.create(null);
  const rules = MARK_RULES[sectionKey];
  if (!Array.isArray(records) || !Array.isArray(rules)) return out;
  records.forEach(function (rec, i) {
    if (!rec || typeof rec !== "object") return;
    for (const rule of rules) {
      if (!(rule.key in rec)) continue;
      let hit = false;
      try { hit = !!rule.when(rec); } catch (e) { hit = false; }
      if (hit) out[i + "|" + rule.key] = rule.why;
    }
  });
  return out;
}

function markLegend(records, sectionKey) {
  const marks = cellMarks(records, sectionKey);
  const fired = new Set(Object.keys(marks).map(function (k) { return marks[k]; }));
  const rules = MARK_RULES[sectionKey] || [];
  const seen = new Set();
  const out = [];
  for (const rule of rules) {
    if (fired.has(rule.why) && !seen.has(rule.why)) { seen.add(rule.why); out.push(rule.why); }
  }
  return out;
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/se/test_se_app_js.py -k "MarkNumber or AffiliateMarks" -v`
Expected: PASS (10 tests)

- [ ] **Step 5: 전체 회귀를 확인한다**

Run: `python -m pytest tests/ -q`
Expected: 기존 통과 수 + 10, 실패 0

- [ ] **Step 6: 커밋**

```bash
git add docs/tool/se/app.js tests/se/test_se_app_js.py
git commit -m "feat(se): 사실 강조 뼈대 + 타법인 출자 3규칙"
```

---

## Task 2: 나머지 섹션 규칙

**Files:**
- Modify: `docs/tool/se/app.js`
- Test: `tests/se/test_se_app_js.py`

**Interfaces:**
- Consumes: Task 1의 `markNumber`·`markLt`·`markNeg`·`MARK_RULES`·`cellMarks`
- Consumes: `pickAccount(records, name)`·`ACCOUNT_ALIASES` (app.js, SE-4f) — 재무 계정 조회는 **반드시 이걸 쓴다.** `당기순이익(손실)` 처럼 괄호가 붙은 실제 계정명을 문자열 비교로 다시 짜면 SE-4f에서 겪은 결함을 되풀이한다.
- Consumes: `is_amendment_disclosure` 상당의 판별 — **파이썬 쪽 core를 부를 수 없다.** 정정공시 판별은 `report_nm`이 `[`로 시작하고 `정정]`을 포함하는지로 판단하되, 아래 실측 접두어를 테스트로 고정한다.
- Produces: `MARK_RULES`에 아래 섹션 키 추가

**섹션 키는 서버 레지스트리와 일치해야 한다.** 구현 전에 확인하라:

```bash
grep -rn "affiliates\|audit_history\|shareholders\|insider_timeline\|financials\|dividends\|disclosures\|distress" se_server/ --include=*.py | grep -i "key\|registry\|section" | head -30
```

키가 이 계획의 표기와 다르면 **레지스트리 쪽이 정답**이다. 다르면 보고하라.

**추가할 규칙:**

| 섹션 | 표시 컬럼 | 조건 | `why` 문구 |
|---|---|---|---|
| `audit_history` | `adt_opinion` | `String(v).trim()`이 `"적정"`으로 **시작하지 않음** | `감사의견이 "적정"이 아님` |
| `shareholders` | `trmend_posesn_stock_qota_rt` | `< bsis_posesn_stock_qota_rt` | `기말 지분율 < 기초 지분율` |
| `insider_timeline` | `delta` (실제 키는 아래 확인) | `< 0` | `보유 비율 감소` |
| `disclosures` | `report_nm` | 정정공시 | `정정공시` |
| `distress` | `rcept_no` | 레코드 존재(항상 참) | `부실 관련 공시가 보고됨` |

**⚠️ `insider_timeline`의 증감 컬럼 키를 반드시 실제 응답에서 확인하라.** 이 계획은 그 키를 확정하지 못했다. 확인 방법:

```bash
grep -n "insider_timeline" -A 25 docs/tool/se/app.js | grep -n "delta\|증감\|rate\|qota"
```

키를 확인하지 못하면 **이 규칙을 빼고 그 사실을 보고하라.** 추측한 키로 규칙을 만들면 화면에서 조용히 아무 일도 일어나지 않는다(SE-4f에서 정렬이 이렇게 죽었다).

**⚠️ 재무제표(`financials`)와 파생 지표(`financial_ratios`)는 레코드 형태가 다르다.**
`financials`는 계정과목 행 배열이라 `{키: 값}` 레코드 규칙이 맞지 않는다. **`cellMarks`의 레코드 모델과 어긋나므로 이번 태스크에서 제외하고, 그 사실을 보고하라.** 억지로 끼워 넣지 마라. 파생 지표는 `{구분, 기간, 지표, 값, ...}` 형태라 규칙이 맞으므로 아래를 넣는다.

| 섹션 | 표시 컬럼 | 조건 | `why` 문구 |
|---|---|---|---|
| `financial_ratios` | `값` | `지표`가 `"영업이익률"` 또는 `"순이익률"` 이고 `값 < 0` | `이익률 < 0` |

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
class TestAuditOpinionMark:
    def test_jeokjeong_is_not_marked(self):
        rows = '[{"adt_opinion":"적정의견"}]'
        assert json.loads(run_js('console.log(JSON.stringify(cellMarks(%s,"audit_history")))' % rows)) == {}

    def test_bujeokjeong_is_marked(self):
        # "부적정의견"은 부분 문자열 "적정"을 포함한다. indexOf 로 짜면
        # 가장 무거운 의견을 정상으로 읽는다 — 접두 일치여야 한다.
        rows = '[{"adt_opinion":"부적정의견"}]'
        m = json.loads(run_js('console.log(JSON.stringify(cellMarks(%s,"audit_history")))' % rows))
        assert m == {"0|adt_opinion": '감사의견이 "적정"이 아님'}

    def test_hanjeong_and_georeol_are_marked(self):
        rows = '[{"adt_opinion":"한정의견"},{"adt_opinion":"의견거절"}]'
        m = json.loads(run_js('console.log(JSON.stringify(cellMarks(%s,"audit_history")))' % rows))
        assert sorted(m.keys()) == ["0|adt_opinion", "1|adt_opinion"]

    def test_whitespace_around_jeokjeong_is_tolerated(self):
        rows = '[{"adt_opinion":" 적정의견 "}]'
        assert json.loads(run_js('console.log(JSON.stringify(cellMarks(%s,"audit_history")))' % rows)) == {}


class TestShareholderMark:
    def test_decrease_is_marked(self):
        # 실측: 오정강 14.82 → 14.41
        rows = ('[{"nm":"오정강","bsis_posesn_stock_qota_rt":"14.82","trmend_posesn_stock_qota_rt":"14.41"},'
                '{"nm":"박수정","bsis_posesn_stock_qota_rt":"0.07","trmend_posesn_stock_qota_rt":"0.07"},'
                '{"nm":"정지선","bsis_posesn_stock_qota_rt":"0.00","trmend_posesn_stock_qota_rt":"0.03"}]')
        m = json.loads(run_js('console.log(JSON.stringify(cellMarks(%s,"shareholders")))' % rows))
        assert list(m.keys()) == ["0|trmend_posesn_stock_qota_rt"]


class TestDisclosureAmendmentMark:
    def test_known_amendment_prefixes(self):
        rows = ('[{"report_nm":"[기재정정]주요사항보고서"},{"report_nm":"[첨부정정]사업보고서"},'
                '{"report_nm":"[정정]반기보고서"},{"report_nm":"사업보고서 (2025.12)"}]')
        m = json.loads(run_js('console.log(JSON.stringify(cellMarks(%s,"disclosures")))' % rows))
        assert sorted(m.keys()) == ["0|report_nm", "1|report_nm", "2|report_nm"]

    def test_bracket_without_amendment_is_not_marked(self):
        rows = '[{"report_nm":"[첨부추가]사업보고서"}]'
        assert json.loads(run_js('console.log(JSON.stringify(cellMarks(%s,"disclosures")))' % rows)) == {}


class TestRatioMark:
    def test_negative_margin_marked_but_other_negatives_not(self):
        rows = ('[{"지표":"영업이익률","값":-25.1},{"지표":"순이익률","값":-22.6},'
                '{"지표":"부채비율","값":130.2},{"지표":"유동비율","값":61.3},'
                '{"지표":"영업이익률","값":3.4}]')
        m = json.loads(run_js('console.log(JSON.stringify(cellMarks(%s,"financial_ratios")))' % rows))
        assert sorted(m.keys()) == ["0|값", "1|값"]

    def test_null_value_is_not_marked(self):
        rows = '[{"지표":"순이익률","값":null,"사유":"매출액 없음"}]'
        assert json.loads(run_js('console.log(JSON.stringify(cellMarks(%s,"financial_ratios")))' % rows)) == {}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/se/test_se_app_js.py -k "AuditOpinionMark or ShareholderMark or DisclosureAmendmentMark or RatioMark" -v`
Expected: FAIL

- [ ] **Step 3: 구현**

`MARK_RULES`에 추가한다. 접두 일치가 핵심이다.

```js
// "부적정의견"은 부분 문자열 "적정"을 포함한다 — indexOf 로 판정하면
// 가장 무거운 의견을 정상으로 읽는다. 반드시 접두 일치로 본다.
function isCleanOpinion(v) {
  return String(v == null ? "" : v).trim().indexOf("적정") === 0;
}

MARK_RULES.audit_history = [
  {
    key: "adt_opinion",
    when: function (r) { return !isNoDataMarker(r.adt_opinion) && !isCleanOpinion(r.adt_opinion); },
    why: '감사의견이 "적정"이 아님',
  },
];

MARK_RULES.shareholders = [
  {
    key: "trmend_posesn_stock_qota_rt",
    when: function (r) { return markLt(r.trmend_posesn_stock_qota_rt, r.bsis_posesn_stock_qota_rt); },
    why: "기말 지분율 < 기초 지분율",
  },
];

// core 의 is_amendment_disclosure 는 파이썬이라 여기서 부를 수 없다.
// 실측 접두어를 테스트로 고정해 두고 같은 판별을 다시 쓴다.
function isAmendmentName(v) {
  const s = String(v == null ? "" : v).trim();
  if (s.charAt(0) !== "[") return false;
  const close = s.indexOf("]");
  return close > 0 && s.slice(1, close).indexOf("정정") >= 0;
}

MARK_RULES.disclosures = [
  { key: "report_nm", when: function (r) { return isAmendmentName(r.report_nm); }, why: "정정공시" },
];

const MARGIN_INDICATORS = ["영업이익률", "순이익률"];

MARK_RULES.financial_ratios = [
  {
    key: "값",
    when: function (r) { return MARGIN_INDICATORS.indexOf(r["지표"]) >= 0 && markNeg(r["값"]); },
    why: "이익률 < 0",
  },
];

MARK_RULES.distress = [
  { key: "rcept_no", when: function () { return true; }, why: "부실 관련 공시가 보고됨" },
];
```

`insider_timeline` 규칙은 **위 확인 절차를 거친 뒤에만** 같은 형식으로 추가한다.

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/se/test_se_app_js.py -v`
Expected: PASS

- [ ] **Step 5: 전체 회귀**

Run: `python -m pytest tests/ -q`
Expected: 실패 0

- [ ] **Step 6: 커밋**

```bash
git add docs/tool/se/app.js tests/se/test_se_app_js.py
git commit -m "feat(se): 감사의견·지분율·정정공시·이익률 강조 규칙"
```

---

## Task 3: 화면 적용 — 셀 표시와 범례

**Files:**
- Modify: `docs/tool/se/ui.js` (`tableEl` 부근, ui.js:743~)
- Modify: `docs/tool/se/index.html`
- Test: `tests/se/test_se_page_assets.py`

**Interfaces:**
- Consumes: `cellMarks(records, sectionKey)`·`markLegend(records, sectionKey)`
- Consumes: `tableEl(table)`가 이미 계산하는 좌표 — ui.js:798~799
  ```js
  const isValueCell = isVertical ? (i === 1) : true;
  const cellKey = isVertical ? table.keys[rowIdx] : table.keys[i];
  ```
  **이 두 줄이 좌표의 정본이다.** 새로 계산하지 말고 그대로 쓴다. 세로 표는 레코드가 하나이므로 행 번호는 `0`, 가로 표는 `rowIdx`.

**시그니처 변경:** `tableEl(table, marks)` — `marks`는 `cellMarks`의 반환값(선택 인자). 호출부에서 넘기지 않으면 아무 표시도 하지 않아 기존 동작이 그대로다.

**⚠️ 세 가지 렌더 경로를 모두 덮어야 한다.**

1. **본문 셀** — 일반 경로
2. **caption(승격된 열)** — `tableLayout`은 **모든 행에서 값이 같은 열을 caption으로 올린다.** 예를 들어 3개 연도 감사의견이 전부 `의견거절`이면 그 열은 caption으로 올라가고, 본문 셀만 처리하면 **가장 중요한 표시가 조용히 사라진다.** caption 항목의 `key`에 대해 레코드 0번의 표시를 확인해 붙인다.
3. **접힌 열(`foldedKeys`/`foldedRows`)** — 펼쳤을 때 나오는 칸에도 같은 규칙을 적용한다.

**범례:** 표 바로 아래에 `markLegend`가 돌려준 문구를 `·`로 이어 한 줄 넣는다. 앞에 `강조: `를 붙인다. 발화한 규칙이 없으면 **아무것도 렌더하지 않는다**(빈 줄을 남기지 않는다).

- [ ] **Step 1: 실패하는 정적 검사를 쓴다**

`tests/se/test_se_page_assets.py`에 추가한다.

```python
class TestMarkStyling:
    def test_mark_vars_defined_in_both_themes(self):
        css = read_index_css()  # 기존 헬퍼 재사용
        dark = extract_rule(css, ":root")
        light = extract_rule(css, ':root[data-theme="light"]')
        for var in ("--mark", "--mark-bg"):
            assert var in dark, f"{var}가 다크 테마에 없다"
            assert var in light, f"{var}가 라이트 테마에 없다"

    def test_mark_does_not_set_text_color(self):
        # 글자색을 바꾸면 대비가 무너진다. 사장님이 지적한 지점이다.
        rule = extract_rule(read_index_css(), ".mk")
        assert "border-left" in rule
        assert not re.search(r"(^|[;{\s])color\s*:", rule), ".mk 가 글자색을 바꾼다"

    def test_mark_background_is_subtle(self):
        # 엔켐 실측 27건 중 21건(78%)이 강조 대상이다. 배경을 진하게 채우면
        # 표 대부분이 칠해져 강조가 배경이 된다.
        rule = extract_rule(read_index_css(), ":root")
        m = re.search(r"--mark-bg:\s*[^;]*?([\d.]+)\s*\)", rule)
        assert m, "--mark-bg 가 알파 채널을 쓰지 않는다"
        assert float(m.group(1)) <= 0.10, f"--mark-bg 알파 {m.group(1)} — 너무 진하다"


class TestMarkRenderPaths:
    def test_caption_path_applies_marks(self):
        # 값이 모든 행에서 같으면 tableLayout 이 그 열을 caption 으로 올린다.
        # 본문 셀만 처리하면 그때 표시가 조용히 사라진다.
        src = read_ui_js()
        cap = extract_function(src, "tableEl")
        head, _, tail = cap.partition("const t = document.createElement(\"table\")")
        assert "marks" in head, "caption 렌더 경로에 marks 적용이 없다"

    def test_folded_path_applies_marks(self):
        src = read_ui_js()
        assert re.search(r"folded[\s\S]{0,600}?marks", extract_function(src, "tableEl")), \
            "접힌 열 렌더 경로에 marks 적용이 없다"


class TestMarkVocabulary:
    def test_rule_texts_carry_no_verdict_words(self):
        # 강조는 사실의 가시화다. 강조에 이름을 붙이면 판정이 된다.
        src = read_app_js()
        block = src[src.index("MARK_RULES"):]
        for w in ("부실", "위험", "주의", "손상", "악화", "양호", "이상 징후", "경고", "심각"):
            assert w not in block, f"규칙 문구에 판정 어휘 '{w}' 가 있다"
```

`read_index_css`·`extract_rule`·`extract_function`·`read_ui_js`·`read_app_js`는 이 파일에 이미 있는 헬퍼다. 없으면 기존 검사가 쓰는 것과 같은 방식으로 만든다.

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/se/test_se_page_assets.py -k Mark -v`
Expected: FAIL

- [ ] **Step 3: CSS를 넣는다**

`docs/tool/se/index.html`의 `:root`와 `:root[data-theme="light"]` 양쪽에 추가한다.

```css
:root {
  --mark: #d98a3a;
  --mark-bg: rgba(217, 138, 58, 0.08);
}
:root[data-theme="light"] {
  --mark: #b5661c;
  --mark-bg: rgba(181, 102, 28, 0.08);
}
```

```css
/* 강조는 얇은 좌측 막대가 주 표기다. 엔켐 실측에서 타법인 출자 27건 중
   19건(70%)이 강조 대상이라, 배경을 진하게 채우면 표 대부분이 칠해져
   강조가 배경이 된다. 글자색은 건드리지 않는다 — 대비가 무너진다. */
.mk {
  border-left: 3px solid var(--mark);
  background: var(--mark-bg);
}
.mk-legend {
  margin-top: 6px;
  font-size: 12px;
  color: var(--muted);
}
.mk-legend b { color: var(--mark); font-weight: 600; }
```

- [ ] **Step 4: `tableEl`에 적용한다**

본문 셀은 기존 좌표 계산 바로 뒤에 붙인다.

```js
const markKey = (isVertical ? 0 : rowIdx) + "|" + cellKey;
const why = marks && isValueCell ? marks[markKey] : null;
if (why) { td.classList.add("mk"); td.title = why; }
```

caption과 접힌 열 경로에도 같은 방식으로 적용한다. caption은 레코드 0번 기준이다.

범례는 `tableEl`이 만드는 조각 맨 뒤에 붙인다. 문구는 `textContent`로만 넣는다.

호출부(`renderSection` 등)에서 `cellMarks(records, key)`를 계산해 `tableEl(table, marks)`로 넘긴다. `key`는 `renderSection`이 이미 받고 있다(SE-4d에서 배선했다).

- [ ] **Step 5: 통과를 확인한다**

Run: `python -m pytest tests/se/ -v`
Expected: PASS

- [ ] **Step 6: 전체 회귀**

Run: `python -m pytest tests/ -q`
Expected: 실패 0

- [ ] **Step 7: 커밋**

```bash
git add docs/tool/se/ui.js docs/tool/se/index.html tests/se/test_se_page_assets.py
git commit -m "feat(se): 강조 셀 렌더 + 범례 (본문·caption·접힌 열)"
```

---

## Task 4: 로그아웃 잔류 확인 + 실측 검증

**Files:**
- Modify: `docs/tool/se/ui.js` (필요 시 `showGate`)
- Test: `tests/se/test_se_page_assets.py`

- [ ] **Step 1: 잔류 검사를 쓴다**

강조 범례는 실명이 아니지만 **이전 사용자가 조회한 회사의 사실**을 드러낸다. 표 자체가 `showGate()`에서 지워지면 범례도 함께 지워지는지 확인한다.

```python
class TestMarksClearedOnGate:
    def test_legend_lives_inside_cleared_container(self):
        # 범례가 #body 밖에 붙으면 로그아웃 후에도 남는다.
        src = read_ui_js()
        gate = extract_function(src, "showGate")
        assert "#body" in gate or "body" in gate
        te = extract_function(src, "tableEl")
        assert "document.body.appendChild" not in te, "범례가 표 조각 밖에 붙는다"
```

- [ ] **Step 2: 실행하고 필요하면 고친다**

Run: `python -m pytest tests/se/test_se_page_assets.py -k Gate -v`

- [ ] **Step 3: 실측 검증 — 엔켐으로 규칙이 실제로 발화하는지 확인한다**

Node로 `app.js`를 직접 불러 실 API 응답 형태의 27건에 대해 발화 건수를 센다. **기대값은 배경 절의 실측과 같아야 한다: 기말장부<최초취득 7건, 순이익<0 21건, 평가손익<0 3건.**

실 API를 다시 호출해 대조하라(키는 `.env.local`의 `DART_API_KEY`). 숫자가 다르면 규칙이나 파싱이 틀린 것이다 — **보고하고 멈춰라.**

- [ ] **Step 4: 전체 회귀 + 커밋**

```bash
python -m pytest tests/ -q
git add -A && git commit -m "test(se): 강조 잔류 방지 + 엔켐 실측 대조"
```

---

## Self-Review 결과

- **범위 밖으로 명시한 것:** 자금 사용 "차이 발생 사유"(필드 확인 실패), 재무제표 계정 행(레코드 모델 불일치), 배당 적자-배당 동시(파생 블록 형태 확인 필요), 감사인 교체·비감사용역(core 산출물 형태 확인 필요). **없는 것을 있는 것처럼 만들지 않는다.**
- **라이브 미검증(⚠):** 감사의견 비적정·부실 이벤트·완전자본잠식은 엔켐에서 발화하지 않는다. 규칙은 맞지만 실화면 확인 사례가 없다. PR에 그대로 적는다.
- **밀도 문제:** 엔켐 타법인 출자 27건 중 19건이 강조된다. 좌측 막대 표기와 알파 0.08 제약으로 대응했고, 검사로 고정했다.
