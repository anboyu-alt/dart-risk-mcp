# v0.8.0 구현 계획 — 탐지 정확도 업그레이드

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** DART OpenAPI 구조화 엔드포인트 8종을 `check_disclosure_anomaly` 지표 ②와 CB_ROLLOVER 탐지에 통합하고, 단일 출력 원칙을 문서로 공식 확정한다.

**Architecture:** 기존 "core 함수 → server 렌더링 → 테스트 유닛(unittest)" 레이어 패턴을 그대로 따른다. 신규 8개 엔드포인트를 `dart_client.py`의 기존 `_fund_usage_cache` 스타일 LRU(최대 20건·10분 TTL)로 감싸고, 실패 시 기존 키워드 매칭으로 graceful degradation한다. CB_ROLLOVER는 잔액 시계열 + 이벤트 조합으로 판정하는 신규 `detect_debt_rollover` 함수가 담당한다.

**Tech Stack:** Python 3.11+ / `requests` / `mcp>=1.0.0` / `unittest.mock` — 외부 의존성 추가 없음(CLAUDE.md 규칙).

**Spec 링크:** `docs/superpowers/specs/2026-04-25-v0.8.0-design.md`

---

## 전체 구조

3개 섹션을 순서대로 구현한다. 각 섹션 끝에서 테스트 PASS를 확인한 뒤 다음 섹션으로 진행.

**섹션 A — Expert mode 분기 공식 폐기 (문서 전용, Task 1)**
**섹션 B — AUDIT 감사의견 구조화 (Task 2~4)**
**섹션 C — CB_ROLLOVER 미상환 잔액 시계열 (Task 5~8)**
**섹션 D — 릴리스(Task 9~10)**

---

## 파일 구조 맵

| 파일 | 역할 | 이 계획에서의 조치 |
|---|---|---|
| `dart_risk_mcp/core/dart_client.py` | DART API 클라이언트, 캐시, 이상 플래그 탐지 | `fetch_audit_opinion_history`, `fetch_debt_balance`, `detect_debt_rollover` + 2개 URL 매핑 + 2개 캐시 추가 (~300줄) |
| `dart_risk_mcp/core/__init__.py` | 공개 API export | 신규 3개 심볼 추가 |
| `dart_risk_mcp/server.py` | MCP 도구 정의 | `get_audit_opinion_history`, `track_debt_balance` 신규 2개 + `check_disclosure_anomaly`·`track_capital_structure` 2개 보강 (~250줄) |
| `tests/test_audit_opinion_history.py` | fetch_audit_opinion_history 유닛 테스트 | 신규(10 테스트) |
| `tests/test_debt_balance.py` | fetch_debt_balance + detect_debt_rollover 유닛 테스트 | 신규(10 테스트) |
| `tests/test_financial_anomaly.py` | `check_disclosure_anomaly` 기존 assert | 지표 ② 출력 형식 갱신 |
| `tests/fixtures/sample_outputs/*.txt` | 골드 파일 15개 | 전체 재생성 |
| `CHANGELOG.md`, `README.md`, `CLAUDE.md`, `__init__.py`, `pyproject.toml` | 문서·버전 | v0.8.0 섹션 추가 |

---

## 검증 커맨드 레퍼런스

```bash
# 특정 테스트 파일만
python -m pytest tests/test_audit_opinion_history.py -v

# 전체 스위트(섹션 완료 시마다)
python -m pytest tests/ -v

# 서버 import + 도구 개수
python -c "from dart_risk_mcp.server import mcp; print(len(list(mcp._tool_manager.list_tools())))"
# Expected: 23 (섹션 B 후 22, 섹션 C 후 23)

# 버전
python -c "import dart_risk_mcp; print(dart_risk_mcp.__version__)"
```

---

---

# 섹션 A — Expert mode 분기 공식 폐기

## Task 1: CHANGELOG·README에 단일 출력 원칙 확정 선언

**Files:**
- Modify: `CHANGELOG.md` (상단에 v0.8.0 플레이스홀더 섹션 추가)
- Modify: `README.md` (상단 소개 뒤에 "출력 원칙" 블록 삽입)

- [ ] **Step 1: CHANGELOG.md 상단에 v0.8.0 플레이스홀더 블록 추가**

`CHANGELOG.md` 맨 위 `# CHANGELOG` 헤더 바로 아래, 현재 v0.7.5 섹션 **위**에 다음 블록 삽입:

```markdown
## [0.8.0] — 2026-04-25 (예정)

### Design Principle (확정)
- **단일 출력 원칙 확정** — v0.7.x 동안 모든 도구를 하나의 한국어 서술 출력로 통일했습니다. v0.8.0에서 expert/easy 모드 분기 가능성을 공식 폐기합니다. 향후 어떤 도구도 `level=`·`mode=`·`format=` 같은 분기 파라미터를 받지 않습니다. 사용자가 더 자세한 원시 데이터를 원하면 개별 도구(`get_disclosure_document`, `view_disclosure`, `list_disclosure_sections`)를 조합해 파이프라인을 구성합니다.
```

(Section B/C의 구체 change 항목은 Task 10에서 채운다.)

- [ ] **Step 2: README.md에 "출력 원칙" 블록 추가**

`README.md` 상단 소개(`# DART 리스크 MCP ...`) 바로 아래, 첫 번째 `##` 섹션 앞에 다음을 삽입:

```markdown
## 출력 원칙

모든 도구는 **하나의 한국어 서술 출력**만 반환합니다. level/mode/format 분기는 지원하지 않습니다. 원시 데이터가 필요하면 `get_disclosure_document`·`view_disclosure`·`list_disclosure_sections` 같은 원문 도구를 직접 조합하세요.
```

- [ ] **Step 3: 커밋**

```bash
git add CHANGELOG.md README.md
git commit -m "docs: v0.8.0 단일 출력 원칙 공식 확정"
```

---

---

# 섹션 B — AUDIT 감사의견 구조화

## Task 2: `fetch_audit_opinion_history` 핵심 함수 (TDD)

**Files:**
- Create: `tests/test_audit_opinion_history.py`
- Modify: `dart_risk_mcp/core/dart_client.py` (URL 매핑 + 캐시 + 함수 추가)
- Modify: `dart_risk_mcp/core/__init__.py` (export 추가)

- [ ] **Step 1: 실패하는 테스트 작성 — 기본 파싱**

`tests/test_audit_opinion_history.py` 생성:

```python
"""fetch_audit_opinion_history 파싱·경고·부분실패·캐시 검증."""
import unittest
from unittest.mock import patch, MagicMock

from dart_risk_mcp.core import dart_client


def _resp(status="000", lst=None):
    r = MagicMock()
    r.json.return_value = {
        "status": status,
        "message": "정상" if status == "000" else "데이터 없음",
        "list": lst or [],
    }
    return r


class TestFetchAuditOpinionHistory(unittest.TestCase):
    def setUp(self):
        dart_client._audit_history_cache.clear()

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_parses_opinion_auditor_year(self, mock_retry):
        # 3개 엔드포인트 응답을 순서대로 반환
        mock_retry.side_effect = [
            _resp(lst=[  # 감사의견 엔드포인트
                {"bsns_year": "2025", "adtor": "삼일회계법인", "adt_opinion": "적정"},
                {"bsns_year": "2024", "adtor": "삼일회계법인", "adt_opinion": "적정"},
                {"bsns_year": "2023", "adtor": "한영회계법인", "adt_opinion": "적정"},
            ]),
            _resp(lst=[  # 감사용역 계약 엔드포인트
                {"bsns_year": "2025", "mendng": "820000000", "tot_reqre_time": "3000"},
            ]),
            _resp(lst=[  # 비감사용역 엔드포인트
                {"bsns_year": "2025", "cntrct_cncls_de": "2025-03-10", "servc_cn": "세무자문", "mendng": "110000000"},
            ]),
        ]
        result = dart_client.fetch_audit_opinion_history("00000001", "KEY", 5)
        self.assertEqual(len(result["opinions"]), 3)
        self.assertEqual(result["opinions"][0]["year"], 2025)
        self.assertEqual(result["opinions"][0]["opinion"], "적정")
        self.assertEqual(result["opinions"][0]["auditor"], "삼일회계법인")
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```
python -m pytest tests/test_audit_opinion_history.py::TestFetchAuditOpinionHistory::test_parses_opinion_auditor_year -v
```
Expected: FAIL — `module 'dart_client' has no attribute '_audit_history_cache'` 또는 `has no attribute 'fetch_audit_opinion_history'`

- [ ] **Step 3: `dart_client.py`에 URL 매핑 + 캐시 + 상수 추가**

`dart_client.py`에서 `_major_decision_cache` 정의 블록(~260줄 근처) **다음 줄**에 추가:

```python
# v0.8.0: 감사의견 이력 엔드포인트 3종 ----------------------------
_AUDIT_OPINION_URLS = {
    "opinion":       f"{DART_BASE}/accnutAdtorNmNdAdtOpinion.json",        # 2020025
    "audit_service": f"{DART_BASE}/adtServcCnclsSttus.json",               # 2020026
    "non_audit":     f"{DART_BASE}/accnutAdtorNonAdtServcCnclsSttus.json", # 2020027
}

_audit_history_cache: dict[tuple, tuple[float, dict]] = {}
_AUDIT_CACHE_MAX = 20
_AUDIT_CACHE_TTL = 600  # 10분

_NON_AUDIT_THRESHOLD = 0.30  # 비감사용역 비중 30% 이상 경고
_AUDITOR_CHANGE_WINDOW = 3   # 3년 내 2회 이상 교체 경고
```

- [ ] **Step 4: `fetch_audit_opinion_history` 함수 추가**

`dart_client.py` 끝부분(기존 `detect_financial_anomaly` 정의가 끝난 뒤)에 추가:

```python
def fetch_audit_opinion_history(
    corp_code: str,
    api_key: str,
    lookback_years: int = 5,
) -> dict:
    """감사의견·감사인 교체·비감사용역 통합 조회.

    반환:
        {
            "opinions": [{"year": int, "opinion": str, "auditor": str,
                          "tenure_years": int, "audit_fee_okwon": int,
                          "non_audit_fee_okwon": int}],
            "auditor_changes": [{"from_year": int, "to_year": int,
                                 "from": str, "to": str}],
            "independence_warnings": [str, ...],
        }
    3개 엔드포인트 중 일부가 실패해도 남은 값으로 구성한다.
    """
    if not corp_code or not api_key:
        return {"opinions": [], "auditor_changes": [], "independence_warnings": []}

    if not isinstance(lookback_years, int) or not (1 <= lookback_years <= 10):
        lookback_years = 5

    cache_key = (corp_code, lookback_years)
    cached = _cache_get(_audit_history_cache, cache_key, _AUDIT_CACHE_TTL)
    if cached is not None:
        return cached

    # 엔드포인트 3종 병렬 조회 (실패 시 빈 리스트로 graceful degradation)
    raw: dict[str, list] = {}
    for kind, url in _AUDIT_OPINION_URLS.items():
        try:
            resp = _retry("GET", url, params={"crtfc_key": api_key, "corp_code": corp_code}, timeout=15)
            data = resp.json() if resp is not None else {}
            if data.get("status") == "000":
                raw[kind] = data.get("list", [])
            else:
                raw[kind] = []
        except Exception:
            raw[kind] = []

    current_year = _dt.datetime.now().year
    cutoff = current_year - lookback_years

    # 연도별 감사의견
    by_year: dict[int, dict] = {}
    for item in raw.get("opinion", []):
        y = _safe_int(item.get("bsns_year"))
        if y is None or y < cutoff:
            continue
        by_year.setdefault(y, {})
        by_year[y]["year"] = y
        by_year[y]["opinion"] = item.get("adt_opinion", "").strip()
        by_year[y]["auditor"] = item.get("adtor", "").strip()

    # 감사보수 (억원 단위)
    for item in raw.get("audit_service", []):
        y = _safe_int(item.get("bsns_year"))
        if y is None or y < cutoff:
            continue
        by_year.setdefault(y, {"year": y, "opinion": "", "auditor": ""})
        by_year[y]["audit_fee_okwon"] = _safe_int(item.get("mendng")) or 0

    # 비감사용역 보수 (연도별 합산)
    non_audit_by_year: dict[int, int] = {}
    for item in raw.get("non_audit", []):
        y = _safe_int_from_de(item.get("cntrct_cncls_de", ""))
        if y is None or y < cutoff:
            continue
        non_audit_by_year[y] = non_audit_by_year.get(y, 0) + (_safe_int(item.get("mendng")) or 0)
    for y, amount in non_audit_by_year.items():
        by_year.setdefault(y, {"year": y, "opinion": "", "auditor": ""})
        by_year[y]["non_audit_fee_okwon"] = amount

    # opinions 리스트(최신 순)
    opinions = []
    for y in sorted(by_year.keys(), reverse=True):
        e = by_year[y]
        opinions.append({
            "year": e.get("year", y),
            "opinion": e.get("opinion", ""),
            "auditor": e.get("auditor", ""),
            "tenure_years": 0,  # 다음 루프에서 채움
            "audit_fee_okwon": e.get("audit_fee_okwon", 0),
            "non_audit_fee_okwon": e.get("non_audit_fee_okwon", 0),
        })

    # tenure_years 계산(같은 감사인 연속 횟수, 최신부터 역순)
    prev_auditor = None
    run = 0
    for o in opinions:  # 최신 → 과거
        if o["auditor"] and o["auditor"] == prev_auditor:
            run += 1
        else:
            run = 1
        o["tenure_years"] = run
        prev_auditor = o["auditor"]

    # 감사인 교체 이벤트(과거 → 최신)
    auditor_changes = []
    asc = sorted(opinions, key=lambda x: x["year"])
    for i in range(1, len(asc)):
        prev_a = asc[i-1]["auditor"]
        cur_a = asc[i]["auditor"]
        if prev_a and cur_a and prev_a != cur_a:
            auditor_changes.append({
                "from_year": asc[i-1]["year"],
                "to_year": asc[i]["year"],
                "from": prev_a,
                "to": cur_a,
            })

    # 독립성 경고
    warnings: list[str] = []
    for o in opinions:
        af = o.get("audit_fee_okwon", 0)
        naf = o.get("non_audit_fee_okwon", 0)
        if af + naf > 0:
            ratio = naf / (af + naf)
            if ratio >= _NON_AUDIT_THRESHOLD:
                warnings.append(f"{o['year']} 비감사용역 비중 {int(ratio*100)}%")

    result = {
        "opinions": opinions,
        "auditor_changes": auditor_changes,
        "independence_warnings": warnings,
    }
    _cache_set(_audit_history_cache, cache_key, result, _AUDIT_CACHE_MAX)
    return result
```

- [ ] **Step 5: 보조 유틸 `_safe_int`, `_safe_int_from_de` 추가**

`dart_client.py` 상단 임포트 블록 근처(`import datetime as _dt`가 이미 있는지 확인; 없으면 추가)에 유틸을 넣는다. 먼저 `_retry` 정의 **위**에 `import datetime as _dt`가 없으면 추가하고, 이어서 아래 함수를 같은 위치에 추가:

```python
def _safe_int(v) -> int | None:
    """문자열/None → int. 콤마·공백 제거. 실패 시 None."""
    if v is None:
        return None
    s = str(v).replace(",", "").strip()
    if not s or s in {"-", "N/A"}:
        return None
    try:
        return int(float(s))
    except (ValueError, TypeError):
        return None


def _safe_int_from_de(de: str) -> int | None:
    """'YYYY-MM-DD' 또는 'YYYYMMDD' → 연도(int). 실패 시 None."""
    if not de:
        return None
    s = str(de).replace("-", "").replace(".", "")[:4]
    return _safe_int(s)
```

※ 이미 `_safe_int` 같은 이름이 존재하면 기존 것을 재사용(중복 정의 금지). 먼저 `grep -n "def _safe_int" dart_risk_mcp/core/dart_client.py`로 확인.

- [ ] **Step 6: `core/__init__.py`에 export 추가**

`dart_risk_mcp/core/__init__.py`의 `__all__` 리스트와 import 블록에 `fetch_audit_opinion_history` 추가. 기존 `fetch_fund_usage`가 있는 그룹 바로 아래에 한 줄씩:

```python
from .dart_client import (
    # ... 기존 import들 ...
    fetch_audit_opinion_history,
)

__all__ = [
    # ... 기존 항목들 ...
    "fetch_audit_opinion_history",
]
```

- [ ] **Step 7: 테스트 실행 — PASS 확인**

```
python -m pytest tests/test_audit_opinion_history.py::TestFetchAuditOpinionHistory::test_parses_opinion_auditor_year -v
```
Expected: 1 passed.

- [ ] **Step 8: 나머지 테스트 9개 추가**

`tests/test_audit_opinion_history.py` `TestFetchAuditOpinionHistory` 클래스에 다음 테스트 추가:

```python
    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_detects_auditor_change(self, mock_retry):
        mock_retry.side_effect = [
            _resp(lst=[
                {"bsns_year": "2025", "adtor": "삼일", "adt_opinion": "적정"},
                {"bsns_year": "2024", "adtor": "한영", "adt_opinion": "적정"},
            ]),
            _resp(lst=[]),
            _resp(lst=[]),
        ]
        r = dart_client.fetch_audit_opinion_history("00000001", "KEY", 5)
        self.assertEqual(len(r["auditor_changes"]), 1)
        self.assertEqual(r["auditor_changes"][0]["from"], "한영")
        self.assertEqual(r["auditor_changes"][0]["to"], "삼일")

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_no_change_when_same_auditor(self, mock_retry):
        mock_retry.side_effect = [
            _resp(lst=[
                {"bsns_year": "2025", "adtor": "삼일", "adt_opinion": "적정"},
                {"bsns_year": "2024", "adtor": "삼일", "adt_opinion": "적정"},
            ]),
            _resp(lst=[]),
            _resp(lst=[]),
        ]
        r = dart_client.fetch_audit_opinion_history("00000001", "KEY", 5)
        self.assertEqual(r["auditor_changes"], [])
        self.assertEqual(r["opinions"][0]["tenure_years"], 2)

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_non_audit_warning_at_30_percent(self, mock_retry):
        mock_retry.side_effect = [
            _resp(lst=[{"bsns_year": "2025", "adtor": "삼일", "adt_opinion": "적정"}]),
            _resp(lst=[{"bsns_year": "2025", "mendng": "700000000"}]),
            _resp(lst=[{"bsns_year": "2025", "cntrct_cncls_de": "2025-03-10",
                        "servc_cn": "세무", "mendng": "300000000"}]),
        ]
        r = dart_client.fetch_audit_opinion_history("00000001", "KEY", 5)
        self.assertTrue(any("30%" in w for w in r["independence_warnings"]))

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_non_audit_no_warning_below_30_percent(self, mock_retry):
        mock_retry.side_effect = [
            _resp(lst=[{"bsns_year": "2025", "adtor": "삼일", "adt_opinion": "적정"}]),
            _resp(lst=[{"bsns_year": "2025", "mendng": "900000000"}]),
            _resp(lst=[{"bsns_year": "2025", "cntrct_cncls_de": "2025-03-10",
                        "servc_cn": "세무", "mendng": "100000000"}]),
        ]
        r = dart_client.fetch_audit_opinion_history("00000001", "KEY", 5)
        self.assertEqual(r["independence_warnings"], [])

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_partial_failure_one_endpoint(self, mock_retry):
        mock_retry.side_effect = [
            _resp(lst=[{"bsns_year": "2025", "adtor": "삼일", "adt_opinion": "적정"}]),
            _resp(status="013"),  # 감사용역 없음
            _resp(status="013"),  # 비감사 없음
        ]
        r = dart_client.fetch_audit_opinion_history("00000001", "KEY", 5)
        self.assertEqual(len(r["opinions"]), 1)
        self.assertEqual(r["opinions"][0]["audit_fee_okwon"], 0)

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_all_endpoints_fail_returns_empty(self, mock_retry):
        mock_retry.side_effect = Exception("network error")
        r = dart_client.fetch_audit_opinion_history("00000001", "KEY", 5)
        self.assertEqual(r["opinions"], [])
        self.assertEqual(r["auditor_changes"], [])
        self.assertEqual(r["independence_warnings"], [])

    def test_rejects_empty_corp_code(self):
        r = dart_client.fetch_audit_opinion_history("", "KEY", 5)
        self.assertEqual(r, {"opinions": [], "auditor_changes": [], "independence_warnings": []})

    def test_rejects_empty_api_key(self):
        r = dart_client.fetch_audit_opinion_history("00000001", "", 5)
        self.assertEqual(r["opinions"], [])

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_cache_hit_skips_network(self, mock_retry):
        mock_retry.side_effect = [
            _resp(lst=[{"bsns_year": "2025", "adtor": "삼일", "adt_opinion": "적정"}]),
            _resp(lst=[]),
            _resp(lst=[]),
        ]
        dart_client.fetch_audit_opinion_history("00000099", "KEY", 5)
        dart_client.fetch_audit_opinion_history("00000099", "KEY", 5)  # 캐시 히트
        self.assertEqual(mock_retry.call_count, 3)  # 두 번째 호출은 캐시에서 반환


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 9: 테스트 실행 — 10개 PASS 확인**

```
python -m pytest tests/test_audit_opinion_history.py -v
```
Expected: 10 passed.

- [ ] **Step 10: 커밋**

```bash
git add tests/test_audit_opinion_history.py dart_risk_mcp/core/dart_client.py dart_risk_mcp/core/__init__.py
git commit -m "feat(v0.8.0): fetch_audit_opinion_history — 감사의견·교체·비감사용역 구조화"
```

---

## Task 3: `get_audit_opinion_history` MCP 도구

**Files:**
- Modify: `dart_risk_mcp/server.py` (신규 도구 정의)

- [ ] **Step 1: 도구 함수를 `server.py`에 추가**

`server.py`의 `check_disclosure_anomaly` 정의 **바로 앞**(1895줄 근처, 기존 `@mcp.tool()` 흐름 따라)에 추가:

```python
@mcp.tool()
def get_audit_opinion_history(company_name: str, lookback_years: int = 5) -> str:
    """
    감사의견·감사인 교체·비감사용역 이력을 조회합니다.

    Args:
        company_name: 기업명 또는 종목코드(6자리).
        lookback_years: 1~10(밖이면 5로 강제).

    Returns:
        연도별 감사의견·감사인 테이블 + 교체 이력 + 독립성 경고 텍스트.
    """
    api_key = os.environ.get("DART_API_KEY", "")
    if not api_key:
        return "❌ DART_API_KEY 환경변수가 설정되지 않았습니다."

    if not isinstance(lookback_years, int) or not (1 <= lookback_years <= 10):
        lookback_years = 5

    corp_info = resolve_corp(company_name, api_key)
    if not corp_info[1]:
        return f"❌ 기업 '{company_name}'을(를) 찾을 수 없습니다."
    corp_name, info = corp_info

    data = fetch_audit_opinion_history(info["corp_code"], api_key, lookback_years)

    if not data["opinions"]:
        return (
            f"📋 **{corp_name}** ({info.get('stock_code','')}) — 감사의견 이력\n\n"
            f"최근 {lookback_years}년 감사의견 공시를 찾지 못했습니다. "
            "비상장·소규모 기업이거나 DART 보고서 제출 의무가 없는 경우일 수 있습니다."
        )

    lines = [
        f"📋 **{corp_name}** ({info.get('stock_code','')}) — 감사의견 이력 (최근 {lookback_years}년)",
        "",
        "**연도별 감사의견**",
    ]
    for o in data["opinions"]:
        warn = ""
        if o["opinion"] in ("한정", "부적정", "의견거절"):
            warn = f" ⚠ {o['opinion']}"
        elif o["opinion"] and o["opinion"] != "적정":
            warn = f" ({o['opinion']})"
        fee_parts = []
        if o["audit_fee_okwon"]:
            fee_parts.append(f"보수 {o['audit_fee_okwon']//100_000_000}억")
        if o["non_audit_fee_okwon"]:
            fee_parts.append(f"비감사 {o['non_audit_fee_okwon']//100_000_000}억")
        fee_str = f" · {' / '.join(fee_parts)}" if fee_parts else ""
        lines.append(
            f"- {o['year']}: {o['auditor'] or '미확인'} "
            f"(연속 {o['tenure_years']}년차){fee_str}{warn}"
        )
    lines.append("")

    if data["auditor_changes"]:
        lines.append("**감사인 교체 이력**")
        for c in data["auditor_changes"]:
            lines.append(f"- {c['from_year']}→{c['to_year']}: {c['from']} → {c['to']}")
        if len(data["auditor_changes"]) >= 2:
            lines.append("  ⚠ 3년 내 2회 이상 교체는 감사 독립성 경고 신호입니다.")
        lines.append("")

    if data["independence_warnings"]:
        lines.append("**독립성 경고**")
        for w in data["independence_warnings"]:
            lines.append(f"- ⚠ {w}")
        lines.append(
            "  비감사용역(세무·자문 등) 보수가 감사보수의 30% 이상이면 "
            "외감법상 독립성 훼손 우려가 제기됩니다."
        )
        lines.append("")

    lines.append(
        "📎 참고: DART 사업보고서 기준 감사의견입니다. 반기·분기 감사인 리뷰 "
        "의견은 별도 공시로 조회하세요."
    )
    return "\n".join(lines)
```

- [ ] **Step 2: `fetch_audit_opinion_history` import 추가**

`server.py` 상단 import 블록에서 `from .core import (...)` 블록에 `fetch_audit_opinion_history` 추가.

- [ ] **Step 3: 서버 import + 도구 개수 검증**

```
python -c "from dart_risk_mcp.server import mcp; names = [t.name for t in mcp._tool_manager.list_tools()]; print(len(names), 'get_audit_opinion_history' in names)"
```
Expected: `22 True`

- [ ] **Step 4: 실 API 스모크 테스트 (선택)**

```
python -c "
import os
from dart_risk_mcp.server import get_audit_opinion_history
print(get_audit_opinion_history('셀트리온', 5))
" 2>&1 | head -40
```
Expected: 감사의견 표가 한글 서술로 출력되고 `AR_SURGE`·`OCF` 같은 영문 내부 토큰이 없어야 함.

- [ ] **Step 5: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "feat(v0.8.0): get_audit_opinion_history MCP 도구"
```

---

## Task 4: `check_disclosure_anomaly` 지표 ② 보강

**Files:**
- Modify: `dart_risk_mcp/server.py` (지표 ② 블록 교체)
- Modify: `tests/test_financial_anomaly.py` (기존 assert 갱신)

- [ ] **Step 1: 기존 테스트 확인**

```
python -m pytest tests/test_financial_anomaly.py -v
```
기존 테스트가 몇 개인지·모두 PASS인지 확인. 실패/갱신 대상 항목을 노트에 적는다.

- [ ] **Step 2: `server.py`의 지표 ② 블록 교체**

`server.py` 1900~1960줄 근처의 `audit_hits` 집계 블록을 찾아, 다음과 같이 `fetch_audit_opinion_history` 호출을 **추가**(기존 키워드 매칭은 fallback으로 유지):

기존 `s_audit = min(20, len(audit_hits) * 20)` 줄 **다음에** 삽입:

```python
    # v0.8.0: 구조화 엔드포인트 보강 (실패 시 키워드 매칭 점수 유지)
    _audit_struct = fetch_audit_opinion_history(corp_code, api_key, 5)
    _audit_bonus = 0
    _auditor_change_count = len(_audit_struct.get("auditor_changes", []))
    _indep_warnings = _audit_struct.get("independence_warnings", [])
    if _auditor_change_count >= 2:
        _audit_bonus += 5
    if _indep_warnings:
        _audit_bonus += 3
    s_audit = min(20, s_audit + _audit_bonus)
```

그리고 `total_score = min(100, s_amend + s_audit + ...)` 계산은 이미 `s_audit`을 반영하므로 별도 수정 불필요.

- [ ] **Step 3: 지표 ② 출력 문구에 구조화 근거 추가**

`lines += [... "**② 감사의견 이슈 — ..."` 블록 바로 아래에, `if audit_hits: lines.append(_top3(audit_hits))` **다음 줄**에 추가:

```python
    if _auditor_change_count >= 2:
        lines.append(
            f"  ⚠ 최근 5년간 감사인 교체 {_auditor_change_count}회 "
            "— 감사 독립성 훼손 경고(+5점)."
        )
    if _indep_warnings:
        lines.append(
            f"  ⚠ 비감사용역 비중 초과 연도: {', '.join(_indep_warnings)} (+3점)."
        )
```

- [ ] **Step 4: `tests/test_financial_anomaly.py`에 구조화 보강 테스트 추가**

테스트 파일 상단에 import 보강:

```python
from unittest.mock import patch
```

`TestCheckDisclosureAnomaly` 클래스(존재하는 클래스 중 적합한 곳) 안에 추가:

```python
    @patch("dart_risk_mcp.server.fetch_audit_opinion_history")
    @patch("dart_risk_mcp.server.fetch_company_disclosures")
    @patch("dart_risk_mcp.server.resolve_corp")
    def test_audit_bonus_from_auditor_change(
        self, mock_resolve, mock_fetch, mock_audit,
    ):
        mock_resolve.return_value = ("테스트", {"corp_code": "00000001", "stock_code": "000001"})
        mock_fetch.return_value = []
        mock_audit.return_value = {
            "opinions": [],
            "auditor_changes": [
                {"from_year": 2023, "to_year": 2024, "from": "A", "to": "B"},
                {"from_year": 2024, "to_year": 2025, "from": "B", "to": "C"},
            ],
            "independence_warnings": [],
        }
        import os
        os.environ["DART_API_KEY"] = "TESTKEY"
        from dart_risk_mcp.server import check_disclosure_anomaly
        out = check_disclosure_anomaly("테스트", 365)
        self.assertIn("감사인 교체 2회", out)
        self.assertIn("+5점", out)

    @patch("dart_risk_mcp.server.fetch_audit_opinion_history")
    @patch("dart_risk_mcp.server.fetch_company_disclosures")
    @patch("dart_risk_mcp.server.resolve_corp")
    def test_audit_bonus_from_non_audit_warning(
        self, mock_resolve, mock_fetch, mock_audit,
    ):
        mock_resolve.return_value = ("테스트", {"corp_code": "00000001", "stock_code": "000001"})
        mock_fetch.return_value = []
        mock_audit.return_value = {
            "opinions": [],
            "auditor_changes": [],
            "independence_warnings": ["2023 비감사용역 비중 32%"],
        }
        import os
        os.environ["DART_API_KEY"] = "TESTKEY"
        from dart_risk_mcp.server import check_disclosure_anomaly
        out = check_disclosure_anomaly("테스트", 365)
        self.assertIn("+3점", out)
```

- [ ] **Step 5: 테스트 실행 — PASS**

```
python -m pytest tests/test_financial_anomaly.py -v
```
Expected: 기존 테스트 PASS + 신규 2개 PASS.

- [ ] **Step 6: 커밋**

```bash
git add dart_risk_mcp/server.py tests/test_financial_anomaly.py
git commit -m "feat(v0.8.0): check_disclosure_anomaly 지표 ② 구조화 보강"
```

---

---

# 섹션 C — CB_ROLLOVER 미상환 잔액 시계열

## Task 5: `fetch_debt_balance` 핵심 함수 (TDD)

**Files:**
- Create: `tests/test_debt_balance.py`
- Modify: `dart_risk_mcp/core/dart_client.py` (URL 매핑 + 캐시 + 함수)
- Modify: `dart_risk_mcp/core/__init__.py` (export)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_debt_balance.py` 생성:

```python
"""fetch_debt_balance / detect_debt_rollover 파싱·경계·부분실패 검증."""
import unittest
from unittest.mock import patch, MagicMock

from dart_risk_mcp.core import dart_client


def _resp(status="000", lst=None):
    r = MagicMock()
    r.json.return_value = {
        "status": status,
        "message": "정상" if status == "000" else "데이터 없음",
        "list": lst or [],
    }
    return r


class TestFetchDebtBalance(unittest.TestCase):
    def setUp(self):
        dart_client._debt_balance_cache.clear()

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_sums_all_five_endpoints(self, mock_retry):
        # 5개 엔드포인트를 순서대로 반환
        mock_retry.side_effect = [
            _resp(lst=[{"remndr_amount": "56700000000", "remndr_within1y_amount": "8900000000"}]),   # 회사채
            _resp(lst=[{"remndr_amount": "2300000000"}]),                                            # 단기사채
            _resp(lst=[{"remndr_amount": "0"}]),                                                     # 기업어음
            _resp(lst=[{"remndr_amount": "13000000000"}]),                                           # 신종자본
            _resp(lst=[{"remndr_amount": "0"}]),                                                     # 조건부자본
        ]
        r = dart_client.fetch_debt_balance("00000001", "KEY", "2025")
        self.assertEqual(r["total"], 72_000_000_000)
        self.assertIn("corporate_bond", r["by_kind"])
        self.assertEqual(r["by_kind"]["corporate_bond"]["total"], 56_700_000_000)

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_defaults_to_previous_year_when_no_year(self, mock_retry):
        mock_retry.side_effect = [_resp(lst=[])] * 5
        r = dart_client.fetch_debt_balance("00000001", "KEY", "")
        self.assertIsNotNone(r["year"])

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_partial_failure(self, mock_retry):
        mock_retry.side_effect = [
            _resp(lst=[{"remndr_amount": "10000000000"}]),
            Exception("network"),
            _resp(lst=[]),
            _resp(lst=[]),
            _resp(lst=[]),
        ]
        r = dart_client.fetch_debt_balance("00000001", "KEY", "2025")
        self.assertEqual(r["total"], 10_000_000_000)

    def test_rejects_empty_corp_code(self):
        r = dart_client.fetch_debt_balance("", "KEY", "2025")
        self.assertEqual(r["total"], 0)

    def test_rejects_empty_api_key(self):
        r = dart_client.fetch_debt_balance("00000001", "", "2025")
        self.assertEqual(r["total"], 0)

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_cache_hit_skips_network(self, mock_retry):
        mock_retry.side_effect = [_resp(lst=[])] * 5
        dart_client.fetch_debt_balance("00000099", "KEY", "2025")
        dart_client.fetch_debt_balance("00000099", "KEY", "2025")
        self.assertEqual(mock_retry.call_count, 5)

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_maturity_1y_share_computed(self, mock_retry):
        mock_retry.side_effect = [
            _resp(lst=[{"remndr_amount": "10000000000", "remndr_within1y_amount": "3000000000"}]),
            _resp(lst=[]), _resp(lst=[]), _resp(lst=[]), _resp(lst=[]),
        ]
        r = dart_client.fetch_debt_balance("00000001", "KEY", "2025")
        self.assertAlmostEqual(r["maturity_1y_share"], 0.30, places=2)


class TestDetectDebtRollover(unittest.TestCase):
    def test_flag_when_flat_balance_and_cb_issues(self):
        balances = [(2023, 50_000_000_000), (2024, 52_000_000_000), (2025, 51_500_000_000)]
        events = [
            {"key": "CB_BW", "rcept_dt": "2023-08-01"},
            {"key": "CB_BW", "rcept_dt": "2024-09-15"},
            {"key": "CB_BW", "rcept_dt": "2025-03-20"},
        ]
        self.assertEqual(dart_client.detect_debt_rollover(balances, events), "CB_ROLLOVER")

    def test_no_flag_when_balance_drops(self):
        balances = [(2023, 50_000_000_000), (2024, 30_000_000_000), (2025, 10_000_000_000)]
        events = [
            {"key": "CB_BW", "rcept_dt": "2023-08-01"},
            {"key": "CB_BW", "rcept_dt": "2024-09-15"},
        ]
        self.assertIsNone(dart_client.detect_debt_rollover(balances, events))

    def test_no_flag_when_fewer_than_two_cb_issues(self):
        balances = [(2023, 50_000_000_000), (2024, 52_000_000_000), (2025, 51_500_000_000)]
        events = [{"key": "CB_BW", "rcept_dt": "2024-09-15"}]
        self.assertIsNone(dart_client.detect_debt_rollover(balances, events))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```
python -m pytest tests/test_debt_balance.py -v
```
Expected: FAIL — `_debt_balance_cache` 또는 `fetch_debt_balance` 없음.

- [ ] **Step 3: `dart_client.py`에 URL 매핑 + 캐시 추가**

Task 2에서 추가한 `_audit_history_cache` 블록 **다음 줄**에:

```python
# v0.8.0: 미상환 채무증권 잔액 엔드포인트 5종 ------------------------
_DEBT_BALANCE_URLS = {
    "corporate_bond":  f"{DART_BASE}/cprndNrdmpBlce.json",              # 회사채
    "short_term_bond": f"{DART_BASE}/srtpdPsndbtNrdmpBlce.json",        # 단기사채
    "commercial_paper": f"{DART_BASE}/entrprsBilScritsNrdmpBlce.json",  # 기업어음
    "new_capital":     f"{DART_BASE}/newCaplScritsNrdmpBlce.json",      # 신종자본증권
    "cnd_capital":     f"{DART_BASE}/cndlCaplScritsNrdmpBlce.json",     # 조건부자본증권
}

_debt_balance_cache: dict[tuple, tuple[float, dict]] = {}
_DEBT_CACHE_MAX = 20
_DEBT_CACHE_TTL = 600  # 10분

_CB_ROLLOVER_YOY_THRESHOLD = 0.10  # 10% 이내 변동 = 평탄
_CB_ROLLOVER_YEARS_REQUIRED = 3    # 3년 연속
_CB_ROLLOVER_EVENTS_REQUIRED = 2   # 같은 기간 CB/BW 발행 2건 이상
```

- [ ] **Step 4: `fetch_debt_balance` 함수 추가**

Task 2의 `fetch_audit_opinion_history` **다음 위치**에:

```python
def fetch_debt_balance(
    corp_code: str,
    api_key: str,
    year: str = "",
) -> dict:
    """채무증권 5종 미상환 잔액 통합.

    반환:
        {
            "year": int,
            "by_kind": {"corporate_bond": {"total": int, "maturity_under_1y": int},
                        "short_term_bond": {...}, ...},
            "total": int,
            "maturity_1y_share": float,
            "equity_ratio": float | None,  # 자기자본 대비(재무제표 있으면)
        }
    """
    empty = {"year": None, "by_kind": {}, "total": 0,
             "maturity_1y_share": 0.0, "equity_ratio": None}
    if not corp_code or not api_key:
        return empty

    # 연도 없으면 직전 사업연도
    if not year:
        year = str(_dt.datetime.now().year - 1)

    cache_key = (corp_code, year)
    cached = _cache_get(_debt_balance_cache, cache_key, _DEBT_CACHE_TTL)
    if cached is not None:
        return cached

    by_kind: dict[str, dict] = {}
    total = 0
    maturity_1y = 0

    for kind, url in _DEBT_BALANCE_URLS.items():
        try:
            resp = _retry("GET", url, params={
                "crtfc_key": api_key,
                "corp_code": corp_code,
                "bsns_year": year,
                "reprt_code": "11011",  # 사업보고서
            }, timeout=15)
            data = resp.json() if resp is not None else {}
        except Exception:
            continue
        if data.get("status") != "000":
            continue

        kind_total = 0
        kind_1y = 0
        for item in data.get("list", []):
            amt = _safe_int(item.get("remndr_amount")) or 0
            kind_total += amt
            within = _safe_int(item.get("remndr_within1y_amount")) or 0
            kind_1y += within

        if kind_total > 0:
            by_kind[kind] = {"total": kind_total, "maturity_under_1y": kind_1y}
            total += kind_total
            maturity_1y += kind_1y

    maturity_share = maturity_1y / total if total > 0 else 0.0

    result = {
        "year": _safe_int(year),
        "by_kind": by_kind,
        "total": total,
        "maturity_1y_share": maturity_share,
        "equity_ratio": None,  # 재무제표 연동은 server 레이어에서 주입
    }
    _cache_set(_debt_balance_cache, cache_key, result, _DEBT_CACHE_MAX)
    return result


def detect_debt_rollover(
    balances: list[tuple[int, int]],
    events: list[dict],
) -> str | None:
    """3년 연속 잔액 YoY <10% AND 같은 기간 CB/BW 발행 ≥2건 → 'CB_ROLLOVER'.

    Args:
        balances: [(year, total_balance), ...] 최소 3개.
        events: 자본 이벤트 목록(key·rcept_dt 필드 필수).
    """
    if len(balances) < _CB_ROLLOVER_YEARS_REQUIRED:
        return None

    # 최신 3개 연도만
    asc = sorted(balances, key=lambda x: x[0])
    recent = asc[-_CB_ROLLOVER_YEARS_REQUIRED:]

    # 연속 YoY 변동이 모두 임계값 이내
    for i in range(1, len(recent)):
        prev = recent[i-1][1]
        cur = recent[i][1]
        if prev <= 0:
            return None
        yoy = abs(cur - prev) / prev
        if yoy >= _CB_ROLLOVER_YOY_THRESHOLD:
            return None

    # 같은 기간 CB/BW 발행 이벤트 수
    start_year = recent[0][0]
    cb_events = [
        e for e in events
        if e.get("key") == "CB_BW"
        and _safe_int_from_de(e.get("rcept_dt", "")) is not None
        and _safe_int_from_de(e.get("rcept_dt", "")) >= start_year
    ]
    if len(cb_events) < _CB_ROLLOVER_EVENTS_REQUIRED:
        return None

    return "CB_ROLLOVER"
```

- [ ] **Step 5: `core/__init__.py` export 추가**

```python
from .dart_client import (
    # ... 기존 ...
    fetch_debt_balance,
    detect_debt_rollover,
)

__all__ = [
    # ... 기존 ...
    "fetch_debt_balance",
    "detect_debt_rollover",
]
```

- [ ] **Step 6: 테스트 실행 — 10개 PASS 확인**

```
python -m pytest tests/test_debt_balance.py -v
```
Expected: 10 passed.

- [ ] **Step 7: 커밋**

```bash
git add tests/test_debt_balance.py dart_risk_mcp/core/dart_client.py dart_risk_mcp/core/__init__.py
git commit -m "feat(v0.8.0): fetch_debt_balance + detect_debt_rollover"
```

---

## Task 6: `track_debt_balance` MCP 도구

**Files:**
- Modify: `dart_risk_mcp/server.py`

- [ ] **Step 1: 도구 함수 추가**

`server.py`의 `track_capital_structure` 정의 **바로 위**에 추가:

```python
@mcp.tool()
def track_debt_balance(company_name: str, year: str = "") -> str:
    """
    미상환 채무증권(회사채·단기사채·어음·신종/조건부자본) 잔액과 만기 구간을 조회합니다.

    Args:
        company_name: 기업명 또는 종목코드(6자리).
        year: 사업연도(YYYY). 빈 문자열이면 직전 연도.

    Returns:
        채무증권 종류별 잔액·만기 구간·위험 경고 텍스트.
    """
    api_key = os.environ.get("DART_API_KEY", "")
    if not api_key:
        return "❌ DART_API_KEY 환경변수가 설정되지 않았습니다."

    corp_info = resolve_corp(company_name, api_key)
    if not corp_info[1]:
        return f"❌ 기업 '{company_name}'을(를) 찾을 수 없습니다."
    corp_name, info = corp_info
    corp_code = info["corp_code"]

    data = fetch_debt_balance(corp_code, api_key, year)

    if data["total"] == 0:
        return (
            f"💰 **{corp_name}** ({info.get('stock_code','')}) — "
            f"{data['year']}년 미상환 채무증권\n\n"
            "조회된 잔액이 없습니다. 채권 미발행 또는 전액 상환 상태일 수 있습니다."
        )

    # 자기자본 조회(실패해도 계속)
    equity = None
    try:
        fs = fetch_financial_statements(corp_code, api_key, str(data["year"]), "annual")
        for item in fs.get("list", []):
            if item.get("account_nm") == "자본총계" and item.get("fs_div") == "CFS":
                equity = _safe_money(item.get("thstrm_amount"))
                break
    except Exception:
        pass

    equity_ratio = (data["total"] / equity) if equity and equity > 0 else None

    _LABELS = {
        "corporate_bond":   "회사채",
        "short_term_bond":  "단기사채",
        "commercial_paper": "기업어음",
        "new_capital":      "신종자본증권",
        "cnd_capital":      "조건부자본증권",
    }

    lines = [
        f"💰 **{corp_name}** ({info.get('stock_code','')}) — "
        f"{data['year']}년 미상환 채무증권",
        "",
        f"**총 미상환: {data['total']//100_000_000}억원**"
        + (f" · 자기자본 대비 **{int(equity_ratio*100)}%**" if equity_ratio else "")
        + f" · 1년 내 만기 **{int(data['maturity_1y_share']*100)}%**",
        "",
        "**종류별 잔액**",
    ]
    for kind in ["corporate_bond", "short_term_bond", "commercial_paper",
                 "new_capital", "cnd_capital"]:
        if kind in data["by_kind"] and data["by_kind"][kind]["total"] > 0:
            info_k = data["by_kind"][kind]
            line = f"- {_LABELS[kind]}: {info_k['total']//100_000_000}억원"
            if info_k["maturity_under_1y"] > 0:
                line += f" (1년 내 {info_k['maturity_under_1y']//100_000_000}억)"
            lines.append(line)
    lines.append("")

    warnings: list[str] = []
    if equity_ratio and equity_ratio > 0.50:
        warnings.append(
            f"⚠ 자기자본 대비 채무증권 {int(equity_ratio*100)}% — "
            "50% 초과는 차환(rollover) 위험이 체감되는 수준."
        )
    if data["maturity_1y_share"] > 0.30:
        warnings.append(
            f"⚠ 1년 내 만기 비중 {int(data['maturity_1y_share']*100)}% — "
            "단기 유동성 리스크."
        )
    if warnings:
        lines.append("**위험 경고**")
        lines.extend(warnings)
        lines.append("")

    lines.append(
        "📎 참고: 사업보고서 기준 잔액입니다. 반기·분기 중 발행·상환은 "
        "`track_capital_structure`로 이벤트 타임라인을 함께 확인하세요."
    )
    return "\n".join(lines)
```

- [ ] **Step 2: import 추가**

`server.py` 상단 import에 `fetch_debt_balance`, `fetch_financial_statements`(이미 있으면 생략) 추가.

- [ ] **Step 3: `_safe_money` 유틸 확인**

`server.py`에 `_safe_money` 헬퍼가 이미 있는지 확인:
```
grep -n "_safe_money" dart_risk_mcp/server.py
```
없으면 다음을 파일 상단 상수 근처에 추가:
```python
def _safe_money(v) -> int | None:
    if v is None:
        return None
    s = str(v).replace(",", "").strip()
    try:
        return int(float(s))
    except (ValueError, TypeError):
        return None
```

- [ ] **Step 4: 도구 개수 검증**

```
python -c "from dart_risk_mcp.server import mcp; names=[t.name for t in mcp._tool_manager.list_tools()]; print(len(names), 'track_debt_balance' in names)"
```
Expected: `23 True`

- [ ] **Step 5: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "feat(v0.8.0): track_debt_balance MCP 도구"
```

---

## Task 7: `track_capital_structure` 잔액 블록 + CB_ROLLOVER 고도화

**Files:**
- Modify: `dart_risk_mcp/server.py` (`track_capital_structure` 함수 보강)

- [ ] **Step 1: `track_capital_structure` 맨 끝(`📎 참고:` 줄 바로 전)에 블록 추가**

server.py 2455줄 근처, `lines.append("📎 참고: 이 도구는 공시 '횟수·리듬'..."` 바로 **앞에** 삽입:

```python
    # v0.8.0: 미상환 채무증권 잔액 블록 + CB_ROLLOVER 판정
    _last_year = _dt.datetime.now().year - 1
    _balances: list[tuple[int, int]] = []
    _latest_bal = None
    for _yr in range(_last_year - 2, _last_year + 1):
        _bal = fetch_debt_balance(corp_code, api_key, str(_yr))
        if _bal["total"] > 0:
            _balances.append((_yr, _bal["total"]))
            _latest_bal = _bal  # 가장 최근 연도 raw dict 보관

    if _balances and _latest_bal is not None:
        lines.append("━━ 현재 미상환 채무증권 ━━")
        _ly, _lv = _balances[-1]
        _maturity_pct = int(_latest_bal["maturity_1y_share"] * 100)
        lines.append(
            f"{_ly}년 총 **{_lv//100_000_000}억원** · 1년 내 만기 {_maturity_pct}%"
        )
        if len(_balances) >= 2:
            _yoy_pieces = [f"{y}→{b//100_000_000}억" for y, b in _balances]
            lines.append(f"3개년 추이: {' / '.join(_yoy_pieces)}")
        lines.append("")

    # CB_ROLLOVER 판정(구조화 기반)
    _rollover_flag = detect_debt_rollover(_balances, signal_events)
    if _rollover_flag == "CB_ROLLOVER":
        lines.append("**차환(CB_ROLLOVER) 탐지**")
        lines.append(
            "3년 연속 채무 잔액이 ±10% 이내로 평탄한 가운데 같은 기간 CB/BW "
            "신규 발행이 2건 이상 있었습니다. 사업 현금흐름으로 상환이 아니라 "
            "새 채권으로 기존 채권을 돌려막는 패턴일 수 있습니다."
        )
        lines.append("")
```

- [ ] **Step 2: import 추가**

`server.py` 상단에 `detect_debt_rollover`, `import datetime as _dt`(이미 있으면 생략) 추가.

- [ ] **Step 3: 스모크 테스트 (선택, API 키 있을 때)**

```
python -c "
import os
from dart_risk_mcp.server import track_capital_structure
print(track_capital_structure('제이스코홀딩스', 3))
" 2>&1 | tail -30
```
Expected: 기존 자본 이벤트 목록 뒤에 `━━ 현재 미상환 채무증권 ━━` 블록이 보이고 3개년 추이 라인이 있어야 함.

- [ ] **Step 4: 전체 테스트 스위트 실행**

```
python -m pytest tests/ -v
```
Expected: 101 passed (81 기존 + 20 신규).

- [ ] **Step 5: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "feat(v0.8.0): track_capital_structure 잔액 블록 + CB_ROLLOVER 고도화"
```

---

---

# 섹션 D — 릴리스

## Task 8: 골드 파일 재생성 + 회귀 테스트

**Files:**
- Modify: `tmp/v072_review/regen_fixtures.py` (신규 2개 도구 추가)
- Modify: `tests/fixtures/sample_outputs/*.txt` (전체 재생성)

- [ ] **Step 1: `tmp/v072_review/regen_fixtures.py` 확인**

```
cat tmp/v072_review/regen_fixtures.py
```
스크립트가 있는 경우 신규 2개 도구(`get_audit_opinion_history`, `track_debt_balance`)를 호출하는 블록 추가. 없으면 다음 스크립트를 `tmp/v072_review/regen_fixtures.py`에 작성:

```python
"""v0.8.0 골드 파일 재수집 스크립트. DART_API_KEY 필요."""
import os
from pathlib import Path
from dart_risk_mcp.server import (
    analyze_company_risk, build_event_timeline, scan_financial_anomaly,
    list_disclosures_by_stock, check_disclosure_risk, find_risk_precedents,
    find_actor_overlap, get_audit_opinion_history, track_debt_balance,
)

OUT = Path(__file__).resolve().parents[2] / "tests" / "fixtures" / "sample_outputs"
OUT.mkdir(parents=True, exist_ok=True)

for name in ["셀트리온", "제이스코홀딩스", "두산에너빌리티"]:
    (OUT / f"{name}_analyze.txt").write_text(analyze_company_risk(name, 180), encoding="utf-8")
    (OUT / f"{name}_timeline.txt").write_text(build_event_timeline(name, 365), encoding="utf-8")
    (OUT / f"{name}_scan_fs.txt").write_text(scan_financial_anomaly(name, "", "annual"), encoding="utf-8")

(OUT / "audit_history.txt").write_text(get_audit_opinion_history("셀트리온", 5), encoding="utf-8")
(OUT / "debt_balance.txt").write_text(track_debt_balance("두산에너빌리티", ""), encoding="utf-8")

print("done.")
```

- [ ] **Step 2: 스크립트 실행**

```
DART_API_KEY=<키> python tmp/v072_review/regen_fixtures.py
```
Expected: `done.` 출력, `tests/fixtures/sample_outputs/` 15개 파일 갱신.

- [ ] **Step 3: 회귀 테스트 실행**

```
python -m pytest tests/test_golden_output_hygiene.py -v
```
Expected: 4 passed. (내부 flag 코드·영문 약어·카탈로그 영문 메타 0건)

- [ ] **Step 4: 커밋**

```bash
git add tests/fixtures/sample_outputs/ tmp/v072_review/regen_fixtures.py
git commit -m "test(v0.8.0): 골드 파일 15개 재생성(+audit_history, debt_balance)"
```

---

## Task 9: 버전 + CHANGELOG + README + CLAUDE.md

**Files:**
- Modify: `dart_risk_mcp/__init__.py`
- Modify: `pyproject.toml`
- Modify: `CHANGELOG.md`
- Modify: `README.md`
- Modify: `CLAUDE.md`

- [ ] **Step 1: 버전 2곳 업데이트**

`dart_risk_mcp/__init__.py`:
```python
__version__ = "0.8.0"
```

`pyproject.toml`:
```toml
version = "0.8.0"
```

- [ ] **Step 2: `CHANGELOG.md` v0.8.0 섹션 완성**

Task 1에서 넣은 플레이스홀더 블록을 다음과 같이 확장:

```markdown
## [0.8.0] — 2026-04-25

### Design Principle (확정)
- **단일 출력 원칙 확정** — v0.7.x 동안 모든 도구를 하나의 한국어 서술 출력로 통일했습니다. v0.8.0에서 expert/easy 모드 분기 가능성을 공식 폐기합니다. 향후 어떤 도구도 `level=`·`mode=`·`format=` 같은 분기 파라미터를 받지 않습니다. 사용자가 더 자세한 원시 데이터를 원하면 개별 도구(`get_disclosure_document`, `view_disclosure`, `list_disclosure_sections`)를 조합해 파이프라인을 구성합니다.

### Added
- `get_audit_opinion_history(company_name, lookback_years=5)` — 감사의견·감사인 교체·비감사용역 보수를 DART 구조화 엔드포인트 3종(`accnutAdtorNmNdAdtOpinion`, `adtServcCnclsSttus`, `accnutAdtorNonAdtServcCnclsSttus`)에서 조회해 한글 서술로 반환.
- `track_debt_balance(company_name, year="")` — 미상환 채무증권 5종(회사채·단기사채·어음·신종/조건부자본) 잔액과 만기 구간을 조회. 자기자본 대비 비율, 1년 내 만기 비중 경고 포함.
- `dart_client.detect_debt_rollover(balances, events)` — 3년 연속 잔액 변동 <10% AND CB/BW 발행 ≥2건 조건으로 `CB_ROLLOVER` 플래그 판정.

### Changed
- `check_disclosure_anomaly` 지표 ② 감사의견 이슈 — 구조화 보강. 감사인 교체 3년 내 2회 이상 +5점, 비감사용역 비중 >30% +3점. 신규 엔드포인트 실패 시 기존 키워드 매칭 fallback 유지.
- `track_capital_structure` — 자본 이벤트 목록 뒤에 `━━ 현재 미상환 채무증권 ━━` 블록 추가(3개년 추이). `detect_debt_rollover`로 CB_ROLLOVER 탐지 정확도 상승.
- 테스트 스위트 81 → 101 (신규 20건: audit 10 + debt 10).
- 도구 개수 21 → 23.

### Out of Scope (v0.8.1+)
- 🟡 #4 INSIDER 대량보유 변경 이벤트
- 🟡 #5 CAPITAL_CHURN 증자/감자/자사주 결정 구조화
- 🟡 #6 재무이상 기업 지표(`fnlttSinglIndx`, `fnlttCmpnyIndx`)
- **Track C 비상장사 감사보고서 정량 추출 — 영구 폐기.**
```

- [ ] **Step 3: `README.md` 업데이트**

`README.md`에서:
1. 버전 헤더 `v0.7.5` → `v0.8.0`
2. 상단 "출력 원칙" 블록(Task 1에서 추가됨) 유지
3. "주요 변경" 섹션을 `### v0.8.0 주요 변경`으로 갱신:
   ```markdown
   ### v0.8.0 주요 변경 (2026-04-25)

   - **신규 도구** `get_audit_opinion_history` · `track_debt_balance` — DART 구조화 엔드포인트 8종 통합으로 감사의견·채무증권 잔액 시계열 조회.
   - **보강** `check_disclosure_anomaly` 지표 ② — 감사인 교체·비감사용역 비중 구조화 점수 추가.
   - **보강** `track_capital_structure` — 미상환 채무증권 3개년 추이 블록 + CB_ROLLOVER 탐지 정확도 상승.
   - **원칙 확정** — 단일 출력 원칙 공식 폐기 반대(expert mode 분기 없음).
   ```
4. 기존 v0.7.5 요약은 `### 최근 릴리스 요약` 섹션으로 푸시.
5. 도구 개수 표기 `21` → `23`.

- [ ] **Step 4: `CLAUDE.md`에 도구 22·23 섹션 추가**

`CLAUDE.md`의 "MCP 도구 21개" 헤더를 "MCP 도구 23개"로 바꾸고, 도구 21(`scan_financial_anomaly`) **다음 위치**에 추가:

````markdown
### 22. `get_audit_opinion_history(company_name, lookback_years=5)` ✨

감사의견·감사인 교체·비감사용역 보수 이력을 구조화 조회합니다.

- 내부 흐름: `resolve_corp` → `fetch_audit_opinion_history` (3개 엔드포인트 통합)
- 엔드포인트: `accnutAdtorNmNdAdtOpinion`(감사의견), `adtServcCnclsSttus`(감사용역), `accnutAdtorNonAdtServcCnclsSttus`(비감사용역)
- 경고 조건: 한정/부적정/의견거절 ⚠, 3년 내 감사인 교체 2회↑ ⚠, 비감사용역 비중 >30% ⚠
- `lookback_years` 범위: 1~10년(기본 5)

### 23. `track_debt_balance(company_name, year="")` ✨

미상환 채무증권 5종(회사채·단기사채·기업어음·신종/조건부자본) 잔액과 만기를 조회합니다.

- 내부 흐름: `resolve_corp` → `fetch_debt_balance` (5개 엔드포인트 통합) → 선택적으로 `fetch_financial_statements`로 자기자본 대비 비율
- 엔드포인트: `cprndNrdmpBlce`, `srtpdPsndbtNrdmpBlce`, `entrprsBilScritsNrdmpBlce`, `newCaplScritsNrdmpBlce`, `cndlCaplScritsNrdmpBlce`
- 경고 조건: 자기자본 대비 >50% ⚠, 1년 내 만기 비중 >30% ⚠
- `year` 빈 문자열이면 직전 사업연도
````

또한 "핵심 내부 함수 > dart_client.py" 테이블 하단에 추가:
```
| `fetch_audit_opinion_history(corp_code, api_key, lookback_years)` | 감사의견·교체·비감사용역 3개 엔드포인트 통합 |
| `fetch_debt_balance(corp_code, api_key, year)` | 미상환 채무증권 5종 잔액·만기 통합 |
| `detect_debt_rollover(balances, events)` | 3년 연속 평탄 + CB/BW 발행 ≥2건 → CB_ROLLOVER 플래그 |
```

- [ ] **Step 5: 최종 검증**

```
python -m pytest tests/ -v
```
Expected: 101 passed.

```
python -c "import dart_risk_mcp; print(dart_risk_mcp.__version__)"
```
Expected: `0.8.0`

```
python -c "from dart_risk_mcp.server import mcp; print(len(list(mcp._tool_manager.list_tools())))"
```
Expected: `23`

- [ ] **Step 6: 단일 커밋으로 묶기**

```bash
git add dart_risk_mcp/__init__.py pyproject.toml CHANGELOG.md README.md CLAUDE.md
git commit -m "chore(v0.8.0): 버전 0.7.5 → 0.8.0 + CHANGELOG/README/CLAUDE.md"
```

- [ ] **Step 7: 사용자 확인 후 푸시 (수동)**

```
git log --oneline -10
```
확인 후, 사용자가 "푸시해줘"라고 명시하면:
```
git push origin master
```

---

## 검증 체크리스트 (최종)

- [ ] `python -m pytest tests/ -v` → 101 passed
- [ ] `tests/fixtures/sample_outputs/` 15개 파일 갱신, `test_golden_output_hygiene.py` 4 passed
- [ ] `dart_risk_mcp.__version__ == "0.8.0"`
- [ ] 도구 개수 23
- [ ] 커밋 순서: Task 1(docs) → Task 2(audit core) → Task 3(audit tool) → Task 4(anomaly 보강) → Task 5(debt core) → Task 6(debt tool) → Task 7(capital 보강) → Task 8(fixtures) → Task 9(version)
- [ ] 내부 flag 코드(`AR_SURGE`, `CASH_GAP` 등) 사용자 출력에 노출 0건
- [ ] 영문 약어(`OCF `) 사용자 출력에 노출 0건

---

## 자체 검토 결과

- **Spec 커버리지:** spec 2.1 (Task 1) · 2.2 (Task 2~4) · 2.3 (Task 5~7) · 섹션 4 (Task 8~9) · 섹션 6 테스트 (Task 2, 5에 분산). 누락 없음.
- **타입 일관성:** `fetch_audit_opinion_history`, `fetch_debt_balance`, `detect_debt_rollover` 시그니처가 spec과 동일. 반환 dict 키(`opinions`, `auditor_changes`, `by_kind`, `total`) 일관.
- **임계값:** 비감사용역 30%, 감사인 교체 2회, 잔액 평탄 10%, CB 이벤트 2건 — 모두 상수(`_NON_AUDIT_THRESHOLD`, `_AUDITOR_CHANGE_WINDOW`, `_CB_ROLLOVER_YOY_THRESHOLD`, `_CB_ROLLOVER_EVENTS_REQUIRED`)로 정의.
- **Graceful degradation:** 모든 신규 함수가 `Exception` 포착 후 빈 값 반환, `check_disclosure_anomaly`는 구조화 실패 시 기존 키워드 매칭 점수 유지.
