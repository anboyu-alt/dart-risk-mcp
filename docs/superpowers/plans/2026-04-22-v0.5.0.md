# DART 리스크 분석 MCP — v0.5.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** P0 확장 — 신규 도구 2개(`track_fund_usage`, `get_major_decision`) + 기존 도구 3개 강화로 금감원 적발 패턴 `zombie_ma`·`fake_new_biz`·`related_party_hollowing`의 탐지 완성도 향상.

**Architecture:** DART OpenAPI의 공모/사모자금 사용내역(DS002 2020016/17) 2개 엔드포인트와 주요사항보고서 결정 공시(DS005 2020042~2020053) 12개 엔드포인트를 `dart_client.py`에 추가. 외부 라이브러리 추가 없이 기존 `_retry`·`_zip_cache` 패턴을 재사용하며 도구별 인메모리 LRU 캐시를 신설. 신규 5개 신호 키를 `signals.py`에 추가해 `SIGNAL_KEY_TO_TAXONOMY`로 기존 패턴 매칭에 편입.

**Tech Stack:** Python 3.11+, `requests`, `mcp` (모두 기존). 테스트는 표준 라이브러리 `unittest` + `unittest.mock.patch` (외부 의존성 추가 없음).

**사전 참고:** 스펙은 [docs/superpowers/specs/2026-04-22-v0.5.0-design.md](../specs/2026-04-22-v0.5.0-design.md), DART API 레퍼런스는 프로젝트 루트 [opendart_api_guide.md](../../../opendart_api_guide.md).

---

## File Structure

### 생성

| 경로 | 책임 |
|---|---|
| `tests/__init__.py` | 테스트 패키지 선언 (빈 파일) |
| `tests/test_signals_new.py` | 신규 5개 신호 키 존재·taxonomy 매핑 검증 |
| `tests/test_fund_usage.py` | `fetch_fund_usage` 정규화·이상 플래그·캐시·연도 부분실패 케이스 |
| `tests/test_major_decision.py` | `fetch_major_decision` 라우팅·플래그·에러 처리 |

### 수정

| 경로 | 변경 요지 |
|---|---|
| `dart_risk_mcp/__init__.py` | 버전 `0.4.0` → `0.5.0` |
| `dart_risk_mcp/core/signals.py` | 신규 5개 키(`FUND_DIVERSION`, `FUND_UNREPORTED`, `DECISION_RELATED_PARTY`, `DECISION_OVERSIZED`, `DECISION_NO_EXTVAL`) + `SIGNAL_KEY_TO_TAXONOMY` 매핑 |
| `dart_risk_mcp/core/dart_client.py` | `fetch_fund_usage`, `fetch_major_decision`, `resolve_decision_type`, 라우팅 테이블·LRU 캐시 추가 |
| `dart_risk_mcp/core/__init__.py` | 신규 함수 export 추가 |
| `dart_risk_mcp/server.py` | `track_fund_usage`·`get_major_decision` 도구 등록 + `check_disclosure_risk`·`analyze_company_risk`·`build_event_timeline` 강화 |
| `CHANGELOG.md` | v0.5.0 Added 섹션 |
| `CLAUDE.md` | 도구 목록·API 엔드포인트 테이블 갱신 |

**실행 명령 표기**: Windows bash 환경. 파이썬은 `python` (venv 아님), 테스트는 `python -m unittest tests.<module> -v`.

---

## Task 1: 신규 5개 신호 키 정의

**Files:**
- Create: `tests/__init__.py`, `tests/test_signals_new.py`
- Modify: `dart_risk_mcp/core/signals.py`

- [ ] **Step 1.1: tests/__init__.py 생성 (빈 파일)**

파일 생성:
```
(빈 파일)
```

- [ ] **Step 1.2: tests/test_signals_new.py 작성**

```python
"""v0.5.0 신규 신호 키 정의·매핑 검증."""
import unittest

from dart_risk_mcp.core.signals import (
    SIGNAL_TYPES,
    SIGNAL_KEY_TO_TAXONOMY,
)


NEW_KEYS = [
    "FUND_DIVERSION",
    "FUND_UNREPORTED",
    "DECISION_RELATED_PARTY",
    "DECISION_OVERSIZED",
    "DECISION_NO_EXTVAL",
]


class TestNewSignalKeys(unittest.TestCase):
    def test_all_new_keys_registered_in_signal_types(self):
        keys = {s["key"] for s in SIGNAL_TYPES}
        for k in NEW_KEYS:
            self.assertIn(k, keys, f"{k} missing from SIGNAL_TYPES")

    def test_all_new_keys_have_taxonomy_mapping(self):
        for k in NEW_KEYS:
            self.assertIn(k, SIGNAL_KEY_TO_TAXONOMY, f"{k} missing mapping")
            self.assertTrue(
                SIGNAL_KEY_TO_TAXONOMY[k],
                f"{k} mapping empty",
            )

    def test_signal_types_have_label_and_score(self):
        for s in SIGNAL_TYPES:
            if s["key"] in NEW_KEYS:
                self.assertIn("label", s)
                self.assertIn("score", s)
                self.assertIsInstance(s["score"], int)
                self.assertGreater(s["score"], 0)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 1.3: 테스트 실행 → FAIL 확인**

Run: `python -m unittest tests.test_signals_new -v`
Expected: 3개 테스트 모두 FAIL (키 미정의)

- [ ] **Step 1.4: signals.py에 신규 SIGNAL_TYPES 항목 추가**

`SIGNAL_TYPES` 리스트의 **끝**(마지막 `GOING_CONCERN` 항목 뒤, 닫는 `]` 앞)에 다음을 삽입:

```python
    # ── v0.5.0: 자금흐름·주요결정 (프로그램적 부착) ─────────────
    {
        "key":   "FUND_DIVERSION",
        "label": "조달자금 목적외 사용",
        "score": 4,
        "keywords": [],  # track_fund_usage 내부에서 부착
    },
    {
        "key":   "FUND_UNREPORTED",
        "label": "자금사용내역 미기재",
        "score": 3,
        "keywords": [],
    },
    {
        "key":   "DECISION_RELATED_PARTY",
        "label": "특수관계인 대상 결정",
        "score": 4,
        "keywords": [],
    },
    {
        "key":   "DECISION_OVERSIZED",
        "label": "자산대비 대형 결정",
        "score": 3,
        "keywords": [],
    },
    {
        "key":   "DECISION_NO_EXTVAL",
        "label": "외부평가 미실시 결정",
        "score": 3,
        "keywords": [],
    },
```

`keywords`가 빈 리스트인 이유: 이 신호들은 공시 제목 매칭이 아니라 `track_fund_usage`·`get_major_decision` 함수가 구조화 필드로 판정 후 **프로그램적으로 부착**한다. `match_signals(report_nm)`에서는 매칭되지 않는다(빈 리스트이므로 기존 `for kw in keywords` 루프에서 자연 스킵).

- [ ] **Step 1.5: SIGNAL_KEY_TO_TAXONOMY 매핑 추가**

`SIGNAL_KEY_TO_TAXONOMY` 딕셔너리의 `# 기존 호환 키` 섹션 **앞**에 다음을 삽입:

```python
    # v0.5.0: 자금흐름·주요결정
    "FUND_DIVERSION":         ["5.3", "8.1"],
    "FUND_UNREPORTED":        ["4.3"],
    "DECISION_RELATED_PARTY": ["4.2"],
    "DECISION_OVERSIZED":     ["5.3"],
    "DECISION_NO_EXTVAL":     ["4.3"],
```

매핑 근거:
- `FUND_DIVERSION` → 5.3(오프마켓 자산이전) + 8.1(자본잠식·부도) — 자금유용은 자산이전이면서 부실 선행지표
- `FUND_UNREPORTED` → 4.3(공시의무위반)
- `DECISION_RELATED_PARTY` → 4.2(특수관계자 거래)
- `DECISION_OVERSIZED` → 5.3
- `DECISION_NO_EXTVAL` → 4.3

- [ ] **Step 1.6: 테스트 재실행 → PASS 확인**

Run: `python -m unittest tests.test_signals_new -v`
Expected: 3 tests OK

또한 기존 스모크 테스트가 깨지지 않았는지 확인:
Run: `python -c "import dart_risk_mcp.server; print('OK')"`
Expected: `OK`

- [ ] **Step 1.7: 커밋**

```bash
git add tests/__init__.py tests/test_signals_new.py dart_risk_mcp/core/signals.py
git commit -m "$(cat <<'EOF'
feat(signals): v0.5.0 신규 5개 신호 키 추가

FUND_DIVERSION·FUND_UNREPORTED·DECISION_RELATED_PARTY·DECISION_OVERSIZED·
DECISION_NO_EXTVAL을 SIGNAL_TYPES에 등록하고 SIGNAL_KEY_TO_TAXONOMY로
기존 4.2/4.3/5.3/8.1 축에 편입. 키워드 매칭이 아닌 함수 내 프로그램적
부착을 전제로 keywords는 빈 리스트.
EOF
)"
```

---

## Task 2: `fetch_fund_usage` 함수 + 단위 테스트

**Files:**
- Create: `tests/test_fund_usage.py`
- Modify: `dart_risk_mcp/core/dart_client.py`

- [ ] **Step 2.1: tests/test_fund_usage.py 작성**

```python
"""fetch_fund_usage 정규화·이상 플래그·캐시·부분실패 검증."""
import unittest
from unittest.mock import patch, MagicMock

from dart_risk_mcp.core import dart_client


def _mock_resp(status="000", lst=None):
    resp = MagicMock()
    resp.json.return_value = {
        "status": status,
        "message": "정상" if status == "000" else "데이터 없음",
        "list": lst or [],
    }
    return resp


class TestFetchFundUsage(unittest.TestCase):
    def setUp(self):
        dart_client._fund_usage_cache.clear()

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_normal_response_no_anomaly(self, mock_retry):
        mock_retry.return_value = _mock_resp(
            lst=[{
                "se_nm": "일반공모",
                "tm": "1",
                "pay_de": "2024-03-10",
                "pay_amount": "10000000000",
                "rs_cptal_use_plan_useprps": "운영자금",
                "rs_cptal_use_plan_prcure_amount": "10000000000",
                "real_cptal_use_dtls_cn": "운영자금 집행",
                "real_cptal_use_dtls_amount": "10000000000",
                "dffrnc_occrrnc_resn": "",
            }],
        )
        result = dart_client.fetch_fund_usage("00000001", "K", 1)
        self.assertTrue(result)
        self.assertEqual(result[0]["plan_amount"], 10_000_000_000)
        self.assertEqual(result[0]["real_dtls_amount"], 10_000_000_000)
        self.assertEqual(result[0]["flags"], [])

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_fund_unreported_flag(self, mock_retry):
        mock_retry.return_value = _mock_resp(
            lst=[{
                "tm": "2",
                "pay_amount": "5000000000",
                "mtrpt_cptal_use_plan_useprps": "시설투자",
                "mtrpt_cptal_use_plan_prcure_amount": "5000000000",
                "real_cptal_use_dtls_cn": "",
                "real_cptal_use_dtls_amount": "0",
                "dffrnc_occrrnc_resn": "",
            }],
        )
        result = dart_client.fetch_fund_usage("00000002", "K", 1)
        self.assertTrue(any("FUND_UNREPORTED" in r["flags"] for r in result))

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_fund_diversion_flag(self, mock_retry):
        mock_retry.return_value = _mock_resp(
            lst=[{
                "tm": "1",
                "pay_amount": "3000000000",
                "mtrpt_cptal_use_plan_useprps": "신사업투자",
                "mtrpt_cptal_use_plan_prcure_amount": "3000000000",
                "real_cptal_use_dtls_cn": "운영자금",
                "real_cptal_use_dtls_amount": "3000000000",
                "dffrnc_occrrnc_resn": "사업 취소로 일반 운영자금으로 변경 사용",
            }],
        )
        result = dart_client.fetch_fund_usage("00000003", "K", 1)
        self.assertTrue(any("FUND_DIVERSION" in r["flags"] for r in result))

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_status_013_returns_empty(self, mock_retry):
        mock_retry.return_value = _mock_resp(status="013", lst=[])
        result = dart_client.fetch_fund_usage("00000004", "K", 1)
        self.assertEqual(result, [])

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_network_failure_silent(self, mock_retry):
        mock_retry.side_effect = Exception("network down")
        result = dart_client.fetch_fund_usage("00000005", "K", 1)
        self.assertEqual(result, [])

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_cache_hit_skips_second_call(self, mock_retry):
        mock_retry.return_value = _mock_resp(lst=[])
        dart_client.fetch_fund_usage("00000006", "K", 2)
        first_count = mock_retry.call_count
        dart_client.fetch_fund_usage("00000006", "K", 2)
        self.assertEqual(mock_retry.call_count, first_count)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2.2: 테스트 실행 → FAIL 확인**

Run: `python -m unittest tests.test_fund_usage -v`
Expected: ImportError 또는 AttributeError (`fetch_fund_usage` 미정의)

- [ ] **Step 2.3: dart_client.py에 캐시 헬퍼 + 정규화·판정 헬퍼 추가**

`dart_client.py`의 **기존 `_zip_cache` 정의 블록 바로 아래**(파일 상단 상수 구역)에 다음을 삽입:

```python
# v0.5.0: fund_usage / major_decision LRU 캐시 ------------------
_fund_usage_cache: dict[tuple, tuple[float, list]] = {}
_FUND_CACHE_MAX = 20
_FUND_CACHE_TTL = 600  # 10분

_major_decision_cache: dict[str, tuple[float, dict]] = {}
_MAJOR_CACHE_MAX = 50
_MAJOR_CACHE_TTL = 600


def _cache_get(cache: dict, key, ttl: int):
    import time
    item = cache.get(key)
    if item is None:
        return None
    ts, val = item
    if time.time() - ts > ttl:
        cache.pop(key, None)
        return None
    return val


def _cache_set(cache: dict, key, val, limit: int) -> None:
    import time
    if len(cache) >= limit and key not in cache:
        oldest_key = min(cache.items(), key=lambda kv: kv[1][0])[0]
        cache.pop(oldest_key, None)
    cache[key] = (time.time(), val)


# v0.5.0: 자금사용내역 엔드포인트 --------------------------------
_FUND_USAGE_URLS = {
    "public":  f"{DART_BASE}/pssrpCptalUseDtls.json",   # 2020016
    "private": f"{DART_BASE}/prvsrpCptalUseDtls.json",  # 2020017
}

_FUND_DIVERSION_KEYWORDS = (
    "목적 변경", "목적변경", "사용목적 변경",
    "사업 취소", "사업취소", "계획 취소", "계획취소",
    "일반 운영자금", "일반운영자금", "운영자금 전용", "운영자금으로",
    "변경 사용", "변경사용", "유보",
)


def _to_int_safe(v) -> int:
    if v is None or v == "":
        return 0
    try:
        return int(str(v).replace(",", "").replace(" ", ""))
    except (ValueError, TypeError):
        return 0


def _normalize_fund_usage(item: dict, kind: str, year: int) -> dict:
    """공모(rs_*)/사모(mtrpt_*)/구필드를 통합 스키마로 정규화."""
    plan_useprps = (
        item.get("rs_cptal_use_plan_useprps")
        or item.get("mtrpt_cptal_use_plan_useprps")
        or item.get("cptal_use_plan")
        or item.get("on_dclrt_cptal_use_plan")
        or ""
    )
    plan_amount = _to_int_safe(
        item.get("rs_cptal_use_plan_prcure_amount")
        or item.get("mtrpt_cptal_use_plan_prcure_amount")
        or item.get("pay_amount")
    )
    real_dtls_cn = (
        item.get("real_cptal_use_dtls_cn")
        or item.get("real_cptal_use_sttus")
        or ""
    )
    return {
        "kind": kind,
        "year": year,
        "tm": str(item.get("tm", "")),
        "pay_de": str(item.get("pay_de", "")),
        "pay_amount": _to_int_safe(item.get("pay_amount")),
        "plan_useprps": str(plan_useprps).strip(),
        "plan_amount": plan_amount,
        "real_dtls_cn": str(real_dtls_cn).strip(),
        "real_dtls_amount": _to_int_safe(item.get("real_cptal_use_dtls_amount")),
        "dffrnc_resn": str(item.get("dffrnc_occrrnc_resn") or "").strip(),
    }


def _detect_fund_anomaly(rec: dict) -> list[str]:
    flags: list[str] = []
    if rec["plan_amount"] > 0 and (
        rec["real_dtls_amount"] == 0 or not rec["real_dtls_cn"]
    ):
        flags.append("FUND_UNREPORTED")
    dffrnc = rec["dffrnc_resn"]
    if dffrnc and any(kw in dffrnc for kw in _FUND_DIVERSION_KEYWORDS):
        flags.append("FUND_DIVERSION")
    return flags
```

**중요**: 이 블록은 `_zip_cache`, `_ZIP_CACHE_MAX`, `_ZIP_CACHE_TTL` 정의 **아래**, 그리고 `def _load_corp_codes` **위**에 와야 한다 (모듈 전역 상수 구역).

- [ ] **Step 2.4: dart_client.py에 `fetch_fund_usage` 함수 추가**

파일 **끝**(마지막 함수 뒤)에 다음을 추가:

```python
def fetch_fund_usage(
    corp_code: str,
    api_key: str,
    lookback_years: int = 3,
) -> list[dict]:
    """공모/사모 자금사용내역(DS002 2020016/17)을 lookback_years만큼 조회해 정규화 반환.

    각 레코드에 `flags` 키(FUND_DIVERSION·FUND_UNREPORTED)가 부착된다.
    API 호출 실패 또는 status!="000"인 연도·보고서 코드는 조용히 스킵한다.
    """
    cache_key = (corp_code, lookback_years)
    cached = _cache_get(_fund_usage_cache, cache_key, _FUND_CACHE_TTL)
    if cached is not None:
        return cached

    from datetime import date
    current_year = date.today().year
    results: list[dict] = []

    for yr in range(current_year - lookback_years, current_year + 1):
        for kind, url in _FUND_USAGE_URLS.items():
            for reprt_code in ("11011", "11012", "11013", "11014"):
                params = {
                    "crtfc_key": api_key,
                    "corp_code": corp_code,
                    "bsns_year": str(yr),
                    "reprt_code": reprt_code,
                }
                try:
                    data = _retry("GET", url, params=params).json()
                except Exception:
                    continue
                if data.get("status") != "000":
                    continue
                for item in data.get("list", []):
                    rec = _normalize_fund_usage(item, kind, yr)
                    rec["flags"] = _detect_fund_anomaly(rec)
                    results.append(rec)

    _cache_set(_fund_usage_cache, cache_key, results, _FUND_CACHE_MAX)
    return results
```

`reprt_code` 4개 반복 근거: DS002는 정기보고서 기반이라 `bsns_year` + `reprt_code`가 필수. 사업(11011)·반기(11012)·1분기(11013)·3분기(11014) 모두 조회해 누락 방지. 실제 공시가 없는 조합은 `status!="000"`로 돌아와 자동 스킵.

- [ ] **Step 2.5: 테스트 재실행 → PASS 확인**

Run: `python -m unittest tests.test_fund_usage -v`
Expected: 6 tests OK

- [ ] **Step 2.6: 커밋**

```bash
git add tests/test_fund_usage.py dart_risk_mcp/core/dart_client.py
git commit -m "$(cat <<'EOF'
feat(dart_client): fetch_fund_usage 공모/사모 자금사용내역 조회 추가

DS002 2020016/17 엔드포인트를 lookback_years만큼 수집해 공모(rs_*)·사모
(mtrpt_*)·구필드를 통합 스키마로 정규화. 차이사유 키워드와 실제사용
공란/0원 패턴으로 FUND_DIVERSION·FUND_UNREPORTED 플래그 부착. 전용
LRU 캐시(최대 20건, 10분 TTL) 추가.
EOF
)"
```

---

## Task 3: `fetch_major_decision` + 라우팅 + 단위 테스트

**Files:**
- Create: `tests/test_major_decision.py`
- Modify: `dart_risk_mcp/core/dart_client.py`

- [ ] **Step 3.1: tests/test_major_decision.py 작성**

```python
"""fetch_major_decision·resolve_decision_type 검증."""
import unittest
from unittest.mock import patch, MagicMock

from dart_risk_mcp.core import dart_client
from dart_risk_mcp.core.dart_client import resolve_decision_type


def _mock_resp(status="000", lst=None):
    resp = MagicMock()
    resp.json.return_value = {
        "status": status,
        "message": "정상" if status == "000" else "",
        "list": lst or [],
    }
    return resp


class TestResolveDecisionType(unittest.TestCase):
    def test_stock_acquisition(self):
        self.assertEqual(
            resolve_decision_type("타법인 주식 및 출자증권 양수결정"),
            "stock_acq",
        )

    def test_merger(self):
        self.assertEqual(resolve_decision_type("회사합병결정"), "merger")

    def test_amendment_prefix_ignored(self):
        self.assertEqual(
            resolve_decision_type("[기재정정] 영업양수결정"),
            "business_acq",
        )

    def test_unknown_returns_empty(self):
        self.assertEqual(resolve_decision_type("분기보고서"), "")


class TestFetchMajorDecision(unittest.TestCase):
    def setUp(self):
        dart_client._major_decision_cache.clear()

    def test_invalid_rcept_no(self):
        result = dart_client.fetch_major_decision("123", "K", "merger")
        self.assertIn("error", result)

    def test_unknown_decision_type_returns_error(self):
        result = dart_client.fetch_major_decision(
            "20240101000001", "K", decision_type=""
        )
        self.assertIn("error", result)

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_related_party_and_oversized_flags(self, mock_retry):
        mock_retry.return_value = _mock_resp(lst=[{
            "dlptn_cmpnm": "특수관계회사",
            "dlptn_rl_cmpn": "최대주주의 계열회사",
            "inh_pp": "50000000000",
            "inhdamount_totalast_rt": "35.5",
            "ftc_stt_atn": "예",
            "exevl_atn": "아니오",
            "bddd": "2024-05-10",
        }])
        result = dart_client.fetch_major_decision(
            "20240510000001", "K", decision_type="stock_acq"
        )
        self.assertNotIn("error", result)
        self.assertIn("DECISION_RELATED_PARTY", result["flags"])
        self.assertIn("DECISION_OVERSIZED", result["flags"])
        self.assertIn("DECISION_NO_EXTVAL", result["flags"])

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_network_failure(self, mock_retry):
        mock_retry.side_effect = Exception("timeout")
        result = dart_client.fetch_major_decision(
            "20240510000002", "K", decision_type="merger"
        )
        self.assertIn("error", result)

    @patch("dart_risk_mcp.core.dart_client._retry")
    def test_cache_hit(self, mock_retry):
        mock_retry.return_value = _mock_resp(lst=[{"bddd": "2024-01-01"}])
        dart_client.fetch_major_decision(
            "20240101000099", "K", decision_type="merger"
        )
        first = mock_retry.call_count
        dart_client.fetch_major_decision(
            "20240101000099", "K", decision_type="merger"
        )
        self.assertEqual(mock_retry.call_count, first)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 3.2: 테스트 실행 → FAIL 확인**

Run: `python -m unittest tests.test_major_decision -v`
Expected: AttributeError (`fetch_major_decision`·`resolve_decision_type` 미정의)

- [ ] **Step 3.3: dart_client.py 상단 상수 구역에 라우팅 테이블 추가**

Task 2에서 추가한 `_FUND_DIVERSION_KEYWORDS` 아래에 이어서 추가:

```python
# v0.5.0: 주요사항보고서 결정 공시(DS005 2020042~2020053) 라우팅 -
_MAJOR_DECISION_URLS = {
    "business_acq":    f"{DART_BASE}/bsnInhDecsn.json",              # 2020042
    "business_div":    f"{DART_BASE}/bsnTrfDecsn.json",              # 2020043
    "tangible_acq":    f"{DART_BASE}/tgastInhDecsn.json",            # 2020044
    "tangible_div":    f"{DART_BASE}/tgastTrfDecsn.json",            # 2020045
    "stock_acq":       f"{DART_BASE}/otcprStkInvscrInhDecsn.json",   # 2020046
    "stock_div":       f"{DART_BASE}/otcprStkInvscrTrfDecsn.json",   # 2020047
    "bond_acq":        f"{DART_BASE}/stkrtbdInhDecsn.json",          # 2020048
    "bond_div":        f"{DART_BASE}/stkrtbdTrfDecsn.json",          # 2020049
    "merger":          f"{DART_BASE}/cmpMgDecsn.json",               # 2020050
    "demerger":        f"{DART_BASE}/cmpDvDecsn.json",               # 2020051
    "demerger_merger": f"{DART_BASE}/cmpDvmgDecsn.json",             # 2020052
    "stock_exchange":  f"{DART_BASE}/stkExtrDecsn.json",             # 2020053
}

# 보고서명 키워드 → decision_type 매핑 (긴 키워드부터 매칭)
_DECISION_NAME_MAP = [
    ("타법인주식및출자증권양수결정", "stock_acq"),
    ("타법인주식및출자증권양도결정", "stock_div"),
    ("주권관련사채권양수결정",       "bond_acq"),
    ("주권관련사채권양도결정",       "bond_div"),
    ("유형자산양수결정",             "tangible_acq"),
    ("유형자산양도결정",             "tangible_div"),
    ("회사분할합병결정",             "demerger_merger"),
    ("주식교환이전결정",             "stock_exchange"),
    ("회사합병결정",                 "merger"),
    ("회사분할결정",                 "demerger"),
    ("영업양수결정",                 "business_acq"),
    ("영업양도결정",                 "business_div"),
]
```

긴 키워드부터 매칭해야 "회사분할합병"이 "회사분할"에 먼저 매칭되는 오류를 피한다.

- [ ] **Step 3.4: `resolve_decision_type` 함수 추가**

파일 **끝**에 다음을 추가:

```python
def resolve_decision_type(report_name: str) -> str:
    """보고서명에서 decision_type을 자동 판별. 판별 실패 시 빈 문자열."""
    import re
    nm = re.sub(r"\[[^\]]*\]", "", report_name or "")
    nm = nm.replace(" ", "").replace("·", "").replace(",", "")
    for keyword, dtype in _DECISION_NAME_MAP:
        if keyword in nm:
            return dtype
    return ""
```

- [ ] **Step 3.5: `fetch_major_decision` 함수 추가**

`resolve_decision_type` 아래에 이어서 추가:

```python
def _normalize_decision(raw: dict, dtype: str, url: str) -> dict:
    """결정 공시 원본을 공통 스키마로 정규화."""
    counterparty = (
        raw.get("dlptn_cmpnm")
        or raw.get("dlptn_rl_cmpn")
        or raw.get("mg_ctrcmp_cmpnm")
        or raw.get("dvcmp_cmpnm")
        or ""
    )
    amount = _to_int_safe(
        raw.get("inh_pp")
        or raw.get("trf_pp")
        or raw.get("trfg_pp")
        or raw.get("mg_rt")
        or raw.get("dlptn_cpt")
    )
    asset_ratio_raw = (
        raw.get("inhdamount_totalast_rt")
        or raw.get("trfamount_totalast_rt")
        or raw.get("totalast_rt")
        or "0"
    )
    try:
        asset_ratio = float(str(asset_ratio_raw).replace("%", "").strip() or 0)
    except ValueError:
        asset_ratio = 0.0

    related = False
    for k in ("ftc_stt_atn", "rl_cmpn_atn", "speclt_pson_atn"):
        v = str(raw.get(k) or "").strip()
        if v in ("예", "Y", "해당", "있음"):
            related = True
            break
    if not related:
        rel_text = str(raw.get("dlptn_rl_cmpn") or "")
        if any(s in rel_text for s in ("특수관계", "계열회사", "관계회사", "자회사", "최대주주")):
            related = True

    external_eval = str(raw.get("exevl_atn") or "").strip() in (
        "예", "Y", "해당", "실시",
    )

    return {
        "decision_type": dtype,
        "endpoint": url,
        "counterparty": str(counterparty).strip(),
        "amount": amount,
        "asset_ratio": asset_ratio,
        "related_party": related,
        "external_eval": external_eval,
        "bddd": str(raw.get("bddd") or raw.get("dcrdd") or "").strip(),
        "raw": raw,
    }


def _detect_decision_anomaly(result: dict) -> list[str]:
    flags: list[str] = []
    if result["related_party"] and result["asset_ratio"] >= 10.0:
        flags.append("DECISION_RELATED_PARTY")
    if result["asset_ratio"] >= 30.0:
        flags.append("DECISION_OVERSIZED")
    if (not result["external_eval"]) and result["amount"] >= 5_000_000_000:
        flags.append("DECISION_NO_EXTVAL")
    return flags


def fetch_major_decision(
    rcept_no: str,
    api_key: str,
    decision_type: str = "",
) -> dict:
    """DS005 12개 결정 공시를 구조화 필드로 반환. 실패 시 {"error": ...}.

    decision_type 미지정 시 빈 결과를 안내 메시지로 반환한다(rcept_no만으로
    보고서명을 역조회하는 API가 없어 호출자가 명시해야 한다).
    """
    if not isinstance(rcept_no, str) or len(rcept_no) != 14 or not rcept_no.isdigit():
        return {"error": f"rcept_no는 14자리 숫자여야 합니다: {rcept_no!r}"}

    cached = _cache_get(_major_decision_cache, rcept_no, _MAJOR_CACHE_TTL)
    if cached is not None and cached.get("decision_type") == (decision_type or cached.get("decision_type")):
        return cached

    dtype = decision_type
    if not dtype or dtype not in _MAJOR_DECISION_URLS:
        return {
            "error": (
                "decision_type 미지정 또는 알 수 없는 값. 지원 타입: "
                + ", ".join(_MAJOR_DECISION_URLS.keys())
            )
        }

    url = _MAJOR_DECISION_URLS[dtype]
    params = {"crtfc_key": api_key, "rcept_no": rcept_no}
    try:
        data = _retry("GET", url, params=params).json()
    except Exception as e:
        return {"error": f"DART 조회 실패: {e}"}

    if data.get("status") != "000":
        return {
            "error": (
                f"DART status={data.get('status')}: "
                f"{data.get('message', '')}"
            )
        }
    items = data.get("list", [])
    if not items:
        return {"error": "해당 공시에 구조화 데이터가 없습니다."}

    result = _normalize_decision(items[0], dtype, url)
    result["flags"] = _detect_decision_anomaly(result)
    _cache_set(_major_decision_cache, rcept_no, result, _MAJOR_CACHE_MAX)
    return result
```

- [ ] **Step 3.6: 테스트 재실행 → PASS 확인**

Run: `python -m unittest tests.test_major_decision -v`
Expected: 7 tests OK

- [ ] **Step 3.7: 커밋**

```bash
git add tests/test_major_decision.py dart_risk_mcp/core/dart_client.py
git commit -m "$(cat <<'EOF'
feat(dart_client): fetch_major_decision + resolve_decision_type 추가

DS005 2020042~2020053 12개 결정 공시를 단일 인터페이스로 라우팅. 상대방·
금액·자산대비·특수관계·외부평가를 공통 스키마로 정규화하고 자산비율
10%/30%·외부평가 미실시+대형금액으로 DECISION_RELATED_PARTY·
DECISION_OVERSIZED·DECISION_NO_EXTVAL 플래그 부착. 보고서명 기반
decision_type 자동 판별은 resolve_decision_type으로 분리.
EOF
)"
```

---

## Task 4: `core/__init__.py` export 갱신

**Files:**
- Modify: `dart_risk_mcp/core/__init__.py`

- [ ] **Step 4.1: export 추가**

`from .dart_client import (` 블록 안, 마지막 항목 `fetch_insider_timeline,` 뒤에 추가:

```python
    fetch_fund_usage,
    fetch_major_decision,
    resolve_decision_type,
```

같은 파일의 `__all__` 리스트, 마지막 항목 `"load_catalog_excerpt",` 앞에 추가:

```python
    "fetch_fund_usage",
    "fetch_major_decision",
    "resolve_decision_type",
```

- [ ] **Step 4.2: import 검증**

Run:
```bash
python -c "from dart_risk_mcp.core import fetch_fund_usage, fetch_major_decision, resolve_decision_type; print('OK')"
```
Expected: `OK`

- [ ] **Step 4.3: 커밋**

```bash
git add dart_risk_mcp/core/__init__.py
git commit -m "chore(core): v0.5.0 신규 함수 3개 export"
```

---

## Task 5: `track_fund_usage` MCP 도구 등록

**Files:**
- Modify: `dart_risk_mcp/server.py`

- [ ] **Step 5.1: server.py 상단 import 보강**

기존 `from .core import (` 블록에 다음 3개를 추가(정렬 유지):

```python
    fetch_fund_usage,
    fetch_major_decision,
    resolve_decision_type,
```

- [ ] **Step 5.2: `track_fund_usage` 도구 함수 추가**

`server.py`의 **마지막 `@mcp.tool()` 함수 뒤, `def main():` 앞**에 삽입:

```python
@mcp.tool()
def track_fund_usage(company_name: str, lookback_years: int = 3) -> str:
    """공모/사모 자금 사용내역(계획 vs 실제)을 조회해 조달자금 유용·
    목적외 사용 신호를 탐지한다. zombie_ma·fake_new_biz 패턴의 핵심 증거.

    Args:
        company_name: 기업명 또는 6자리 종목코드
        lookback_years: 조회 연도 수 (1~5, 기본 3)
    """
    if not _DART_API_KEY:
        return "❌ DART_API_KEY 환경변수가 설정되지 않았습니다."
    if not isinstance(lookback_years, int) or not (1 <= lookback_years <= 5):
        return "❌ lookback_years는 1~5 사이 정수여야 합니다."

    corp_name, info = resolve_corp(company_name, _DART_API_KEY)
    if not info:
        return f"❌ '{company_name}'에 해당하는 기업을 찾을 수 없습니다."

    records = fetch_fund_usage(info["corp_code"], _DART_API_KEY, lookback_years)
    if not records:
        return (
            f"💰 **{corp_name}** 자금사용내역\n\n"
            f"최근 {lookback_years}년간 등록된 공모/사모 자금사용내역이 없습니다.\n"
            f"(정기보고서(사업/반기/분기) 제출 시점에만 갱신됩니다.)"
        )

    lines = [
        f"💰 **{corp_name}** 조달자금 사용내역 (lookback={lookback_years}년)",
        f"총 {len(records)}건 조회",
        "",
    ]
    anomaly_records = []
    for rec in records:
        flags_str = " ".join(f"⚠{f}" for f in rec["flags"])
        if rec["flags"]:
            anomaly_records.append(rec)
        lines.append(
            f"[{rec['year']} {rec['kind']} 회차{rec['tm']}] "
            f"납입 {rec['pay_amount']:,}원"
        )
        lines.append(
            f"  계획: {rec['plan_useprps'][:60] or '(공란)'} "
            f"({rec['plan_amount']:,}원)"
        )
        lines.append(
            f"  실제: {rec['real_dtls_cn'][:60] or '(공란)'} "
            f"({rec['real_dtls_amount']:,}원) {flags_str}"
        )
        if rec["dffrnc_resn"]:
            lines.append(f"  차이사유: {rec['dffrnc_resn'][:100]}")
        lines.append("")

    if anomaly_records:
        lines.append(f"🚨 **이상 플래그 {len(anomaly_records)}건**")
        lines.append("")
        lines.append(load_catalog_excerpt(["zombie_ma", "fake_new_biz"]))
    else:
        lines.append("✅ 탐지된 이상 플래그 없음")

    return "\n".join(lines)
```

- [ ] **Step 5.3: 스모크 테스트**

Run:
```bash
python -c "
from dart_risk_mcp.server import mcp
tools = [t.name for t in mcp._tool_manager.list_tools()]
assert 'track_fund_usage' in tools, f'not registered: {tools}'
print('registered:', len(tools), 'tools')
"
```
Expected: `registered: 18 tools`

- [ ] **Step 5.4: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "feat(server): track_fund_usage MCP 도구 등록"
```

---

## Task 6: `get_major_decision` MCP 도구 등록

**Files:**
- Modify: `dart_risk_mcp/server.py`

- [ ] **Step 6.1: `get_major_decision` 도구 함수 추가**

`track_fund_usage` 바로 아래(`def main():` 위)에 삽입:

```python
@mcp.tool()
def get_major_decision(rcept_no: str, decision_type: str = "") -> str:
    """DS005 주요사항보고서 12종 결정 공시(양수도·합병·분할·교환)를
    구조화 필드로 조회한다. related_party_hollowing·delisting_evasion
    패턴의 경로 추적에 사용.

    Args:
        rcept_no: 14자리 접수번호
        decision_type: 결정 유형 (미지정 시 지원 타입 안내).
            business_acq | business_div | tangible_acq | tangible_div |
            stock_acq | stock_div | bond_acq | bond_div |
            merger | demerger | demerger_merger | stock_exchange
    """
    if not _DART_API_KEY:
        return "❌ DART_API_KEY 환경변수가 설정되지 않았습니다."

    result = fetch_major_decision(rcept_no, _DART_API_KEY, decision_type)
    if "error" in result:
        return f"❌ {result['error']}"

    lines = [
        f"📑 **주요사항 결정 공시** (rcept_no={rcept_no})",
        f"- 유형: `{result['decision_type']}`",
        f"- 상대방: {result['counterparty'] or '(미기재)'}",
        f"- 금액: {result['amount']:,}원",
        f"- 자산대비: {result['asset_ratio']:.2f}%",
        f"- 특수관계인: {'예' if result['related_party'] else '아니오'}",
        f"- 외부평가: {'실시' if result['external_eval'] else '미실시'}",
        f"- 결의일: {result['bddd'] or '(미기재)'}",
    ]
    if result["flags"]:
        lines.append("")
        lines.append("🚨 **탐지 플래그**: " + ", ".join(result["flags"]))
        tax_ids = sorted({
            t for f in result["flags"]
            for t in SIGNAL_KEY_TO_TAXONOMY.get(f, [])
        })
        if tax_ids:
            lines.append(f"관련 taxonomy ID: {', '.join(tax_ids)}")
    lines.append("")
    lines.append(f"원문 전체 보기: `view_disclosure('{rcept_no}')`")
    return "\n".join(lines)
```

- [ ] **Step 6.2: 스모크 테스트**

Run:
```bash
python -c "
from dart_risk_mcp.server import mcp
tools = [t.name for t in mcp._tool_manager.list_tools()]
assert 'get_major_decision' in tools
assert len(tools) == 19, f'expected 19 got {len(tools)}'
print('OK', len(tools))
"
```
Expected: `OK 19`

- [ ] **Step 6.3: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "feat(server): get_major_decision MCP 도구 등록"
```

---

## Task 7: `check_disclosure_risk` 강화

**Files:**
- Modify: `dart_risk_mcp/server.py`

- [ ] **Step 7.1: 현재 함수 구조 확인**

`check_disclosure_risk` 함수의 반환 직전(마지막 `return "\n".join(lines)` 직전)에 삽입할 블록을 식별한다.

- [ ] **Step 7.2: DS005 결정 공시 분기 추가**

`check_disclosure_risk` 함수 본문 안, **CB 인수자 추출 블록 뒤·return 앞**에 다음을 삽입:

```python
    # v0.5.0: DS005 결정 공시면 구조화 필드 추가 ---------------
    report_nm = report_name or (disclosure.get("report_nm") if disclosure else "")
    dtype = resolve_decision_type(report_nm)
    if dtype and rcept_no:
        dec = fetch_major_decision(rcept_no, _DART_API_KEY, dtype)
        if "error" not in dec:
            lines.append("")
            lines.append("📑 **결정 공시 구조화 정보**")
            lines.append(f"- 유형: `{dec['decision_type']}`")
            lines.append(f"- 상대방: {dec['counterparty'] or '(미기재)'}")
            lines.append(
                f"- 금액: {dec['amount']:,}원 "
                f"(자산대비 {dec['asset_ratio']:.2f}%)"
            )
            lines.append(
                f"- 특수관계인: {'예' if dec['related_party'] else '아니오'}"
            )
            lines.append(
                f"- 외부평가: {'실시' if dec['external_eval'] else '미실시'}"
            )
            if dec["flags"]:
                lines.append("- 플래그: " + ", ".join(dec["flags"]))
```

**삽입 위치 상세**: `check_disclosure_risk`에서 `extract_cb_investors` 호출 후 인수자 정보를 `lines`에 추가하는 블록 다음, 그리고 `load_catalog_excerpt` 호출 블록 **앞**에 넣는다(카탈로그는 리포트 말미에 와야 함).

`disclosure` 지역 변수는 함수에서 `fetch_company_disclosures`로 받은 dict로 가정. 없으면 `report_name` 파라미터를 사용.

- [ ] **Step 7.3: 스모크 테스트 (기존 호환)**

Run:
```bash
python -c "
import os; os.environ.setdefault('DART_API_KEY','dummy')
from dart_risk_mcp.server import check_disclosure_risk
out = check_disclosure_risk(rcept_no='', report_name='')
assert '❌' in out or '조회' in out or '공시' in out
print('OK')
"
```
Expected: `OK` (에러 가드가 작동해 예외 없이 문자열 반환)

- [ ] **Step 7.4: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "feat(server): check_disclosure_risk — DS005 결정 공시 구조화 출력 추가"
```

---

## Task 8: `analyze_company_risk` 강화

**Files:**
- Modify: `dart_risk_mcp/server.py`

- [ ] **Step 8.1: 현재 함수의 리포트 조립 마지막 구간 식별**

`analyze_company_risk`가 CB 인수자 섹션과 타임라인을 조립한 뒤 `load_catalog_excerpt` 호출 **앞**에 신규 섹션을 삽입한다.

- [ ] **Step 8.2: 주요 결정 상대방 섹션 추가**

해당 위치에 다음 블록 삽입:

```python
    # v0.5.0: DS005 결정 공시 최대 10건 구조화 ------------------
    decision_items = []
    for disc in disclosures:
        if resolve_decision_type(disc.get("report_nm", "")):
            decision_items.append(disc)
    decision_items = decision_items[:10]

    decisions = []
    failed = 0
    for disc in decision_items:
        dtype = resolve_decision_type(disc["report_nm"])
        r = fetch_major_decision(disc["rcept_no"], _DART_API_KEY, dtype)
        if "error" in r:
            failed += 1
            continue
        decisions.append((disc, r))

    if decisions:
        lines.append("")
        lines.append("📑 **주요 결정 상대방** (최근 순, 최대 10건)")
        for disc, r in decisions:
            flag_str = " " + ",".join(r["flags"]) if r["flags"] else ""
            lines.append(
                f"- [{disc['rcept_dt']}] {disc['report_nm']}"
            )
            lines.append(
                f"  → {r['counterparty'] or '(미기재)'} / "
                f"{r['amount']:,}원 ({r['asset_ratio']:.1f}%)"
                f"{flag_str}"
            )
        if failed:
            lines.append(f"  (추가 {failed}건 구조화 조회 실패)")
```

- [ ] **Step 8.3: 자금사용내역 요약 섹션 추가**

위 블록 바로 아래 이어서 삽입:

```python
    # v0.5.0: 자금사용내역 요약 --------------------------------
    fund_records = fetch_fund_usage(info["corp_code"], _DART_API_KEY, 3)
    if fund_records:
        anomaly_recs = [r for r in fund_records if r["flags"]]
        lines.append("")
        lines.append(
            f"💰 **조달자금 사용내역** (최근 3년, {len(fund_records)}건, "
            f"이상 {len(anomaly_recs)}건)"
        )
        for r in anomaly_recs[:5]:
            lines.append(
                f"- [{r['year']} {r['kind']} 회차{r['tm']}] "
                f"{','.join(r['flags'])} | "
                f"계획 {r['plan_useprps'][:30]} → "
                f"실제 {r['real_dtls_cn'][:30]}"
            )
```

**중요**: `info` 변수가 해당 함수 스코프에 존재하는지 확인. 기존 `analyze_company_risk`에서 `resolve_corp` 반환을 `corp_name, info`로 받았다면 그대로 사용. 다른 이름이면 실제 변수명으로 치환.

- [ ] **Step 8.4: 신규 플래그 집계 반영 확인**

`analyze_company_risk`의 신호 집계(`signal_events`)에 `decisions`의 플래그와 `fund_records`의 플래그를 합산하는 처리가 있어야 `find_pattern_match`가 신규 플래그를 패턴 매칭에 사용할 수 있다. 아래 블록을 Step 8.3 직후에 추가:

```python
    # 신규 플래그를 signal_events에 합산 (패턴 매칭용) ---------
    for _, r in decisions:
        for fkey in r["flags"]:
            for tid in SIGNAL_KEY_TO_TAXONOMY.get(fkey, []):
                signal_events.setdefault(tid, []).append({
                    "rcept_dt": r.get("bddd", ""),
                    "report_nm": f"[결정:{r['decision_type']}] {r.get('counterparty','')}",
                    "rcept_no": "",
                })
    for r in fund_records:
        for fkey in r["flags"]:
            for tid in SIGNAL_KEY_TO_TAXONOMY.get(fkey, []):
                signal_events.setdefault(tid, []).append({
                    "rcept_dt": r.get("pay_de", ""),
                    "report_nm": f"[자금:{r['kind']} 회차{r['tm']}] {r.get('plan_useprps','')[:30]}",
                    "rcept_no": "",
                })
```

**변수 확인**: 기존 `analyze_company_risk`가 사용하는 집계 dict의 실제 이름을 확인해 `signal_events` 위치에 대입. 만약 다른 이름(`matched_by_tax`, `events_by_id` 등)이면 그 이름으로 치환.

- [ ] **Step 8.5: 스모크 테스트 (서버 import)**

Run:
```bash
python -c "import dart_risk_mcp.server; print('OK')"
```
Expected: `OK` (문법 오류 없음)

- [ ] **Step 8.6: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "$(cat <<'EOF'
feat(server): analyze_company_risk — 주요결정·자금사용내역 섹션 추가

DS005 결정 공시 최신 10건을 구조화로 끌어와 주요 결정 상대방 섹션을
출력하고, 최근 3년 자금사용내역의 이상 건을 요약. 신규 플래그는
signal_events로 합산해 find_pattern_match가 zombie_ma·fake_new_biz·
related_party_hollowing 매칭 정확도를 높임.
EOF
)"
```

---

## Task 9: `build_event_timeline` 강화

**Files:**
- Modify: `dart_risk_mcp/server.py`

- [ ] **Step 9.1: 단계 분류 매핑 확장**

`build_event_timeline` 함수에서 단계 분류(`PHASE_MAP` 또는 이와 동등한 구조)를 찾아 확장. 예시(실제 키 이름은 기존 구현에 맞게 조정):

```python
    # v0.5.0: 단계 매핑 확장 ----------------------------------
    EXTRA_PHASE = {
        "DECISION_RELATED_PARTY": "entry",      # 진입기
        "FUND_DIVERSION":         "entry",
        "DECISION_OVERSIZED":     "middle",     # 심화기
        "DECISION_NO_EXTVAL":     "middle",
        "FUND_UNREPORTED":        "middle",
    }
```

기존 매핑 dict가 있으면 `update(EXTRA_PHASE)`, 없으면 해당 블록에서 `.get(key, default)` 조회 시 폴백으로 사용.

- [ ] **Step 9.2: "상대방" 행 추가**

타임라인을 출력할 때, 해당 이벤트가 `resolve_decision_type(report_nm)` 으로 판별되는 결정 공시이면 `fetch_major_decision`으로 상대방을 끌어와 다음 한 줄을 추가:

```python
    # v0.5.0: 결정 공시에 상대방 행 추가 -----------------------
    from .core import resolve_decision_type as _rdt
    from .core import fetch_major_decision as _fmd

    dtype = _rdt(event.get("report_nm", ""))
    if dtype and event.get("rcept_no"):
        dec = _fmd(event["rcept_no"], _DART_API_KEY, dtype)
        if "error" not in dec and dec["counterparty"]:
            lines.append(
                f"      └ 상대방: {dec['counterparty']} "
                f"({dec['amount']:,}원)"
            )
```

**호출 비용 주의**: 타임라인의 이벤트 수가 많으면 N번 호출. 기존 캐시(`_major_decision_cache`, 최대 50건) 재사용되므로 같은 `rcept_no` 중복 호출은 자동 차단. `analyze_company_risk`가 먼저 호출한 경우 캐시 히트.

- [ ] **Step 9.3: 스모크 테스트**

Run:
```bash
python -c "import dart_risk_mcp.server; print('OK')"
```
Expected: `OK`

- [ ] **Step 9.4: 커밋**

```bash
git add dart_risk_mcp/server.py
git commit -m "$(cat <<'EOF'
feat(server): build_event_timeline — 신규 플래그 단계 매핑 + 상대방 행

DECISION_RELATED_PARTY·FUND_DIVERSION을 진입기, DECISION_OVERSIZED·
DECISION_NO_EXTVAL·FUND_UNREPORTED를 심화기에 추가. 타임라인 이벤트가
DS005 결정 공시일 때 fetch_major_decision으로 상대방·금액을 한 줄 덧붙임.
EOF
)"
```

---

## Task 10: 릴리스 준비 — 버전·CHANGELOG·CLAUDE.md

**Files:**
- Modify: `dart_risk_mcp/__init__.py`, `CHANGELOG.md`, `CLAUDE.md`

- [ ] **Step 10.1: 버전 번프**

`dart_risk_mcp/__init__.py`:
```python
"""DART 기업 위험 분석 MCP 서버 패키지."""

__version__ = "0.5.0"
```

- [ ] **Step 10.2: CHANGELOG.md에 v0.5.0 섹션 추가**

기존 `## [0.4.0]` **앞**에 삽입:

```markdown
## [0.5.0] - 2026-04-22

### Added
- 신규 도구 `track_fund_usage`: 공모/사모자금 사용내역(DS002 2020016/17)의
  계획 vs 실제 사용 대조로 조달자금 유용·목적외 사용 탐지. zombie_ma·
  fake_new_biz 패턴의 핵심 증거 확보.
- 신규 도구 `get_major_decision`: 주요사항보고서 결정 공시(DS005
  2020042~2020053) 12종을 구조화 필드(상대방·금액·자산비율·특수관계·
  외부평가)로 단일 인터페이스 조회. related_party_hollowing·
  delisting_evasion 경로 추적 지원.
- 신규 신호 5개: `FUND_DIVERSION`, `FUND_UNREPORTED`,
  `DECISION_RELATED_PARTY`, `DECISION_OVERSIZED`, `DECISION_NO_EXTVAL`
  (taxonomy 4.2/4.3/5.3/8.1에 편입).

### Changed
- `check_disclosure_risk`: DS005 결정 공시이면 구조화 필드를 자동 추가.
- `analyze_company_risk`: 주요 결정 상대방 섹션과 자금사용내역 요약 섹션
  추가 (DS005 최대 10건·자금사용 3년). 신규 플래그가 패턴 매칭에 합산됨.
- `build_event_timeline`: 신규 5개 플래그의 단계 매핑 편입, 결정 공시에
  상대방 행 추가.

### Infra
- `dart_client.py`: 자금사용내역·주요결정 전용 LRU 캐시 2종 추가
  (각 10분 TTL, 최대 20/50건).
```

- [ ] **Step 10.3: CLAUDE.md 갱신**

다음 두 곳을 수정:

**(1) MCP 도구 목록 헤더** ("MCP 도구 17개" → "MCP 도구 19개")

**(2) 도구 목록 마지막(`track_insider_trading` 항목 뒤)에 2개 섹션 추가**:

```markdown
### 18. `track_fund_usage(company_name, lookback_years=3)` ✨

공모/사모자금 사용내역(DS002 2020016/17)을 조회해 계획 vs 실제 불일치를 탐지합니다.

- 내부 흐름: `resolve_corp` → `fetch_fund_usage` (연도 × 2종 × 4보고서)
- 이상 플래그: `FUND_DIVERSION`(차이사유 키워드 매칭), `FUND_UNREPORTED`(실제 공란/0원)
- `lookback_years` 범위: 1~5년, 기본 3

### 19. `get_major_decision(rcept_no, decision_type="")` ✨

DS005 주요사항보고서 12종 결정 공시(양수도·합병·분할·교환)를 구조화 필드로 단일 조회합니다.

- 12개 엔드포인트 라우팅 — 2020042~2020053
- `decision_type` 허용값: `business_acq` / `business_div` / `tangible_acq` / `tangible_div` / `stock_acq` / `stock_div` / `bond_acq` / `bond_div` / `merger` / `demerger` / `demerger_merger` / `stock_exchange` (보고서명으로 자동 판별 시 생략 가능)
- 이상 플래그: `DECISION_RELATED_PARTY`(특수관계인 & 자산 10%↑), `DECISION_OVERSIZED`(자산 30%↑), `DECISION_NO_EXTVAL`(외부평가 X & 50억↑)
```

**(3) 핵심 내부 함수 표에 3줄 추가** (dart_client.py 표):

```markdown
| `fetch_fund_usage(corp_code, api_key, lookback_years)` | DS002 2020016/17 공모·사모 자금사용내역 정규화·플래그 |
| `fetch_major_decision(rcept_no, api_key, decision_type)` | DS005 12개 결정 공시 라우팅 + 정규화·플래그 |
| `resolve_decision_type(report_name)` | 보고서명 → decision_type 자동 판별 |
```

**(4) DART API 엔드포인트 표에 14줄 추가**:

```markdown
| `GET /api/pssrpCptalUseDtls.json` | 공모자금 사용내역 |
| `GET /api/prvsrpCptalUseDtls.json` | 사모자금 사용내역 |
| `GET /api/bsnInhDecsn.json` / `bsnTrfDecsn.json` | 영업양수·양도결정 |
| `GET /api/tgastInhDecsn.json` / `tgastTrfDecsn.json` | 유형자산양수·양도결정 |
| `GET /api/otcprStkInvscrInhDecsn.json` / `otcprStkInvscrTrfDecsn.json` | 타법인주식양수·양도결정 |
| `GET /api/stkrtbdInhDecsn.json` / `stkrtbdTrfDecsn.json` | 주권관련사채권양수·양도결정 |
| `GET /api/cmpMgDecsn.json` | 회사합병결정 |
| `GET /api/cmpDvDecsn.json` / `cmpDvmgDecsn.json` | 회사분할·분할합병결정 |
| `GET /api/stkExtrDecsn.json` | 주식교환·이전결정 |
```

**(5) 캐시 구조 표에 2줄 추가**:

```markdown
| 자금사용내역 | 메모리 `_fund_usage_cache` (최대 20건) | 10분 |
| 주요결정 구조화 | 메모리 `_major_decision_cache` (최대 50건) | 10분 |
```

- [ ] **Step 10.4: 전체 스모크 테스트**

Run:
```bash
python -c "
import dart_risk_mcp
assert dart_risk_mcp.__version__ == '0.5.0'
from dart_risk_mcp.server import mcp
tools = [t.name for t in mcp._tool_manager.list_tools()]
assert 'track_fund_usage' in tools
assert 'get_major_decision' in tools
assert len(tools) == 19, tools
print('version:', dart_risk_mcp.__version__)
print('tools:', len(tools))
"
python -m unittest discover -s tests -v
```
Expected:
- `version: 0.5.0`, `tools: 19`
- 모든 unittest PASS

- [ ] **Step 10.5: 커밋**

```bash
git add dart_risk_mcp/__init__.py CHANGELOG.md CLAUDE.md
git commit -m "$(cat <<'EOF'
chore: v0.5.0 릴리스 — 자금사용·주요결정 API 확장

버전 번프(0.4.0→0.5.0). CHANGELOG에 신규 도구 2개·신호 5개·기존 도구
3개 강화 기록. CLAUDE.md에 18·19번 도구 섹션, 내부 함수 3개, DART
엔드포인트 14개, 캐시 2종을 반영.
EOF
)"
```

---

## 검증 시나리오 (스펙 §5.2 릴리스 게이트)

Task 10까지 완료 후 수행한다.

- [ ] **Step V.1: 회귀 확인 — 삼성전자 `analyze_company_risk`**

Run:
```bash
DART_API_KEY="$DART_API_KEY" python -c "
from dart_risk_mcp.core import analyze_company_risk  # noqa - tool 접근 경로 확인
from dart_risk_mcp.server import analyze_company_risk as tool
print(tool('삼성전자', 90)[:2000])
"
```
Expected: 기존 형식의 리포트가 출력되고, 공시 내용에 따라 신규 섹션(주요 결정 상대방·자금사용내역)이 덧붙음.

- [ ] **Step V.2: 금감원 적발 상장사 5건 재탐지**

`catalog.py`의 MD 카탈로그에서 금감원 감독회보·증선위 의결로 공개된 상장사 5건을 선정(실명·접수번호 공개된 건만). 각 기업에 대해:

```bash
DART_API_KEY="$DART_API_KEY" python -c "
from dart_risk_mcp.server import analyze_company_risk
print(analyze_company_risk('<기업명>', 365)[:3000])
" | tee /tmp/verify_<기업명>.txt
```

각 결과에서 기존 대비 신규 증거(`FUND_DIVERSION`·`FUND_UNREPORTED`·`DECISION_RELATED_PARTY`·`DECISION_OVERSIZED`·`DECISION_NO_EXTVAL` 중 1개 이상)가 탐지되었는지 확인.

- [ ] **Step V.3: 릴리스 게이트 판정**

5건 중 **4건 이상**에서 신규 증거가 탐지되면 v0.5.0 승인. 게이트 미달 시:
- 이상 플래그 규칙 완화 검토 (예: `DECISION_NO_EXTVAL` 금액 기준 50억 → 30억)
- 정규화 누락 필드 검색 (실제 응답에 `inhdamount_totalast_rt` 외 다른 비율 필드가 있는지)
- 위 두 조치로도 미달 시 스펙 5.4의 "과소탐지 감수" 원칙에 따라 게이트 완화 or P1 병행 고려 — 사용자 결정 요청

- [ ] **Step V.4: 검증 결과 요약 문서화**

검증 결과를 `docs/superpowers/specs/2026-04-22-v0.5.0-design.md` 끝에 "검증 결과" 섹션으로 추가(통과 사례·미탐지 사유·후속 조치 메모). 새 커밋:

```bash
git add docs/superpowers/specs/2026-04-22-v0.5.0-design.md
git commit -m "docs: v0.5.0 릴리스 게이트 검증 결과 기록"
```

---

## Self-Review Checklist (구현 시작 전 마지막 점검)

- **스펙 커버리지**: 스펙 §1~§5의 모든 항목이 Task 1~10 중 하나에 대응되는가? ✅
- **타입 일관성**: `flags`는 `list[str]`, `asset_ratio`는 `float`, `amount`는 `int`로 Task 2·3·5·6·7·8·9 전체에서 일관됨. ✅
- **호환성**: 기존 도구 17개의 **시그니처·반환 형식**은 변경되지 않음 — 강화 블록은 모두 기존 출력 뒤에 "추가 섹션"으로만 붙음. ✅
- **Placeholder 스캔**: TBD/TODO/"적절히" 없음. 실패 케이스 포함 실제 코드 블록 전부 삽입됨. ✅
- **변수명 치환 주의 (Task 8)**: `signal_events`·`info`·`disclosure`는 **기존 `analyze_company_risk`·`check_disclosure_risk` 본문의 실제 변수명**으로 치환해야 한다. 현재 코드를 Read한 뒤 이름을 확인하고 삽입할 것.
- **테스트 격리**: 각 테스트 `setUp`에서 해당 캐시를 `.clear()` 해 병렬·반복 실행 시 상호 간섭 방지. ✅

---

## 실행 방식 선택 가이드

- **Subagent-Driven (권장)**: Task별 신규 서브에이전트 + 2단계 리뷰. `superpowers:subagent-driven-development` 사용.
- **Inline Execution**: 현재 세션에서 체크포인트 배치 실행. `superpowers:executing-plans` 사용.

Task 7·8의 "기존 변수명 치환"이 현재 구현에 의존하므로, **Inline이 안전**할 수 있음(현재 세션이 이미 서버 구조를 파악함). 반면 나머지 Task(1~6, 9, 10)는 자족적이라 Subagent도 문제없음.
